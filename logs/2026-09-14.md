# Log de sessão — 2026-09-14

## Conversa 1 — raia de execução (`/apply-wo` + `/wrap` da wo0123)

### Pauta

Aplicar a wo0123 — as quatro regras que o projeto `Sand-Land-Map` devolveu como feedback (`FK-AB`,
`FK-V`, `FK-AC`/`FK-AF`, `DEC-045`), com caso medido nas próprias wo0121/wo0122 deste repositório —
nos dois lados do dogfooding: a casa (`meta/workorders/_TEMPLATE.md`, `meta/CEREBRO.md`) e o que o
kit entrega (`src/index.template.html`, dentro de `buildWoTemplate()` e do gerador do CEREBRO).

### Avançou

- **wo0123 aplicada no repositório real**, as 11 edições em quatro arquivos: checagem negativa
  ancorada em `^`, padrão de conferência RECORTADO nunca redigitado, e valor exigido declara ONDE
  NASCE no cabeçalho do molde de WO (casa e produto); o bloco «Quem roda» ganha o terceiro caso
  (olho humano num renderizador é do dono, MEDIDO); `validate.js` ganha a quinta superfície do
  `C43` (`meta/CEREBRO.md`, que nunca tinha sido comparado com nada) e seis cláusulas novas;
  `D-151` anexada em `DECISIONS.md`.
- **Nos dois arquivos CRLF** (`_TEMPLATE.md`, `index.template.html`), as edições saíram por um
  script Python operando em bytes crus com `\r\n` explícito — nunca `sed` —, para não cair no modo
  de falha que a própria WO institui (âncora que não casa por causa de `\n` puro).
- **`index.html` 877.931 → 880.810 bytes**, harness **19/19 · 105/105 · 0 erros**, `C43` com
  «instalado confere com o gerado em 31 clausulas» (25→31, nenhum check novo), `C28` inalterado.
- **Os três pares negativos previstos, um por direção protegida, todos reproduzidos e
  restaurados**: A (casa perde a clausula do `^` no molde) e B (casa perde «olho humano» no
  CEREBRO — prova direta da quinta superfície nova) e C (produto perde «valor tem fonte») — os
  três deram VERMELHO com a mensagem exata prevista pela WO.

### Decidido

- **D-151 (via wo0123):** as quatro regras da leva do mapa entram no MESMO texto nos dois lados —
  casa e o que o kit entrega —, para que o `C43` confira os dois com um regex só; nenhuma vai às
  Instruções (teto sem folga suficiente); as duas regras de WO ficam fora do CEREBRO gerado (o
  CEREBRO não ensina WO). O `C43` ganha a quinta superfície instalada.

### Tensão aparecida

- **Um desvio de contagem, não-bloqueante:** a tabela da WO previa `src/index.template.html` em
  4.915 linhas; a soma líquida real das linhas inseridas (+14+4+2=+20 sobre 4.897) dá **4.917**. O
  byte count — o número que o próprio checklist manda conferir — bateu exato (389.607), e foi o
  que decidiu a questão; a contagem de linhas da tabela ficou registrada como aproximação da
  própria WO, não corrigida silenciosamente.
- Nenhuma outra. As dez âncoras bateram exatas, o `git diff` ficou na forma prevista, e os três
  pares negativos reproduziram de primeira.

### Números do dia

- `index.html` 877.931 → **880.810 bytes**, a partir de 19 módulos.
- Harness **19/19 · 105/105 · 0 erros**. `C28` inalterado: `padrao 6622/6900 · +Code 514/550 ·
  +ASU 372/400 · compart 372/450 · combo 7508/7600`.
- `CLAUSULAS` do `C43`: 25 → **31**. Superfícies instaladas do `C43`: 4 → **5**.
- Commit `50d7fb7`, empurrado (`0c84279..50d7fb7  main -> main`).

### Pendente

- **Roteiro para o dono** (a própria regra que esta WO institui: olho humano num renderizador é
  dele) — abrir o `index.html` publicado, gerar um pacote em modo Code e conferir visualmente os
  dois itens novos no CEREBRO baixado e no `_TEMPLATE.md` do zip. Ainda não rodado.
- `FK-AE`/`FK-AD` (do ritual, não do molde) e `DEC-056` (revisão externa de WO) seguem fora de
  escopo, para uma WO própria e uma decisão do dono, respectivamente.

## Conversa 2 — raia de execução (`/apply-wo` + `/wrap` da wo0124)

### Pauta

Fechar a leva do mapa: as duas últimas ausências (`FK-AD`, `FK-AE`), as duas do ritual — o que
acontece antes de escrever qualquer coisa —, com a mesma forma («a ressalva que parece rigor»)
que a wo0123 já tinha nomeado. A `FK-AD` tinha caso dentro desta própria casa: o `meta/CEREBRO.md`
afirmava que o mount chega sem árvore de pastas, e isso é falso desde que o FlatDrop passou a
gerar o `_TREE_*.md`.

### Avançou

- **wo0124 aplicada no repositório real**, as 7 edições em quatro arquivos: a árvore do FlatDrop
  como segundo artefato + a frase falsa corrigida + a ressalva com custo em `meta/CEREBRO.md`; as
  mesmas duas regras em `src/index.template.html` (`UPDATE_PROTOCOL` + `HYGIENE_RULES`); duas
  cláusulas novas em `validate.js`, mesmo alvo `cerebro` que a wo0123 instalou; `D-152` em
  `DECISIONS.md`.
- **`index.html` 880.810 → 882.573 bytes**, harness **19/19 · 105/105 · 0 erros**, `C43` com
  «instalado confere com o gerado em 33 clausulas» (31→33, nenhum check novo), `C28` inalterado.
- **Os dois pares negativos previstos, reproduzidos e restaurados**: A (casa perde «árvore por
  pergunta») e B (produto perde «ressalva tem custo») — os dois deram VERMELHO com a mensagem
  exata prevista.
- **Bônus:** o backup remanescente `/tmp/wo0123backup`, que a sessão anterior não tinha
  conseguido apagar (a política da ferramenta bloqueou um `rm -rf` composto), saiu desta vez —
  arquivos removidos um a um, depois o diretório vazio.

### Decidido

- **D-152 (via wo0124):** a árvore do FlatDrop entra por PERGUNTA ao lado do manifesto — os dois
  respondem perguntas diferentes, e descrevê-los por nome faz a segunda nunca ser consultada —, e
  «não verificado» passa a ser proibido para o que está no mount, com a exigência de que toda
  ressalva declare qual leitura a sustenta. Fecha a leva `FK-R`..`FK-AF` do projeto do mapa: das
  seis ausências, quatro saíram na wo0123 e as duas últimas aqui.

### Tensão aparecida

- **Um desvio achado e corrigido na própria aplicação:** a Edição 4 da WO citava indentação de 4
  espaços para a nova linha de `HYGIENE_RULES`, mas as outras ~19 entradas desse array usam 2 —
  um deslize da WO, que citou o padrão de `UPDATE_PROTOCOL` (que é 4 espaços) para o array errado.
  Apliquei primeiro com 2 espaços por consistência; isso derrubou um grep do próprio checklist e
  ficou 2 bytes abaixo do previsto. Revertido para o texto literal da WO (4 espaços), pela regra
  «recorte, não redigite» que a própria wo0123 instituiu — os números bateram exatos depois.
  Fica registrado para uma eventual WO de higiene de indentação nesse array.
- Nenhuma outra. As dez âncoras bateram exatas, o `git diff` ficou na forma prevista, e os dois
  pares negativos reproduziram de primeira (após a correção de indentação).

### Números do dia

- `index.html` 880.810 → **882.573 bytes**, a partir de 19 módulos.
- Harness **19/19 · 105/105 · 0 erros**. `C28` inalterado: `padrao 6622/6900 · +Code 514/550 ·
  +ASU 372/400 · compart 372/450 · combo 7508/7600`.
- `CLAUSULAS` do `C43`: 31 → **33**. Superfícies instaladas continuam **5** (mesmo alvo `cerebro`
  da wo0123, sem superfície nova desta vez).
- Commit `5dd41d5`, empurrado (`be94719..5dd41d5  main -> main`).

### Pendente

- **Roteiro para o dono** (olho humano num renderizador, regra da wo0123) — abrir o `index.html`
  publicado, gerar um `CEREBRO.md` e conferir visualmente os dois trechos novos. Ainda não rodado.
- **`DEC-056`** (revisão externa de WO irreversível) segue como a única ausência da leva do mapa
  ainda em aberto — decisão do dono, exige um segundo agente, não é item de backlog.
- **Correspondência de volta ao `Sand-Land-Map`**, avisando que as seis foram recebidas — é
  correspondência, canal próprio, não uma WO.

## Conversa 3 — raia de execução (`/apply-wo` + `/wrap` da wo0125)

### Pauta

Fechar a extração da leva do mapa: as duas últimas ausências, `FK-R` (o `^` não cobre o número
absoluto — três WOs seguidas erraram um número previsto) e `FK-S` (aviso é dívida do autor —
quando dá para descrever o modo de falha, dá para consertar), mais duas correções de defeito
introduzido pelas próprias wo0123/wo0124 deste repositório: a causa citada no bloco «Quem roda»
vinha de outro episódio da fonte, e uma entrada de `HYGIENE_RULES` ficou com indentação diferente
das outras 21.

### Avançou

- **wo0125 aplicada no repositório real**, as 9 edições em quatro arquivos: `FK-R` e `FK-S` no
  cabeçalho e nas armadilhas do molde de WO (`_TEMPLATE.md`), mais a correção de causa no bloco
  «Quem roda»; as mesmas três em `src/index.template.html` (literais JS de `buildWoTemplate()`),
  mais a correção de indentação de `HYGIENE_RULES` (4 espaços → 2); duas cláusulas novas em
  `validate.js`, alvo `woTemplate`; `D-153` em `DECISIONS.md`.
- **`index.html` 882.573 → 884.945 bytes**, harness **19/19 · 105/105 · 0 erros**, `CLAUSULAS`
  33 → **35** (nenhum check novo), `HYGIENE_RULES` no bloco do array 21+1 → **22+0**, `C28`
  inalterado.
- **Os dois pares negativos previstos, reproduzidos e restaurados**: A (a casa perde a cláusula
  do comando no lugar do número) e B (o produto perde a cláusula do aviso é dívida) — os dois
  deram VERMELHO com a mensagem exata prevista.
- **Um terceiro vermelho, citado pela própria WO como par negativo de graça, não provocado nesta
  aplicação:** o `C42` (frase `nunca peca um resultado que voce nao ensinou a produzir` contígua)
  cairia se as duas últimas linhas do bloco «Quem roda» fossem requebradas de outro jeito — o
  texto final já entrega a frase inteira numa linha, então o check ficou verde, mas a WO registrou
  que ele provou, em sandbox, que protege o que diz proteger.

### Decidido

- **D-153 (via wo0125):** onde o número absoluto é caro de simular, o molde de WO passa a pedir o
  comando medido (antes/depois, com a diferença explicada) em vez do número simulado — a
  aritmética muda de mão, de quem escreve para quem tem o repositório. A seção de armadilhas passa
  a aceitar só o que é propriedade do arquivo ou do ambiente; todo o resto volta como conserto
  antes de publicar. Fecha a extração da leva do mapa: das oito ausências (seis da triagem de
  13/09 e `FK-R`/`FK-S`), todas entraram.

### Tensão aparecida

- Nenhuma. As nove âncoras bateram exatas (incluindo as duas substituições de bloco multi-linha
  em arquivo CRLF), o `git diff` ficou na forma prevista, e os dois pares negativos reproduziram
  de primeira.

### Números do dia

- `index.html` 882.573 → **884.945 bytes**, a partir de 19 módulos.
- Harness **19/19 · 105/105 · 0 erros**. `C28` inalterado: `padrao 6622/6900 · +Code 514/550 ·
  +ASU 372/400 · compart 372/450 · combo 7508/7600`.
- `CLAUSULAS` do `C43`: 33 → **35**.
- Commit `ad9a70d`, empurrado (`3e96447..ad9a70d  main -> main`).

### Pendente

- **Roteiro para o dono** (olho humano num renderizador) — o mesmo herdado da wo0124, com um item
  a mais: no molde do zip, o parágrafo do comando no lugar do número aparece logo antes do
  parágrafo da checagem ancorada em `^`. Ainda não rodado.
- **`DEC-056`** (revisão externa de WO irreversível) segue como a única ausência da leva do mapa
  ainda em aberto — decisão do dono, exige um segundo agente, não é item de backlog.
- **Correspondência de volta ao `Sand-Land-Map`**, avisando que as oito foram recebidas — sai
  depois desta WO, por decisão do dono de 14/09; ainda não escrita.
