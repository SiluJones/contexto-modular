# Log do dia — 2026-08-14

> Escrito em 2026-08-18, a partir dos relatórios de aplicação e do histórico da conversa. **Reconstituição declarada:** o log não foi escrito no dia, e este registro é posterior aos fatos — os números vêm dos relatórios do executor `[medido por instrumento]`, o encadeamento vem da conversa `[relatado pelo dono]`.

## Pauta

Auditar o `.claude/` do Sand-Land-Map antes de gerar o pacote de update deles. A pasta tinha reaparecido no mount depois de o autor corrigir o `.gitignore`.

## Avançou

- **wo0098 aplicada** (`KIT_VERSION 1.117.0`, check **C50** novo, harness 18/18 · 94/94, custo de teto zero).
- Auditados os três arquivos do `.claude/` do Sand-Land-Map, que nunca tinham chegado ao mount: as duas skills (`wrap`, `apply-wo`) e o `settings.json`. **Os três estavam pré-D-108/D-115** — as duas skills mandando entregar o commit em três blocos ao dono, e o `settings.json` sem `Write`, sem `additionalDirectories` e sem `defaultMode`.
- Escrita a mensagem de abertura do update deles (`260814-ABERTURA-update-sand-land.md`), com as três correções do `.claude/` arquivo por arquivo, os 26 pontos da varredura de revogações tabelados, e as duas linhas que exigiam julgamento em vez de substituição (`CEREBRO:187` fica — raia de planejamento; `INSTRUCOES:17` depende de quem a frase dirige).

## Decidido

- **D-132 — varredura muda não é varredura limpa.** O prompt de update ganha um **pré-voo** que confere se as superfícies a varrer chegaram ao mount, **posicionado antes** da ordem de varrer (a posição é travada por asserção de índice no C50). A higiene ganha *«o que o `.gitignore`/`.flatdropignore` esconde, o assistente não audita — e não sabe que não auditou»*, com as duas conferências baratas e a fórmula **«ausência de resultado não é resultado»**. A tabela de gatilhos ganha o evento da varredura muda.

## Tensão aparecida

- **O `.claude/` do Sand-Land-Map esteve fora do mount durante toda a negociação, e a auditoria daqui não percebeu** — varreu o que chegou e chamou de completa, nas duas rodadas. Foi o autor quem apontou.
- É a **quarta ocorrência do mesmo padrão** nesta série: `grep … || echo "ausente"` inventando arquivo faltando (D-126); `/sess[aã]/i` não casando «sessões» (D-130); a auditoria que não abriu o pacote (D-130); e esta. **A regra da D-126 continua sendo violada por quem a escreveu**, e nas quatro vezes quem apontou foi o autor ou o projeto irmão — nunca o instrumento daqui.
- Uma frase mal escrita minha (*«não pude auditar exatamente onde estavam os dois defeitos mais caros do Mapsmith»*) sugeriu que o Mapsmith ainda tinha o problema. **Não tinha** — o merge deles fechou em 20/20. O `.gitignore` contraditório era só do Sand-Land-Map. `[relatado pelo dono]`, corrigido no mesmo turno.

## Próximos passos

- Gerar o pacote do Sand-Land-Map e entregar com a abertura.

## Versão depois deste dia

`v1.117.0` · harness 18/18 · 94/94 · 0 erros · C28 `padrao 6605/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7491/7600`.
