# Log de sessão — 2026-09-15

## Conversa 1 — raia de execução (`/apply-wo` + `/wrap` da wo0126)

### Pauta

Aplicar a wo0126 — cinco regras que estavam FORA da seção «Feedback para o Kit» do `Sand-Land-Map`,
vindas de uma varredura do material que eles NÃO curaram para nós (`DEC-048`, `DEC-052`, o
`ROTEIRO-conferencia-humana` de 29/08, a técnica do `grep` em prosa do CEREBRO deles) — nos dois
lados do dogfooding: a casa (`meta/workorders/_TEMPLATE.md`, `meta/CEREBRO.md`) e o que o kit
entrega (`src/index.template.html`), mais o registro em `meta/DECISIONS.md` (canal CODE neste ciclo).

### Avançou

- **wo0126 aplicada no repositório real**, as 12 edições em 6 arquivos: `DEC-048` (unidade DENTRO
  do número de cobertura), `DEC-052` (variante MOVER com as duas metades — escreve + apaga —,
  substituição do bloco de 3 linhas por 9), a válvula da checagem DE LEITURA (quando o `grep` não
  serve porque a prosa quebra em várias linhas), e o roteiro virando tipo com forma própria (Para ·
  Por que · Formato · Tempo + «o que este roteiro NÃO testa») — as quatro em `_TEMPLATE.md` e
  replicadas linha a linha em `buildWoTemplate()`; «a frase vai sozinha» (o modo de falha OPOSTO ao
  da lacuna: ressalva colada na frase do Próximo lida como segunda tarefa) em `meta/CEREBRO.md` e no
  gerador do CEREBRO; cinco cláusulas novas em `validate.js`; `D-154` em `DECISIONS.md`.
- **Todas as âncoras bateram exatas na primeira tentativa** — nenhuma precisou de recorte
  alternativo, nenhum item caiu em idempotência.
- **`index.html` 884.945 → 888.250 bytes**, harness **19/19 · 105/105 · 0 erros**, `CLAUSULAS`
  35 → **40** (nenhum check novo), `C28` inalterado.
- **Os três pares negativos previstos, um por superfície, todos reproduzidos e restaurados**: A
  (molde perde «roteiro tem forma»), B (CEREBRO perde «a frase vai sozinha») e C (produto perde
  «unidade da cobertura», com rebuild) — os três deram VERMELHO com a mensagem exata prevista pela
  WO. Backup dos três arquivos no scratchpad da sessão antes de cada teste, apagados ao final.

### Decidido

- **D-154 (via wo0126):** quando um projeto vizinho tem acervo, o `DECISIONS` e as «técnicas
  específicas» dele valem tanto quanto a seção que ele escreveu para nós — a varredura do material
  NÃO curado rendeu cinco regras num turno, contra seis da seção curada em dois dias de leitura. A
  `DEC-044` (o Próximo diz de quem é o resultado) já estava no kit; o que faltava era o modo de
  falha oposto ao da lacuna — a ressalva colada — e uma varredura anterior tinha declarado a
  `DEC-044` ausente por grepar a FRASE e não o FATO, erro que o próprio Inventário do kit proíbe.

### Tensão aparecida

- **Um desvio de tamanho, investigado e não é defeito:** o checklist previa (working tree)
  `meta/CEREBRO.md` em 50.155 bytes e `validate.js` em 165.393; um `wc -c` simples devolveu 50.471 e
  167.564. Investigado com `git diff --stat` (mudança mínima e exatamente a pretendida — 1 linha
  trocada em CEREBRO.md, 7 linhas novas em validate.js) e depois com `git cat-file -s` sobre o índice
  apos `git add`: os BLOBS que o git efetivamente grava (normalizados para LF) batem exato com os
  números da WO. A causa é o `core.autocrlf=true` desta máquina, que converte para CRLF no checkout
  arquivos que o `.gitattributes` marca só `text=auto` — contrariando a convenção do próprio
  CLAUDE.md, que só reserva CRLF (por design, `text=crlf` explícito) para `src/index.template.html`.
  Confirmado com `git stash` que o desvio já existia ANTES desta WO — não foi introduzido por
  nenhuma edição, e não afeta o que foi commitado. `core.autocrlf` não foi tocado (config de git é
  fora de escopo de qualquer WO). Fica registrado como o tipo exato de "número previsto carrega o
  fim de linha" que a própria Edição 1 desta WO nomeia.
- Nenhuma outra. As doze âncoras bateram exatas, o `git diff` ficou na forma prevista, e os três
  pares negativos reproduziram de primeira.

### Números do dia

- `index.html` 884.945 → **888.250 bytes**, a partir de 19 módulos.
- Harness **19/19 · 105/105 · 0 erros**. `C28` inalterado: `padrao 6622/6900 · +Code 514/550 ·
  +ASU 372/400 · compart 372/450 · combo 7508/7600`.
- `CLAUSULAS` do `C43`: 35 → **40**.
- Commit `5b7dc6f`, empurrado (`a02e16f..5b7dc6f  main -> main`).

### Pendente

- **Roteiro para o dono** (olho humano num renderizador, regra da wo0123) — o mesmo herdado das
  wo0124/wo0125, com um item a mais: no `CEREBRO.md` gerado pelo kit, o bullet do **Próximo** traz
  a frase nova — certo é «E a frase vai sozinha» aparecer no MESMO parágrafo da regra do resultado,
  não como item separado. Ainda não rodado.
- **`DEC-056`** (revisão externa de WO irreversível) segue como a única ausência da leva do mapa
  ainda em aberto — decisão do dono, exige um segundo agente, não é item de backlog.
- **As 19 análises e a spec do `Sand-Land-Map`** — domínio, varredura declarada pendente, não
  concluída.
- **A carta 04** de volta ao `Sand-Land-Map` — sai depois desta WO, quando a extração fechar.

## Conversa 2 — raia de execução (`/apply-wo` + `/wrap` da wo0127)

### Pauta

Aplicar a wo0127 — fecha a extração do `Sand-Land-Map`: o roteiro (molde de WO) ganha os três campos
que faltavam desde a wo0126 (que só tinha lido o `ROTEIRO-conferencia-humana` de 29/08, não o
`ROTEIRO-passe-de-navegador` de 01/09, que tem três campos a mais), o heurístico «risco que se
confere por comando não é risco» entra no `CEREBRO.md` e no gerador, e o byte previsto passa a
declarar em que PONTA (blob × árvore de trabalho) foi medido — resposta direta ao achado do
relatório de aplicação da própria wo0126 sobre o `core.autocrlf`.

### Avançou

- **wo0127 aplicada no repositório real**, as 8 edições em 4 arquivos: o bloco do roteiro em
  `_TEMPLATE.md` (7→12 linhas: `Estado`, `Preparo`, e «o que me mandar de volta» — o contrato de
  retorno); a linha do byte previsto ganha a declaração da ponta; as MESMAS duas replicadas em
  `buildWoTemplate()`; o heurístico do risco em `meta/CEREBRO.md` e no `L.push` do gerador; três
  cláusulas novas em `validate.js`; `D-155` em `DECISIONS.md`.
- **Todas as âncoras bateram exatas na primeira tentativa.**
- **`index.html` 888.250 → 889.971 bytes**, harness **19/19 · 105/105 · 0 erros**, `CLAUSULAS`
  40 → **43** (nenhum check novo), `C28` inalterado.
- **Os três pares negativos previstos, um por superfície, todos reproduzidos e restaurados**: A
  (molde perde «o que me mandar de volta»), B (CEREBRO perde o heurístico do risco) e C (produto
  perde «qual das duas pontas», com rebuild) — os três deram VERMELHO com a mensagem exata prevista.

### Decidido

- **D-155 (via wo0127):** fecha a extração do `Sand-Land-Map` — catorze regras entraram ao todo,
  a maioria vinda do material que eles NÃO curaram para nós. Fica aberta só a `DEC-056` (decisão do
  dono, exige segundo agente); a carta 04 sai depois desta WO.

### Tensão aparecida

- **A própria tabela de bytes previstos da wo0127 errou o que ela mesma ensina a declarar.** A
  Seção 2 rotulou como «blob» três valores (`_TEMPLATE.md`, `index.template.html`, `index.html`)
  que na verdade são o tamanho da ÁRVORE DE TRABALHO — confirmado com `git cat-file -s` após
  `git add` e com contagem de `\r` no blob (zero CR: puro LF). Só `meta/CEREBRO.md` e `validate.js`
  bateram exato no blob previsto. Não bloqueou o commit (as âncoras, a forma do diff e o harness são
  o que decide verde/vermelho, e todos bateram); registrado no relatório da aplicação.
- **Achado de higiene no próprio `STATUS.md`, corrigido nesta sessão (`FIX-038`):** o fecho da
  wo0126 tinha sobrescrito a entrada «Última sessão» da wo0125 em vez de rebaixá-la para «Sessão
  anterior» — violando o append-only do `CLAUDE.md`. O bullet da wo0125 foi recuperado do commit
  `a02e16f` e reinserido no lugar certo; nenhum conteúdo foi perdido, e a lição de método fica em
  `meta/DECISIONS.md`.

### Números do dia

- `index.html` 888.250 → **889.971 bytes**, a partir de 19 módulos.
- Harness **19/19 · 105/105 · 0 erros**. `C28` inalterado: `padrao 6622/6900 · +Code 514/550 ·
  +ASU 372/400 · compart 372/450 · combo 7508/7600`.
- `CLAUSULAS` do `C43`: 40 → **43**.
- Commit `6badc7d`, empurrado (`470462d..6badc7d  main -> main`).

### Pendente

- **Roteiro para o dono** — o pendente das wo0124-0126 precisa ser reemitido no formato NOVO desta
  WO (cinco linhas de cabeçalho, Preparo, itens, «o que NÃO testa» e «o que me mandar de volta»);
  tarefa do chat no fecho, não de quem aplica. Ainda não rodado.
- **`DEC-056`** segue em aberto — decisão do dono, exige um segundo agente.
- **As 19 análises do `Sand-Land-Map`** seguem por amostragem, não por varredura integral —
  recomendação registrada de não fazer essa varredura (rendimento em queda: seis → cinco → três).
- **A carta 04** de volta ao `Sand-Land-Map` — sai agora, fecha o ciclo do lado deles.
