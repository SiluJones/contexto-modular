# Log de sessão — 2026-08-12

> Primeiro log do dia — e o kit chegou ao fim do dia sem nenhum, o mesmo problema que a wo0092 corrige.

## Resumo
Sessão de aplicação da wo0092: auditoria completa do Mapsmith (56 arquivos) revelou quatro achados — a skill `wrap` publicada nunca escrevia o log do dia (IDEA-056 do Mapsmith), o CEREBRO abria exceção contradizendo a própria regra de "nunca bloco colável" para pedido de medição, faltava a regra de ler antes de sobrescrever, e a correção obrigatória do `settings.json` chegava truncada em 120 caracteres no manifesto. Nove edições aplicadas em `src/index.template.html` (5 regiões) e `validate.js` (C37 ajustado, C47 novo), mais D-126 em `meta/DECISIONS.md` e registro em `meta/IDEAS.md`.

## Avançou
- `KIT_VERSION` 1.110.1 → 1.111.0.
- Skill `wrap` gerada passa a escrever `logs/AAAA-MM-DD.md`, com a distinção criar/regenerar explícita.
- CEREBRO fecha a brecha da medição colada na mensagem — "não é WO" agora significa "outro artefato", nunca "vai colado".
- `HYGIENE_RULES` e `TRIGGERS_BASE` ganham a regra de ler antes de sobrescrever e a restrição do dono como MEDO, não especificação.
- `buildUpdatePack` ganha o campo `obrigatorio` (por arquivo) e o manifesto ganha seção própria "Correções obrigatórias", sem truncamento de 120 chars.

## Decidido
- D-126 registrada em `meta/DECISIONS.md`: os quatro achados, a nota de instrumento sobre o `||` do grep na wo0090 (afirmação errada de arquivo ausente), e o fechamento da IDEA-056 do Mapsmith pelo lado do kit.

## Tensão aparecida
- Durante a prova de vida (forçar o vermelho renomeando `obrigatorio`→`obrigatorio_x`), um `sed -i` no Git Bash converteu **todo** o `src/index.template.html` de CRLF para LF (4666 quebras de linha soltas) — dano fora do escopo da edição pretendida, silencioso até a checagem de bytes acusar `index.html` fora do valor esperado. Corrigido reescrevendo o arquivo inteiro com `\r\n`, confirmado 0 CR soltos antes da reconversão. Lição: `sed -i` em arquivo CRLF neste ambiente não é seguro, mesmo para uma substituição de uma linha — usar Edit/Node para qualquer toque em `src/index.template.html`.

## Próximos passos
- `/wrap` fecha o `meta/STATUS.md` (v1.111.0, 91/91, C47 antes do C46, D-126) — deixado de fora desta aplicação por instrução explícita da wo0092.
- Avisar o Mapsmith, ao entregar o pacote, que a IDEA-056 fecha pelo lado do kit.

## Versão atual depois desta sessão
- v1.111.0, 18/18 nichos, 91/91 checagens, 0 erros. `index.html` 808.190 bytes.
