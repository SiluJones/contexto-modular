# STATUS — Kit de Contexto Universal — 2026-08-22

> Rolante: só o agora + próximos passos. Item resolvido sai daqui (vai pro CHANGELOG).
> Versão atual: **v1.120.0**. Teste: **18/18 nichos, 97/97 checagens, 0 erros JS** + C53 (a conferência sai de quem tem o viés: campo «Âncoras lidas em» recusável por quem aplica no modelo de WO, P8 tipa a afirmação por espécie — ferramenta/símbolo/estado do mount — e nega que declarar «não li» autorize entregar em cima, comando entregue INTEIRO com o executor nomeado, item Estado começa pelo manifesto e registra que o mount zera a data por arquivo, Arquivar/Manter exaustivo com leitura forte e prazo, próximo comando cru e sozinho no caminho verde, `AskUserQuestion` serve para escolher e não para disparar, passo 1b confere o relatório anterior contra o repo, wo0102) + C52 (duas coberturas, o esqueleto do relatorio e o gatilho do verde: manifesto passa a declarar «arquivos do pacote comparados: N/N» e «ocorrencias varridas no repo: N, corrigidas, justificadas» separadas, com a segunda podendo ficar pendente sem invalidar o merge; regra nova manda varrer o proprio `.gitignore`/`.flatdropignore` como conteudo; CEREBRO ganha o esqueleto do relatorio de sonda — cabecalho com sha256/mtime, data como unica linha nao-deterministica, `## ALARMES (N)` com negacao embutida em N=0, secao sem insumo saindo NAO CONFERIDA, `## O QUE NAO FOI OLHADO` com lista fixa e dinamica —; e o TRIGGERS_BASE ganha o gatilho que faltava a FK-M: verde dispara a pergunta de qual das duas perguntas ele responde, na mesma linha, wo0100) + C51 (o numero declarado e piso, e as regras do merge vem na frente: manifesto entrega o comando de varredura com alvo declarado — zero, menos o que se justifique item a item —, mais o aviso de que `grep -c` conta linhas e nao ocorrencias; secao «Aplique PRIMEIRO» com quatro itens, posicionada antes da tabela de arquivos; checklist da WO manda simular o texto final de TODAS as edicoes antes de declarar o numero esperado, e o menu numerado em texto vira ferramenta de botoes (`AskUserQuestion`) nas duas skills geradas e nas do proprio KCM, wo0099) + C50 (varredura muda nao e varredura limpa: o pacote confere se as superfícies chegaram antes de mandar varrê-las — pré-voo no prompt de update, posição travada por índice antes da ordem de varrer; higiene ganha a regra do que o `.gitignore`/`.flatdropignore` esconde, com a fórmula «ausência de resultado não é resultado»; gatilho novo para varredura que não achou nada onde deveria, wo0098) + C49 (o retorno do primeiro merge dos `fusao`: o gerado deixa de usar o vocabulário que ele próprio revoga — cadência «ao final de cada/uma sessão» varrida de 15 linhas no CEREBRO, Instruções, `CLAUDE.md` e nos modelos de 16 nichos —, o funil da sonda ganha o terceiro estado `instrumento` (versionado, testado, com veredito, gatilho de promoção na segunda execução), e a propriedade «nada truncado» passa a nomear amostra como defeito distinto de truncamento, wo0094) + C48 (o pacote não confunde quem lê: carimbo do manifesto passa a dizer `skills-do-nicho`, com nota de que `.claude/skills/` é outra coisa e skill própria do projeto não é sobra; revogação distingue texto que MANDA — sai — de texto que RELATA fato histórico medido — fica; e a correção obrigatória do `Write` deixa de dizer que a falta "nega" e passa a dizer que o efeito é atrito — pedido de aprovação a cada arquivo novo, wo0093) + C47 (o fecho escreve o log e a medição também é arquivo: a skill `wrap` gerada passa a escrever `logs/AAAA-MM-DD.md` — cria se o arquivo do dia não existe, acrescenta se existe —, o CEREBRO fecha a exceção que deixava pedido de medição ir colado na mensagem do executor, `HYGIENE_RULES`/`TRIGGERS_BASE` ganham ler antes de sobrescrever + restrição do dono como MEDO e não especificação, e a correção obrigatória do `.claude/settings.json` sai do campo `role` truncado em 120 caracteres e ganha campo/seção próprios «Correções obrigatórias» no manifesto, wo0092) + C46 (as revogações alcançam o instalado: as três decisões que MUDARAM comportamento entram na lista `REVOCATIONS` — bloco de git, vocabulário sessão→turno, blocos soltos em modo Code —, e o manifesto/prompt do update passam a mandar varrer pelo FATO, começando pelas skills, wo0090) + C45 (correspondência entre projetos vira tipo nomeado: seção nova no CEREBRO — carta com nome `AAMMDD-<quem>-para-<quem>-NN-<assunto>.md`, contador `NN` único e compartilhado pelos dois lados, transitoriedade com o custo de versionar correspondência nomeado, espera sem gatilho vira item com prazo —, e o instantâneo de dado derivável vira regra de higiene universal: escreva a regra, não o valor, wo0089) + C44 (sonda e exploração como par: seção nova no CEREBRO — sonda determinística/exploração sem hipótese prévia, três propriedades do relatório, proibição de veredito com a razão, existência não é aptidão —, funil ganha o degrau que mede antes de raciocinar, «Quem abriu, fecha» nos TRIGGERS_BASE/HYGIENE_RULES, política do gatilho oportunista, item novo no checklist da WO, wo0088) + C43 (o instalado não fica atrás do gerado: o KCM é usuário do próprio kit, as skills `wrap`/`apply-wo` e o `settings.json` do repositório migram para a ordem verde/vermelho que o kit publica, check novo abre `.claude/` do repositório e confere cláusula por cláusula contra `buildCodeKitFiles()`, nos dois lados, wo0087) + C42 (a conferência sai do artefato: três campos por passo de verificação — Quem roda, Chega no ramo?, Prova de vida —, seção `## Inventario` antes da primeira edição com contagem declarada e não truncada, e o gatilho no item «Próximo (b)» do bloco de fecho para a frase só cobrar resultado que o dono sabe produzir, wo0086) + C41 (o fecho em modo Code registra em vez de listar: canal por doc — WO cirúrgica × arquivo inteiro —, log do dia com gatilho de evento, regenerar × criar, origem do fato relatado, wo0085) + C40 (vocabulário turno x conversa: ritual e tabela de gatilhos falam em turno/conversa nos 18 CEREBROs + Instruções; prompt de update carrega «Linhas revogadas» e carimbo de modos ANTES de `Arquivos no pacote:`; modelo de WO inclui a própria WO no `git add`, wo0084) + C39 (skill `ficha-de-choque` no narrative: teste de entrada, quatro campos com citação literal, marca de confiança, 🔧 com critério duplo, ciclo de ticket de leva e fronteira do método, wo0083) + C38 (higiene universal + o update que sabe subtrair: quatro regras nos 18 CEREBROs, gatilho do fecho na tabela de eventos, registro `REVOCATIONS` publicado no manifesto, carimbo de modos completo com a regra de reportar-e-não-remover: wo0082) + C37 (settings.json válido, Write no allow, push antes do relatório, CONTINUIDADE não é snapshot: wo0081) + C36 (modelo de análise no pacote + contagem repetida no STATUS: natureza `modelo-em-espera`, pasta preguiçosa preservada, wrap confere valor antigo, válvula cita IDEAS por ID) + C35 (medição delegada: seção no CEREBRO dos 18, gatilho só no Modo Code, seção sem âncora no modelo de WO, formato de retorno no kit do Code) + C34 (degrau de saída do funil de análise: teste de quem decide, achado vira armadilha da ordem de trabalho, saída do CRLF no modelo de WO, mensagem entre frentes não vira pasta) + C33 (leitura antes do trabalho: abertura de turno antes de qualquer ferramenta, carimbo `Base:` no Estado, falsa confirmação do sandbox, canal rápido do relatório) + C32 (anatomia do bloco gerado: cinco regras + duas obrigações no CEREBRO, marcadores não citados em comentário, Arquivos Críticos no CONTEXT, P11 com a metade estrutural) + C31 (paridade dos templates de nicho: IDEAS de todo nicho tem os dois endereços de feedback, formato legado não é alternativa, log por dia acumula sessões) + C30 (contrapeso do gatilho de análise: teste barato antes do gatilho, cláusula de abandono, kit do Code grava o relatório em arquivo) + C29 (fecho da leva sand-land: gaveta Adiadas com gatilho de volta no IDEAS de cada nicho, HISTORY do dev/research ganha pesquisa-de-convenções e autópsia, pacote de update fica no mount até o merge fechar ou declara cobertura de leitura) + C28 (teto por configuração: 6900 no padrão, orçamento por modo — Code ≤550 · ASU ≤400 · compartilhado ≤450 —, total ≤7600 nos combos, CEREBRO publica os três orçamentos) + C27 (leva sand-land: modelo de WO, seção Técnicas específicas protegida do template-update, Estado distingue não-verificado de não-legível pelo canal, contrapartida do carimbo de emissão) + C26 (curadoria das linhas de modo: versão curta nas Instruções, definição completa no CEREBRO, incremento dos modos vigiado pelo orçamento Code+ASU) + C25 (protocolo de update e gatilho da releitura: estado do repo antes de comparar, template não substitui vivo, `.claude/commands/` legado, `SPEC.md` diz SDD/sob-demanda/não-é-WO) + C24 (convivência gerado × manual: bloco marcado no `.flatdropignore`, Estado verificado no turno, gatilho de análise por formato) + C23 (a cópia não é a fonte da verdade: regra nos 18 CEREBROs, ausente das Instruções) + C22 (disciplina de entrega no modo Code: WO com `/apply-wo` junto, bloco git inteiro, relatório na raia de execução) + integridade dos chips (FIX-004, agora de fato validada — ver D-070) + smoke/round-trip do HUB + G6 (skills-pack) + G7 (modo Code, migrado pro workmode) + G8 (selos de estado, agora lendo `WORK_MODES`) + G16 (teto de pior caso ≤ 7600) + G17 (SO no modal + D-059) + G18 (chips do career) + G19 (CEREBRO ensina a refinar as próprias Instruções — agora **dever proativo**, D-088) + G22 (contador de instrução, `INSTR_TETO`) + G23 (prompts de setup mode+entrega-aware) + G24 (`KIT_VERSION` exposto) + G25 (ritual cita o doc-âncora de cada nicho; Instr não cita `.md` inexistente) + C10 (refino narrativo spec0048: 5 erros nomeados + cena-existe + notas-revisão + eco físico + gatilho revisada) + C11 (universais leva C spec0049: pedido composto + gênero em rename + sincronia Instr↔CEREBRO) + C12 (nomes com opções na ficção spec0050: narrativa ambos os modos + game/rpg/comics) + C13 (E-ASU: CEREBRO avisa que ASU não cobre `.docx`/binários; B6: seção Retcon no template CONTINUIDADE da narrativa) + C14 (adesão ao ritual spec0052: mount-check por turno + memória×mount + fix vazamento ASU no modo Code) + C15 (rename spec→WO spec0053: caminhos/comando/prosa; sem quebrar pt-BR/`aspect-ratio`/história) + C16 (SDD leve spec0054: `SPEC.md` no dev, critérios de aceite verificáveis nos prompts J/dev e H/game, cláusula de ambiguidade no princípio `analyze`) + C17 (auto-refino obrigatorio wo0055: dever proativo, liberdade de promover regra, feedback ao kit) + C18 (motor do enxugamento wo0056-A/B: campo `short` curado nas Instruções, definição completa no CEREBRO — **96 curados** nos 18 nichos, wo0057+wo0059, WO-B concluída) + C19 (bloco de fecho de turno padronizado wo0058, **corrigido na wo0060, e v2 na wo0064**: ordem Próximo→Estado→Arquivar/Manter→Config→Handoff, «Próximo» em duas partes — ação + pedido de volta —, «Arquivar/Manter» em lista, gatilho nas Instruções + formato completo e personalizável no CEREBRO) + C20 (nome `AAMMDD-HANDOFF-BRIEF.md` nos prompts E/F de transferência e retomada, wo0061) + C21 (análise antes do compromisso wo0063: seção nova no CEREBRO gerado dos 18 + gatilho nas Instruções + funil `analiseFunil()` + pasta preguiçosa `analises/`/`specs/` + `.flatdropignore` gerado corrigido). `N[dev]` em **6000**/`INSTR_TETO` (6900, folga 900). O mais apertado agora é **`narrative`** 6605/6900 (folga 295), depois **`game`** 6513/6900 (folga 387). **Orçamento por modo (vigiado pelo C28 a cada build):** compartilhado **372**/450 · Code **514**/550 · ASU **372**/400 · combo **7491**/7600. `KIT_VERSION 1.119.0`. **Enxugamento A→B→C concluído** (D-090/D-091/D-093/D-094); **curadoria das linhas de modo concluída** (D-103); **leva sand-land concluída** (D-104, D-106); **teto por configuração concluído** (D-105); **contrapeso do gatilho de análise + relatório do Code em arquivo concluído** (D-108); **paridade dos templates de nicho concluída** (D-109); **anatomia do bloco gerado + metade estrutural do P11 concluída** (D-110); **leitura antes do trabalho concluída** (D-111); **degrau de saída do funil de análise concluído** (D-112); **medição delegada concluída** (D-113); **modelo de análise no pacote + contagem repetida no STATUS + IDEAS por ID concluído** (D-114); **settings.json válido + push antes do relatório + CONTINUIDADE cresce concluído** (D-115); **higiene universal + protocolo de update que sabe subtrair concluído** (D-116); **skill `ficha-de-choque` no narrative concluída** (D-117); **vocabulário turno x conversa + prompt de update alcança projeto desatualizado + WO no próprio git add concluído** (D-118); **fecho em modo Code registra em vez de listar concluído** (D-119); **a conferência sai do artefato concluído** (D-120); **o KCM aplica a si mesmo o kit que publica concluído** (D-121); **sonda e exploração como par + quem abre fecha + gatilho oportunista concluído** (D-122); **correspondência entre projetos como tipo nomeado + instantâneo de dado derivável concluído** (D-123); **as revogações alcançam o instalado concluído** (D-124); **o gerado não contradiz a própria revogação concluído** (D-125); **a skill de fecho escreve o log + medição também é arquivo concluído** (D-126); **o primeiro merge real expôs carimbo ambíguo + revogação sem escopo + diagnóstico refutado concluído** (D-127); **o kit aplica a própria revogação ao gerado + terceiro estado da sonda (instrumento) + amostra não é cobertura concluído** (D-128); **a refinaria ganha o terceiro caso (título que estrutura muda junto) + domínio de sessão vira exceção nomeada no instrumento concluído** (D-129); **o pacote de update varrido pelo C49 inteiro, nos 18 nichos concluído** (D-130); **a FK-M fecha: o relatório declara qual das duas perguntas o instrumento não responde, ao lado do que não foi olhado, concluído** (D-131); **o pacote confere se as superfícies chegaram antes de mandar varrê-las concluído** (D-132); **o número declarado é piso, as regras do merge vêm na frente, e o menu vira botão concluído** (D-133); **duas coberturas declaradas separadas, o esqueleto do relatório de sonda e o gatilho que faltava ao verde concluído** (D-134); **a conferência sai de quem tem o viés concluído** (D-135). (Detalhe do método no CONTEXT §3.)
> **i-N36 fase "topbar inteiro" parte 2 aplicada** (spec0031: cluster de ação nasce com a engrenagem → modal `<dialog>` de configuração; afixo migrado da aba Templates para a seção Projeto do modal). Resta da i-N36 o atualizar (spec0033/0034) e o encaixe do atualizador i-N40. Próximas frentes: spec0032 (aba Nicho + campos `multi`/`segmented`), spec0033 (download estruturado), spec0034 (atualizar), **spec0044 (paleta unificada dos nichos, i-N49)**.

> **Mudanças nesta revisão (v1.33.0):** códigos de área do HUB **curados** por nicho (DEV, não "DESE") + **variador** de duplicata (DEV0/DEV1/DEV2); rótulo de grupo reescrito (D-027). Capturada a **direção estratégica**: refator modular + **i18n com idioma misto** (i-N13 expandido + i-N26 em IDEAS) — sem código até decisão. Respostas do usuário às perguntas em aberto registradas em IDEAS. (Histórico completo de versões no CHANGELOG.)

## 💬 Última sessão (2026-08-22 — v1.120.0)
- **A conferência sai de quem tem o viés (D-135, wo0102):** o dono aceitou as opções B1, B2 e E da análise `260820-ANALISE-a-afirmacao-verificavel.md` e recusou a gradação de esperar um segundo consumidor — correção de defeito não espera consumidor aparecer. A família de regras que falhou dez vezes em duas séries era autoendereçada (quem confere é quem tem o viés); esta WO converte quatro delas no desenho que nunca falhou («PARE e reporte»: a conferência passa para quem aplica). **Onze edições:** o modelo de WO ganha o campo obrigatório **«Âncoras lidas em»**, recusável por quem aplica se vier vazio; o P8 tipa a afirmação verificável em três espécies (ferramenta · símbolo · estado do mount) e nega que declarar «não li» autorize entregar em cima; **HYGIENE_RULES** ganha a regra do comando entregue INTEIRO com o executor NOMEADO (o kit não tinha regra nenhuma sobre isso — só a proibição de caminho ambíguo em comando destrutivo, e o comando que estourou no Mapsmith era um `verify` que só lê); o item **Estado** do bloco de fecho passa a **começar pelo manifesto** — a exceção do «commit não legível pelo mount» estava escrita depois da instrução e chegava tarde, família da FK-O — e registra o fato medido de que o mount zera a data por arquivo (conferir mudança é conferência de conteúdo, nunca de data); **Arquivar/Manter** vira exaustivo, com «Arquivar» só para o lido inteiro no turno e prazo para «Manter: não li»; o modelo de WO e as skills geradas ganham a enunciação do **próximo comando** cru e sozinho na última linha do caminho verde; e o `AskUserQuestion` ganha o limite medido — serve para ESCOLHER, não para DISPARAR, porque não contorna `disable-model-invocation`. **Duas provas negativas rodadas** (trocar a recusa por conferência opcional, e reinverter a ordem da exceção do manifesto) confirmaram que o C53 cobra as duas asserções certas. **O C43 cobrou a casa sozinho de novo** — quarta cláusula nova apontando `.claude/skills/*` do próprio KCM atrás do gerado, antes do autor pensar nisso (D-121 funcionando). Lido no `260820-2247.txt` do Mapsmith: a afirmação deles de que a regra do manifesto era «falsa» foi refutada por leitura — a exceção existe, só chega tarde; e a conclusão de que o `mtime` por arquivo é dispensável é meio certa — o cabeçalho do manifesto responde «o lote é novo», não «qual arquivo mudou». **Registrado, não decidido:** a i-N56 disparou — o C31 reprovou a primeira redação da Edição 3 por testar a prosa literal, não o comportamento; fica anotado no IDEAS com o caso concreto. Check **C53** novo, e quatro cláusulas novas no **C43**. `KIT_VERSION 1.120.0`. **Custo de teto ZERO** (C28 idêntico `padrao 6605/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7491/7600`). `index.html` de **823.845 → 828.852** bytes. Harness **18/18, 96/96 → 97/97, 0 erros**. **Fora do escopo:** a carta corrigida ao FlatDrop (sai pela raia do chat); mexer no C31 (está verde e a Edição 3 foi escrita para mantê-lo assim). **Base:** `meta/analises/260820-ANALISE-a-afirmacao-verificavel.md` (opções B1, B2, E) + os nove itens do `meta/IDEAS.md` (wo0101) + `260820-2247.txt` do Mapsmith. Nada criado fora do repositório além do relatório em `../AAMMDD-HHMM-code-kcm.txt`.

## 💬 Sessão anterior (2026-08-18 — v1.119.0)
- **Duas coberturas declaradas separadas; o esqueleto do relatório de sonda entra como FORMA; e o verde ganha o gatilho que o campo da FK-M não tinha (D-134, wo0100):** a carta 02 do Sand-Land-Map respondeu à pergunta de método que fechava a wo0099 — **o «20/20» contava arquivos comparados contra o pacote, não ocorrências varridas no repo**. O número era verdadeiro e a conclusão era errada; um número sozinho preenche o lugar dos dois. **(a) FK-Q:** o manifesto passa a pedir **«arquivos do pacote comparados: N/N»** e **«ocorrências varridas no repo: N, corrigidas, justificadas»** separadas, com a segunda podendo ficar declarada como **pendente** sem invalidar o merge — a válvula que torna a regra aplicável. **(b)** a varredura completa deles (**54 ocorrências, 9 corrigidas, 45 justificadas em quatro categorias**) achou um sétimo arquivo que a nossa tabela não listava: o `.flatdropignore`. A observação vale mais que o achado — *«a regra nova pega o que os arquivos escondem e não pega o arquivo que esconde»*; o `.gitignore`/`.flatdropignore` nunca são lidos como conteúdo. **(c)** o campo da FK-M (D-131) não tinha hora: *«ninguém perguntou qual das duas o verde respondia»* — item (7) do Mapsmith batendo numa regra que nós mesmos tínhamos acabado de escrever. `TRIGGERS_BASE` ganha o gatilho: verde dispara a pergunta «está lá? ou presta?», declarada **na mesma linha do verde, não no rodapé**. **(d)** a posição deles sobre a skill de sonda é «ainda não», e o kit a acata — esta WO escreve **o contrato**, não a skill: o esqueleto do relatório entra no CEREBRO, destilado dos três scripts que o autor subiu (`probe-data.mjs`, `scan-games.mjs`, `probe_pacote.py`), não das descrições. Três detalhes que só o uso revela: data como única linha não-determinística (torna dois relatórios diffáveis), lista **fixa** do que a sonda nunca olha (mais valiosa que a dinâmica), e a negação colada ao zero de alarmes na própria linha. Check **C52** novo, com nove provas negativas — a quinta consertou o próprio check (o recorte do bloco de código, não a seção inteira, senão a frase da prosa abaixo do bloco mascarava o esqueleto vazio). Terceiro caso registrado de prova negativa consertando o instrumento (C45/D-123, C48/D-127). `KIT_VERSION 1.119.0`. **Custo de teto ZERO** — C28 idêntico `padrao 6605/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7491/7600`, folga do `narrative` em **295**. `index.html` de **820.575 → 823.845** bytes. Harness **18/18, 95/95 → 96/96, 0 erros**. Prova de vida feita: marca da data trocada por `(data)` dentro do bloco de código, C52 falhou com a mensagem certa, revertido, verde de novo. **Fora do escopo:** escrever a skill/molde de sonda (posição deles é «ainda não»); os 9 pontos que eles vão corrigir (WO 0043 deles já escrita); corrigir o `.flatdropignore` do kit (já está limpo). **Base:** `260818-sand-land-para-kcm-02-fecho-do-merge.md`, mais os três scripts do autor. Nada criado fora do repositório além do relatório em `../AAMMDD-HHMM-code-kcm.txt`.

## 💬 Sessão anterior (2026-08-18 — v1.118.0)
- **O pacote descreve bem O QUE mudar e mal COMO conduzir a mudança: número como piso, regras do merge na frente, e o menu vira botão (D-133, wo0099):** o merge do Sand-Land-Map fechou em **20/20, seis WOs, cinco commits** (`af7fa1b`, DEC-047) e devolveu três itens de feedback — **FK-N**, **FK-O**, **FK-P** — mais um pedido direto do autor, os quatro sobre **o processo de merge**, não sobre o conteúdo. **(a) FK-N:** o manifesto declarou 11 pontos de revogação para o `CEREBRO.md` deles; a varredura pelo fato achou **22** — *«quem escreve o manifesto olha as SEÇÕES que mudou; a linha revogada mora no ITEM»*, e o desvio cresce com a distância entre versões, então o projeto que mais precisa do merge recebe o número mais otimista. Prova colhida no próprio merge deles: fecharam 20/20 ENCERRADO e ainda restam ocorrências da cadência revogada em seis arquivos (`CONTEXT.md`, `HISTORY.md`, `GLOSSARY.md`, `STATUS.md`, `README.md`, `_TEMPLATE.md` de WO) que a lista de 20 não enumerava com o termo. O manifesto passa a entregar o **comando de varredura** (`grep -rn`), com alvo declarado — zero, menos o que se justifique item a item — e o aviso de que `grep -c` conta linhas, não ocorrências. **(b) FK-O:** a cláusula que impede o pedido impossível estava na fatia 3 do merge deles, e o defeito que ela impede aconteceu na fatia 1 — *«o grep deu N (…) não faço ideia de como descobrir para te passar»*, repetido dois turnos depois. O manifesto ganha a seção **«Aplique PRIMEIRO»**, com quatro itens (item «Próximo», medição delegada, varredura pelo fato, três campos por passo de verificação), posicionada ANTES da tabela de arquivos — quem lê a tabela primeiro já escolheu a ordem antes de chegar no aviso. **(c) FK-P:** o checklist de uma WO precisa simular o texto final de TODAS as suas próprias edições — a wo0041 deles pedia `grep -c = 1` para frases que a própria Edição 11 citava de novo, o grep deu 2 e custou um ciclo vermelho falso; o modelo de WO ganha o aviso, com a forma de declarar termo citado por mais de uma edição. **(d) O pedido do autor, maior alcance:** o menu numerado escrito no corpo da mensagem ainda obriga o dono a digitar a escolha, e por isso continua sendo prosa — as duas skills geradas (`wrap`, `apply-wo`) e as instaladas do próprio KCM passam a oferecer as saídas pela ferramenta `AskUserQuestion`, recomendada marcada, com fallback declarado quando a ferramenta não existir. **O C43 e o C37 reprovaram sozinhos** assim que o texto do menu mudou no gerador, cobrando que `.claude/skills/*` do próprio KCM acompanhasse — a D-121 funcionando sem ninguém lembrar dela. Check **C51** novo, com onze provas negativas. `KIT_VERSION 1.118.0`. **Custo de teto ZERO** — manifesto, modelo de WO e skills vivem fora de `buildInstr`: C28 idêntico `padrao 6605/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7491/7600`, folga do `narrative` em **295**. `index.html` de **816.174 → 820.575** bytes. Harness **18/18, 94/94 → 95/95, 0 erros**. Prova de vida feita: seção «Aplique PRIMEIRO» movida temporariamente para depois da tabela de arquivos, C51 falhou com a mensagem certa, revertido, verde de novo. **Aberto, para levar ao Sand-Land-Map:** os seis arquivos com resto de cadência, e a pergunta de método — o «20/20» deles contava arquivos comparados ou arquivos varridos? **Base:** os sete relatórios do merge do Sand-Land-Map (17–18/08) e o transcrito lido bloco a bloco. Nada criado fora do repositório além do relatório em `../AAMMDD-HHMM-code-kcm.txt`.

## 💬 Sessão anterior (2026-08-14 — v1.117.0)
- **Varredura muda não é varredura limpa: o pacote confere se as superfícies chegaram antes de mandar varrê-las (D-132, wo0098):** auditoria do `.claude/` do Sand-Land-Map, possível só hoje — a pasta esteve **ausente do mount durante toda a negociação** e ninguém percebeu, porque o `.gitignore` deles tinha uma contradição interna (`# NAO ignore .claude/…` duas linhas acima de `.claude/`), o FlatDrop obedeceu à regra e pulou a pasta, e a auditoria **varreu o que chegou e chamou de completa**. Repostos hoje, os três arquivos estavam pré-D-108/D-115: as duas skills mandavam entregar o commit em três blocos ao dono, o `settings.json` sem `Write`. **São exatamente as superfícies que o pacote manda varrer primeiro** — o merge teria rodado a instrução sobre um mount sem elas e concluído «limpo». **Princípio geral:** instrução que executa e não faz nada é pior que instrução nenhuma; ausente, a lacuna fica visível, rodando no vazio produz silêncio, e silêncio é lido como limpeza. Remédio em três superfícies: **pré-voo** no prompt de update (pergunta se as superfícies chegaram, ANTES da ordem de varrer, posição travada por check de índice), **higiene** nova (o que o `.gitignore`/`.flatdropignore` esconde, com as duas conferências baratas e a fórmula «ausência de resultado não é resultado») e **gatilho** novo (varredura que não achou nada onde deveria achar algo). **Quarta vez nesta negociação** em que uma ausência relatada por mim era do instrumento, não do arquivo — a D-126 continua certa e continua sendo violada por quem a escreveu; daí o remédio ser check e gatilho, não promessa. Check **C50** novo, com cinco provas negativas; a asserção de **ordem** (pré-voo antes da varredura) é a que carrega mais peso e não tem prova negativa própria. `KIT_VERSION 1.117.0`. **Custo de teto ZERO** — C28 idêntico: `padrao 6605/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7491/7600`, folga do `narrative` em **295**. `index.html` de **814.394 → 816.174** bytes. Harness **18/18, 93/93 → 94/94, 0 erros**. Prova de vida feita: pré-voo movido temporariamente para depois de «Varra as skills primeiro», C50 falhou com a mensagem certa, revertido, verde de novo. **Fora do escopo do kit:** corrigir o `.claude/` do Sand-Land-Map (já é deles) e o `.gitignore` deles (já corrigido pelo autor); auditar o `.flatdropignore` do próprio KCM ficou registrado no IDEAS com gatilho. **Base:** auditoria do Sand-Land-Map, 2026-08-14. Nada criado fora do repositório além do relatório em `../AAMMDD-HHMM-code-kcm.txt`.

## 💬 Sessão anterior (2026-08-13 — v1.116.0)
- **Fecha a FK-M: «existência não é aptidão» ganha a metade operacional — o relatório declara qual das duas perguntas o instrumento NÃO responde (D-131, wo0097):** auditoria do Sand-Land-Map antes de gerar o pacote dele achou um item de feedback aberto que a leva da sonda não tinha fechado — a **FK-M** (2026-08-10), **anterior** à D-122. **O kit tinha o princípio e não tinha a metade operacional:** a D-122 instalou *«Existência não é aptidão (…) ao escrever uma sonda, pergunte o que ela NÃO abre»* — pergunta ao **autor**, que depende de ele lembrar. A FK-M pede que a resposta vire **propriedade declarada do relatório**, ao lado de «o que não foi olhado» — que o leitor cobra. **E é o leitor que paga:** sem a declaração, *«45/45 existem, extensões certas, índice batendo»* é um verde de **«está lá?»** que a leitura seguinte consome como verde de **«presta?»** — e os 45 ícones estavam destruídos por dentro; os dois projetos irmãos tinham o mesmo ponto cego, cada um no seu instrumento. **Entra também o argumento de custo que a FK-M carrega:** ler 30 bytes de cabeçalho respondeu «presta?» por 45 arquivos de uma vez, sem dependência nenhuma — regra sem custo declarado perde para o «depois eu vejo». **Duas superfícies:** a propriedade 2 do relatório de sonda (quem mede declara a lacuna) e o passo de verificação do modelo de WO (quem escreve a conferência declara qual pergunta o passo não responde, com a distinção invólucro × conteúdo). **Nota de simetria:** a FK-A pediu a distinção *«não verifiquei»* × *«não dá para ler daqui»*; a FK-M pede a irmã, um nível acima — *«conferi»* × *«conferi o invólucro»* — do mesmo projeto, dez meses de prática entre as duas. **Fecha o feedback dos dois projetos irmãos:** Mapsmith (nove itens + três pedidos de volta) e Sand-Land-Map (FK-A a FK-M, todos com desfecho) ficam sem pendência aberta. Check **C49** estendido, com quatro provas negativas — a quarta apertou uma asserção frouxa antes de entrar. `KIT_VERSION 1.116.0`. **Custo de teto ZERO** — C28 idêntico: `padrao 6605/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7491/7600`, folga do `narrative` em **295**. `index.html` de **813.436 → 814.394** bytes. Harness **18/18, 93/93, 0 erros** (nenhum check novo — o C49 cresceu pela quarta vez). Prova de vida feita: a segunda declaração da propriedade 2 trocada temporariamente por texto incompleto, C49 falhou com a mensagem certa, revertido, verde de novo. **Registrado como não olhado, para o guia do pacote:** o `.claude/` do Sand-Land-Map não está no mount — o `.gitignore` deles ignora a pasta inteira, contradizendo o próprio comentário do arquivo; vai como pergunta no pacote, não como diagnóstico. **Base:** auditoria do Sand-Land-Map antes de gerar o pacote dele (2026-08-13). Nada criado fora do repositório além do relatório em `../AAMMDD-HHMM-code-kcm.txt`.

## 💬 Sessão anterior (2026-08-13 — v1.115.0)
- **O pacote de update para de entregar a cadência que ele manda varrer (D-130, wo0096):** o Mapsmith fechou o update em **20/20 comparados, 7/7 decisões** e, nos últimos oito arquivos, achou a D-125 viva pela terceira vez — na formulação deles: *«as varreduras alcançaram o CEREBRO, as Instruções, o `CLAUDE.md` e os modelos de 16 nichos — e não alcançaram os templates de conteúdo do próprio pacote de update. Cada projeto novo nasce com a cadência errada e depois recebe um pacote mandando corrigi-la.»* Eles usaram `grep` nos oito templates do nicho `dev` e acharam três; a varredura completa abriu **os 20 arquivos do pacote nos 18 nichos** (via `buildUpdatePack()`) e achou **onze** — de 96 ocorrências, 85 ficam (domínio ou relato). **E o instrumento aqui errou por baixo pela terceira vez nesta negociação:** a expressão da primeira varredura (`/sess[aã]/i`) não casava «sessões» (plural com `õ`); o arquivo que eles apontaram (`STATUS__template-update.md`) só entrou depois de ampliar a regex — a D-126 valendo contra o próprio autor de novo. **A refinaria de quatro casos (D-127, D-129) ganha um quinto:** o contraste deliberado — o CEREBRO diz *«Não é cerimônia de início de SESSÃO: é de TURNO»*, frase que existe para OPOR os dois termos; trocar destruiria o argumento. **O remédio é o check, não a 11ª substituição:** onze correções literais fecham o buraco de hoje (`src/index.template.html`, `dev.js`, `research.js`, `narrative.js`, `brainstorm.js`); o C49 ganha o bloco `(1c)`, que abre `buildUpdatePack()` para os 18 nichos (exceto `rpg`, via `DOMINIO_SESSAO`) e testa `REVOGADO` + a regex nova `CADENCIA_NO_PACOTE` contra o `content` de cada arquivo do pacote — fechando a terceira varredura seguida da mesma doença (wo0094 nos gerados, wo0095 nos modelos de nicho, esta no pacote). Nenhum check novo — total continua **93/93**, o C49 cresceu pela terceira vez. `KIT_VERSION 1.115.0`. **Custo de teto ZERO** — C28 idêntico: `padrao 6605/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7491/7600`, folga do `narrative` em **295**. `index.html` de **813.428 → 813.436** bytes (+8). Harness **18/18, 93/93, 0 erros**. Prova de vida feita: texto revogado devolvido temporariamente a `dev.js` (`reexplicaria a cada sessão sem isto`), C49 falhou citando exatamente `dev/GLOSSARY__template-update.md`, revertido, verde de novo. **Desvio achado e reportado, não corrigido:** o checklist da WO esperava `grep -c "sessão" src/niches/rpg.js` = 32 (valor deixado pela wo0095); a contagem obtida foi 25 (variações de flag chegaram a até 38, nenhuma bateu 32 exato). `git diff --stat` confirma `rpg.js` **intocado** por esta aplicação — o domínio não foi comido; o número do checklist parece desatualizado, não uma regressão desta WO. **Base:** `mapsmith_10_-_v4.md`, bloco 4 (2026-08-13) — o fecho do update do Mapsmith. Nada criado fora do repositório além do relatório em `../AAMMDD-HHMM-code-kcm.txt`.

## 💬 Sessão anterior (2026-08-13 — v1.114.0)
- **A refinaria MANDA × RELATA ganha um terceiro caso — título que define a UNIDADE do documento muda junto — e a exceção de domínio sai da disciplina e entra no instrumento (D-129, wo0095):** o Mapsmith comparou o `LOG-TEMPLATE` do pacote com o deles, linha a linha, e **recusou adotá-lo**: *«adotar seria regredir»*. Estavam certos. Depois da varredura da wo0094 o modelo ainda dizia «Formato do Log de Sessão», «Objetivo da sessão» e «Última Sessão», porque títulos foram classificados como rótulo (D-127, MANDA×RELATA) e deixados — **faltava o terceiro caso:** há título que rotula e título que **estrutura**; o log passou a ser por **dia**, com `## Conversa N` por conversa, e um arquivo por dia cujo corpo pergunta «qual era o objetivo da sessão» não tem rótulo velho, tem a **unidade errada**. **Um quarto caso saiu da disciplina e entrou no instrumento:** «sessão» no nicho de RPG é a mesa de jogo — a wo0094 preservou as 33 ocorrências à mão, contando com memória de quem varre; agora o C49 traz `DOMINIO_SESSAO = new Set(["rpg"])`, exceção nomeada, não silenciosa, com prova negativa confirmando que ela carrega peso. **Um caso que a wo0094 não tinha alcançado:** `career.js` trazia `# logs/AAAA-MM-DD.md — log de sessão`, o título do próprio arquivo de log — o mais estrutural de todos. 49 substituições em 17 módulos, seis provas negativas. `KIT_VERSION 1.114.0`. **Custo de teto ZERO** — títulos vivem nos modelos de nicho, fora de `buildInstr`: C28 idêntico (`padrao 6605/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7491/7600`), folga do `narrative` em **295**. `index.html` de **813.541 → 813.428** bytes: 113 a menos, a correção encolhe. Harness **18/18, 93/93, 0 erros** (nenhum check novo — o C49 cresceu). **Nota de método:** é a segunda vez que o Mapsmith devolve uma regra melhor que a nossa e a terceira que o retorno do merge conserta o kit em vez do projeto — a varredura por fato (D-124) já se pagou três vezes. **Base:** `mapsmith_10_-_v3.md`, bloco 8 (2026-08-13). Log do dia acrescentado (`logs/2026-08-13.md`, «Conversa 3»). Nada criado fora do repositório além do relatório em `../AAMMDD-HHMM-code-kcm.txt`.

## 💬 Sessão anterior (2026-08-13 — v1.113.0)
- **O kit aplica a própria revogação ao próprio gerado; a sonda ganha o terceiro estado (instrumento); e amostra deixa de passar por cobertura (D-128, wo0094):** o Mapsmith leu o `CEREBRO__template-update.md` inteiro e devolveu o §6 pedido, com quatro achados, todos nossos. **(a)** o kit mandava os projetos varrerem a cadência «ao final de cada/uma sessão» — revogada desde a v1.106.0 (D-118) — e continuava publicando-a em **15 linhas** do próprio texto gerado: CEREBRO, Instruções, `CLAUDE.md` e os modelos de 16 nichos; é a D-125 (a skill `wrap` publicada contradizendo a própria revogação) reaparecendo uma camada acima, agora em todo o gerado. Corrigido: log entra por gatilho de evento (cortar versão, registrar decisão, virar o dia), não por cadência de sessão; check **C49** varre o gerado pela expressão do comportamento revogado, incluindo o `content` dos modelos de nicho (que o CEREBRO não carrega, e por isso escapava do primeiro bloco do check). **(b)** o funil da sonda parava em dois artefatos — sonda e análise — e o Mapsmith nomeou o que faltava: *sonda que amadurece vira **instrumento**, versionado, testado, e aí PODE dar veredito*, com gatilho de promoção explícito (rodada uma segunda vez para comparar antes/depois). **(c)** «nada truncado» não cobria amostragem — truncar esconde o fim da lista, amostrar esconde que ela nem foi lida; a propriedade 3 do relatório da sonda passa a nomear os dois defeitos, com a saída «se olhou uma parte, diga quantos de quantos». **(d)** onde a sonda mora deixa de ser regra (fica sempre fora do repositório) e vira padrão com trade-off nomeado — versionar é escolha legítima, e quem versiona registra o desvio; isto também destrava a promoção da alínea (b). **Espécie nova de passo de verificação errado, da mesma conversa:** um `grep -c` deu zero com a frase presente, quebrada em duas linhas pelo próprio texto que a WO inseria — `grep` casa por linha; o modelo de WO ganha a espécie, com a regra geral: ausência relatada por instrumento é uma afirmação e precisa de prova, igual a qualquer outra. **Aplicando a regra que eles nos deram ontem (MANDA × RELATA, D-127) na nossa própria varredura:** as 33 ocorrências de «sessão» do `rpg.js` são vocabulário de domínio (a mesa de jogo) e ficaram todas intactas; `custom.js` não tinha ocorrência. **Recusado nesta leva, parqueado no IDEAS com gatilho:** a exceção que eles propõem à D-119 (registro que depende de validação pendente pode atravessar um turno se a WO que o carrega já estiver escrita) — muda o critério de aceite de um fecho, precisa de decisão do autor. Check **C49** novo, com dez provas negativas — duas delas expuseram buracos no próprio check antes de entrar. `KIT_VERSION 1.113.0`. **Custo de teto NEGATIVO** — «log do dia» é mais curto que «log da sessão»: C28 sai de `padrao 6611/6900 · combo 7497/7600` para **`padrao 6605/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7491/7600`**, folga do `narrative` de 289 para **295**. `index.html` cresce **+4.369** bytes (809.172 → 813.541). Harness **18/18, 92/92 → 93/93, 0 erros**. Prova de vida feita: forçado o vermelho no bloco (1b) do C49 (texto revogado devolvido temporariamente a `dev.js`), C49 falhou citando `dev/LOG-TEMPLATE.md`, revertido, verde de novo. **Base:** `mapsmith_10_-_Continuação.md` (2026-08-13) — o merge dos `fusao` do Mapsmith. **Log do dia acrescentado** (`logs/2026-08-13.md`, «## Conversa 2» — mesmo arquivo do dia, pela própria regra de gatilho de evento que esta WO institui). Nada criado fora do repositório além do relatório em `../AAMMDD-HHMM-code-kcm.txt`. **Aberto do lado do Mapsmith:** o merge do `INSTRUCOES-DO-PROJETO.md` e as quatro perguntas item a item que fizeram ao autor.

## 💬 Sessão anterior (2026-08-13 — v1.112.0)
- **O primeiro merge real do pacote expõe três defeitos nossos: carimbo ambíguo, revogação sem escopo, e um diagnóstico refutado com evidência (D-127, wo0093):** o primeiro pacote de update chegou ao Mapsmith e o merge fez o que o kit projetou — leu, varreu pelo fato, não aplicou nada, reportou. Três coisas voltaram, as três nossas. **(a)** o carimbo de modos declarava `skills nao` no mesmo pacote que envia duas skills para `.claude/skills/` — `skillsOn` é o pacote de skills do NICHO, as skills do modo Code viajam com `Code sim`; o leitor não tinha como distinguir e parou, corretamente (a regra «reporte, não remova» funcionou como rede). Carimbo passa a dizer `skills-do-nicho`, com nota de que a pasta é outra coisa e skill própria do projeto (ex.: uma sonda) não é sobra. **(b)** a revogação `sessão → turno` não tinha escopo — trocar a palavra num custo medido histórico («já custou uma sessão inteira») falsificaria o registro para consertar a regra; a varredura passa a distinguir texto que MANDA (sai) de texto que RELATA fato histórico medido (fica). **(c)** a wo0092 e o guia afirmaram que, sem `Write` no `allow`, «a permissão nega os dois» — refutado em campo pelo Mapsmith com os relatórios de trabalho e os logs que existem: sem `Write` o efeito observado é pedir aprovação a cada vez, não negar em silêncio. Regra nova: afirmação sobre o repositório de outro projeto é hipótese até ele medir. **O que confirmou-se certo:** a varredura por fato achou uma contradição interna no CEREBRO do Mapsmith que nenhuma comparação de template acharia. **Erro de método próprio, na mesma entrega:** o guia carimbava `v1.111.0` no §0 e ainda mandava conferir `v1.110.1` no §1 — D-123 violada no documento que leva a versão ao outro projeto; o guia passa a não carimbar número. Check **C48** novo, com cinco provas negativas — a primeira consertou o próprio check (recorte da linha do carimbo, não o manifesto inteiro). `KIT_VERSION 1.112.0`. **Custo de teto ZERO nas cinco edições** — sexta leva seguida: C28 idêntico à v1.111.0 (`padrao 6611/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7497/7600`). `index.html` de **808.190 → 809.172** bytes. Prova de vida feita: pacote gerado pelo harness (nicho `dev`, Modo Code ligado), carimbo confirmado `skills-do-nicho nao` com a nota explicativa logo abaixo; carimbo revertido temporariamente para `skills`, C48 caiu com a mensagem certa, revertido, verde de novo. Harness **18/18, 91/91 → 92/92, 0 erros**. **Base:** relatório de merge do Mapsmith (`mapsmith_10_-_Retomada.md`, 2026-08-13) — primeiro pacote de update processado por um projeto irmão. **Log do dia escrito** (`logs/2026-08-13.md`). Nada criado fora do repositório além do relatório em `../AAMMDD-HHMM-code-kcm.txt`. **Aberto do lado do Mapsmith, para a leva seguinte:** o merge dos dois `fusao` (CEREBRO e INSTRUCOES) não foi feito, e a pergunta do §6 sobre a seção da sonda continua sem resposta por depender desse merge.

## 💬 Sessão anterior (2026-08-12 — v1.111.0)
- **A skill de fecho escreve o log; medição não é WO mas é arquivo; ler antes de sobrescrever; e a correção obrigatória para de chegar cortada (D-126, wo0092):** auditoria completa do Mapsmith (56 arquivos no mount) pedida pelo autor antes de gerar o pacote de update dele. Quatro achados, todos medidos. **(a)** a skill `wrap` publicada nunca escrevia o log do dia — o kit manda o log existir em três lugares (CEREBRO, tabela de gatilhos, prompt de transferência) e a única skill que roda no fim do trabalho não o criava; é a **IDEA-056** do Mapsmith, aberta desde 01/08, com sete sessões reconstituídas de memória como custo medido lá. **(b)** o CEREBRO abria uma exceção que contradizia a própria regra — «nunca empurre bloco para o executor colar (…) bloco colável só para o que não tem âncora nem commit, um pedido de medição, por exemplo» —, escrita na wo0085 contra evidência de campo que já estava no mount: o autor reclamou literalmente disso em `mapsmith_7.md`. Corrigido: medição não é ordem de trabalho, mas continua sendo arquivo — «não é WO» quer dizer «outro artefato», nunca «vai colado na mensagem». **(c)** restrição do dono cumprida ao pé da letra, e sobrescrita sem leitura prévia — duas ocorrências, dois projetos (o `.claude/launch.json` deste repositório na wo0090, e o caso nomeado pelo autor no Mapsmith); entra a regra em duas metades: ler antes de escrever por cima, e restrição do dono é MEDO — não especificação — que se atende copiando para fora do espaço de trabalho, não parando de fazer o pedido. **(d)** a correção obrigatória do `settings.json` chegava truncada em 120 caracteres no manifesto (o campo `role` da tabela corta ali), terminando no meio da frase, sem nunca dizer que faltava `Write` — e isso importava agora porque o `settings.json` do Mapsmith não tem `Write` nem `additionalDirectories`, a metade mecânica da causa (a). Correções obrigatórias ganham campo próprio (`obrigatorio`) e seção própria no manifesto, sem corte, fora da regra de não substituir arquivo vivo. **Nota de instrumento, do próprio autor destas WOs:** na wo0090 foi afirmado que uma skill do Mapsmith não estava no mount — estava; o comando de varredura era `grep … || echo "ausente"`, e o `||` dispara quando o grep não acha nada, não quando o arquivo falta. Corrigido no relatório desta WO. Check **C47** novo, com sete provas negativas; C37 ajustado para cobrar o campo novo inteiro no manifesto. `KIT_VERSION 1.111.0`. **Custo de teto ZERO nas nove edições** — quinta leva seguida: C28 idêntico à v1.110.1 (`padrao 6611/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7497/7600`). `index.html` de **805.153 → 808.190** bytes. Prova de vida feita: pacote gerado pelo harness (nicho `dev`, Modo Code ligado), seção «Correções obrigatórias» presente com 3 itens sob `.claude/settings.json`, frase do `Write` chegando inteira; campo `obrigatorio` renomeado temporariamente para `obrigatorio_x`, C37 caiu com a mensagem certa, revertido, verde de novo. Harness **18/18, 90/90 → 91/91, 0 erros**. **Incidente de sessão, corrigido na hora:** durante a prova de vida, `sed -i` no Git Bash converteu todo o `src/index.template.html` de CRLF para LF (dano fora do escopo da edição pretendida) — detectado pela contagem de bytes do `index.html` fora do esperado, corrigido reescrevendo o arquivo inteiro em CRLF após confirmar 0 CR soltos, sem perda de conteúdo. Registrado em `logs/2026-08-12.md`. **Base:** auditoria dos 56 arquivos do Mapsmith, pedida pelo autor em 2026-08-12. **Log do dia escrito** (`logs/2026-08-12.md`) — primeiro desde 2026-07-04, pela própria regra que esta WO institui. `.claude/launch.json` não foi tocado nesta sessão (pré-existente, fora do escopo).

## 💬 Sessão anterior (2026-08-12 — v1.110.1)
- **A skill `wrap` publicada parava de contradizer a própria revogação (D-125, wo0091):** conferência do pacote de update da v1.110.0 antes de entregá-lo aos dois projetos irmãos — o defeito foi achado **lendo o pacote**, não o fonte. A skill `wrap` que o kit publica dizia, em duas linhas consecutivas, *«e me mostre o `git diff` e o comando de commit…»* e, logo abaixo, *«Verde: `add`, `commit` e `push` sem perguntar»* — a correção da D-115 tinha sido **acrescentada** sem remover a frase antiga, e o `description` do front-matter repetia o erro. **Era bloqueante:** a wo0090 acabara de registrar a revogação dessa mesma frase para os projetos removerem; o pacote entregaria a linha revogada e, no mesmo envio, o pedido de removê-la. **Por que sobreviveu a seis versões:** o C43 comparava instalado × gerado só por cláusulas afirmativas — ninguém perguntava se o gerado tinha também o que a lista de revogações manda tirar. Correção: `description` e corpo da skill `wrap` reescritos (verbo certo, sem a promessa do comando de commit); C43 ganha trecho que percorre `REVOCATIONS`, seleciona as entradas de escopo skill e afirma a **ausência** nos dois artefatos gerados (`wrap`, `apply-wo`) — dirigido pela lista, não por cópia dela. **De carona:** `description` da `wrap` também tinha resíduo da D-118 («fechar a sessão de trabalho»). Prova de vida: frase revogada devolvida temporariamente à Edição 3, C43 caiu com a mensagem certa, revertido, verde de novo. `KIT_VERSION 1.110.1` (correção). **Custo de teto ZERO** — skills não passam por `buildInstr`, C28 idêntico à v1.110.0. `index.html` de **805.160 → 805.153** bytes (sete a menos). Harness **18/18, 90/90, 0 erros** (nenhum check novo — o C43 cresceu). **Base:** conferência do pacote de update, feita pelo autor antes de entregá-lo, 2026-08-12. Nada criado fora do repositório nesta sessão.

## 💬 Sessão anterior (2026-08-12 — v1.110.0)
- **As revogações alcançam o instalado: três decisões entram na lista, e a varredura do update passa a ser pelo fato, começando pelas skills (D-124, wo0090):** pré-voo pedido pelo autor antes de gerar os pacotes de update do Mapsmith e do Sand-Land-Map — «se tiver certeza que não temos nada a corrigir antes de eu fazer update nesses dois projetos». A varredura dos arquivos vivos dos dois projetos achou o **mesmo defeito que esta casa sofreu em 12/08**: as duas skills `wrap` ainda entregam blocos de `git` para o dono colar, e os dois `CEREBRO.md` carregam «blocos soltos», «bloco de commit pronto para copiar» e o vocabulário «sessão» — tudo revogado pelas D-115/D-118/D-119, e nenhuma das três estava na lista `REVOCATIONS` (uma entrada só, de v1.90.0). **E o pacote de update, como estava, não consertaria:** a regra «template genérico nunca substitui arquivo vivo refinado» preservava a skill viva com a linha revogada dentro — a regra que protege o refinamento estava protegendo o defeito. Entram as três, cada uma com o **substituto**, não só a remoção; e o manifesto/prompt passam a mandar **varrer pelo FATO, não pela frase** (D-116 aplicada ao merge — «Entregue o commit em TRÊS blocos separados» não tem uma palavra em comum com a frase do kit), começando pelas **skills** — a superfície mais esquecida e mais perigosa, lida ANTES de trabalhar —, com a exceção explícita de que «não substituir o vivo» nunca protege uma linha revogada. **Completa a D-121:** aquela disse que todo update do gerador tem um passo de auto-aplicação; esta acrescenta que toda decisão que APAGA comportamento tem um passo de revogação registrada. **Fecha também o pedido nº 1 do Mapsmith** («testar o pacote com `meta/analises/` inexistente»), confirmado correto por inspeção do pacote gerado (natureza `modelo-em-espera`, ausência da pasta não é pendência) — os três pedidos de volta do Mapsmith estão respondidos. Check **C46** novo, com sete provas negativas. `KIT_VERSION 1.110.0`. **Custo de teto ZERO** — quarta leva seguida: C28 imprime os mesmos números da v1.109.0 (`padrao 6611/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7497/7600`). `index.html` de **801.792 → 805.160** bytes. Prova de vida feita no navegador (manifesto do pacote de update com as 4 linhas na tabela «Linhas revogadas», célula de v1.104.0 inteira e sem quebra); entrada `1.104.0` removida temporariamente do `REVOCATIONS`, C46 caiu para vermelho, revertido, verde de novo. Harness **18/18, 89/89 → 90/90, 0 erros**. **Base:** medição feita nesta rodada, nos arquivos vivos dos dois projetos, pedido pelo autor em 2026-08-12. **Desvio registrado:** o `.claude/launch.json` do repositório (untracked, fora do commit) foi sobrescrito sem checar o conteúdo anterior, para servir o `index.html` localmente durante a verificação manual — se havia algo nele antes, foi perdido; não afeta o produto. Nenhum outro artefato criado fora do repositório nesta sessão.

## 💬 Sessão anterior (2026-08-12 — v1.109.0)
- **Correspondência entre projetos vira tipo nomeado (carta), com contador compartilhado e regra de descarte; e o instantâneo de dado derivável vira regra de higiene (D-123, wo0089):** o kit já dizia que mensagem entre projetos **não se versiona** — a regra de higiene existia e era única no fonte —, e não dizia **o que ela é**. Quando dois projetos com kits separados negociam um contrato de dados, o que trocam não é spec, não é análise, não é ordem de trabalho e não é bilhete: é **carta**, com regras próprias porque tem **dois donos** — o que o sand-land aprendeu ao longo de 17 delas com o Mapsmith (Sand-Land-Map **FK-H**, 2026-08-03). **Quatro cláusulas de forma:** nome com remetente e destinatário (`AAMMDD-<quem>-para-<quem>-NN-<assunto>.md`, porque a mesma pasta guarda os dois sentidos); **contador `NN` único e compartilhado pelos dois lados** — não um por remetente, porque com duas séries «respondendo à sua 7» vira ambíguo; uma carta, um assunto; e a marca de qual lado afirma o quê («o nosso lado já grava X» é fato, «vocês deveriam gravar Y» é pedido — sem a marca o destinatário lê pedido como fato). **Transitoriedade com o custo que a sustenta:** a carta vive fora do repositório enquanto serve, o que sobrevive é o que foi extraído dela; versionar correspondência cria uma segunda fonte de verdade que envelhece sozinha — a wo0021 do sand-land achou três lacunas numa pasta de cartas versionadas, uma delas um dado de estado desatualizado lido como fato pela leitura seguinte. E a metade que a FK-H não pedia: carta enviada e não respondida é responsabilidade de quem enviou, vira item com gatilho. **Entra junto a generalização da FK-G**, o instantâneo de dado derivável: contador escrito à mão, contagem de arquivos, versão instalada, estado de branch — tudo derivável de algo que muda sozinho, copiado para documento que não muda junto, parecendo autoridade para a leitura seguinte. Saída: escrever a **regra**, não o valor («a maior existente + 1, confira a pasta»); quando o valor precisa aparecer, vem datado e com a origem. **Estava na mesma WO de propósito** — é o antídoto do próprio contador `NN` que a WO institui; separar instalaria o problema junto com a solução. Check **C45** novo, com dez provas negativas — a primeira versão do check afirmava contra o CEREBRO inteiro e ficou verde com a cláusula do contador removida (a frase também vive na regra de higiene irmã); corrigido para recortar a seção antes de afirmar, o caso mais claro até aqui de prova negativa consertando o instrumento, não o código. `KIT_VERSION 1.109.0`. **Custo de teto ZERO nas oito edições** — C28 imprime os mesmos números da v1.108.0 (`padrao 6611/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7497/7600`). `index.html` de **798.111 → 801.792** bytes. Prova de vida feita: cláusula do contador da CARTA trocada temporariamente por texto errado, C45 falhou com a mensagem certa, revertido, verde de novo. Harness **18/18, 88/88 → 89/89, 0 erros**. **Base:** `meta/analises/260812-ANALISE-o-que-sobrou-do-feedback.md` §B2, aceita pelo autor em 2026-08-12. **§B fecha por inteiro** (B1/B4/B5 na wo0088, B2 nesta) — resta só **B3**, que é questão de organização de cada projeto, não do kit, e sai do escopo do kit. Nada criado fora do repositório nesta sessão.

## 💬 Sessão anterior (2026-08-12 — v1.108.0)
- **Sonda e exploração entram como PAR; quem abre fecha; e o gatilho de princípio vira oportunista (D-122, wo0088):** a D-113 instalou o princípio da medição delegada («quem tem o disco mede, quem tem o contexto decide») e deixou o artefato de fora — a palavra «sonda» não aparecia uma vez no fonte do kit. Estudando o material que os dois projetos construíram desde então (a skill `/sondar` do Mapsmith, `probe_pacote.py`, `probe-data.mjs`, `scan-games.mjs` e quatro relatórios reais), ficou claro que o kit pedia a coisa errada: **não é um artefato, são dois.** Na formulação do autor, que fecha o assunto: *«exploração produz hipótese, script produz evidência»*. A **sonda** é determinística, responde perguntas que alguém já sabia fazer, e é reexecutável (comparar relatórios antes/depois prova que uma mudança fez o que prometia); a **exploração** é uma passada sem hipótese prévia que devolve candidatos a checagem — e **não parte da lista de checagens da sonda**, senão só acha o que a sonda já acharia (regra do inventário da D-120 uma camada acima). **A lição que dá o custo:** o `probe_pacote.py` produziu um relatório verde em tudo (`45/45 existem`, extensão certa, índice bate) sobre 45 ícones destruídos por dentro — nenhum instrumento abriu uma imagem. Existência não é aptidão. Entram no CEREBRO: a seção «Sonda e exploração», as três propriedades do relatório (tabela e contagens nunca prosa · o que não foi olhado é declarado · nada truncado em silêncio), a proibição de veredito com a razão (*teste de conformidade não detecta que a especificação está errada*), o funil ganhando o degrau `exploração/sonda → análise → WO` no modo Code. **Duas frentes menores da mesma família — falta de gatilho, não falta de regra:** «quem abre, fecha» (B4, FK-L do sand-land — servidor de dev esquecido chegou a travar uma pasta noutro projeto; aqui, um `.zip` de teste largado na pasta pessoal do autor) vira regra de higiene, gatilho de evento e item do checklist da WO; e a política do **gatilho oportunista** (B5, Mapsmith feedback 7) — não auditar os 13 princípios, e sim dar gatilho ao princípio que falhar em campo, com o evento real do caso. C21 ajustado (afrouxa no meio, aperta na ponta — exige o degrau novo). Check **C44** novo, com dez provas negativas. `KIT_VERSION 1.108.0`. **Custo de teto ZERO nas nove edições** — C28 imprime os mesmos números da v1.107.1 (`padrao 6611/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7497/7600`). Prova de vida feita: propriedade do truncamento trocada temporariamente por texto errado, C44 falhou com a mensagem certa, revertido, verde de novo. Harness **18/18, 87/87 → 88/88, 0 erros**. **Base:** `meta/analises/260812-ANALISE-o-que-sobrou-do-feedback.md` §B (B1, B4, B5), aceita integralmente pelo autor em 2026-08-12 («aceito a B toda como vc recomenda»). Nada criado fora do repositório nesta sessão. **§C da mesma análise segue sem decisão do autor; B2 e B3 ficam para a leva seguinte (registrados no IDEAS).**

## 💬 Sessão anterior (2026-08-12 — v1.107.1)
- **O KCM é usuário do próprio kit: o instalado deixa de ficar atrás do gerado (D-121, wo0087):** o `/wrap` do KCM entregou os três blocos de `git` para o autor colar no fecho da wo0086, e o relatório — escrito antes do push — não tinha como dizer se o push acontecera (`260812-0911-code-kcm.txt` terminava em «COMMIT — Ainda NÃO executado»). **A causa não era a skill mal escrita: era a casa não ter aplicado a si mesma o que publica para os outros.** A skill `wrap` que o kit **emite** está correta desde a D-115; o arquivo `.claude/skills/wrap/SKILL.md` **do próprio KCM** continuava com o texto pré-D-115 («me mostre o comando de commit pronto… para eu copiar isolado») — a D-115(g) já dizia que «consertar o gerador não conserta o instalado» e criou pacote de update para terceiros sem incluir a própria casa. **Mesmo defeito, mais grave, no `/apply-wo`** (passou pelas wo0085/wo0086 por acidente, porque as duas traziam os blocos de `git` no corpo). **Terceiro achado:** `.claude/settings.json` do KCM sem `Write` no `allow`, coberto só por `defaultMode: acceptEdits` — um segundo mecanismo escondendo uma permissão ausente. **Correção, em três partes:** (1) as duas skills e o `settings.json` recebem as cláusulas que o kit publica — acrescentando, não substituindo, preservando a personalização legítima das skills do KCM; (2) check **C43** novo, o primeiro do harness a abrir arquivos de `.claude/` **do repositório** e a conferi-los cláusula por cláusula contra `buildCodeKitFiles()`, **nos dois lados** (se a cláusula sumir do gerado, falha ali também); (3) a lição registrada em forma geral — todo update do gerador tem um passo de auto-aplicação, porque a casa é o primeiro instalado. Sete provas negativas rodadas. `KIT_VERSION 1.107.1` (correção, não feature). **Custo de teto ZERO** — C28 imprime os mesmos números da v1.107.0 (`padrao 6611/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7497/7600`). Harness **18/18, 86/86 → 87/87, 0 erros**. **Base:** `meta/analises/260812-ANALISE-o-que-sobrou-do-feedback.md` §A, aprovada pelo autor em 2026-08-12 («vai a §A, aceito todas as suas recomendações»); item **FK-L(b)** do Sand-Land-Map, aberto desde 2026-08-09, fechado por esta WO (registrado no IDEAS). **§B e §C da mesma análise seguem sem decisão do autor.**

## 💬 Sessão anterior (2026-08-12 — v1.107.0)
- **A conferência sai do artefato: três campos por passo, inventário declarado e não truncado, e a frase pedida de volta só cobra o que o dono sabe produzir (D-120, wo0086):** três projetos chegaram à mesma pergunta — *de onde saiu a lista?* — em três alturas diferentes: a lista de **conferências** de um instrumento (Mapsmith, IDEA-073, 05/08), a lista de **lugares a mudar** num grep global (sand-land, FK-J, mesmo dia, sem combinação), e os **passos de verificação de uma WO** (Mapsmith, feedback item 9, 10/08). O sand-land nomeou o mecanismo: quando a correção e o instrumento saem do mesmo inventário, o que ficou de fora fica invisível dos dois lados. **Custo com número:** FIX-0010 (Mapsmith) — conferência verde em tudo (`45/45 existem`) com os 45 ícones sem canal alfa e visualmente destruídos, nenhum instrumento abriu uma imagem; FK-K (sand-land) — inventário de um `grep … | head -25` truncado, pego só porque a WO declarava «onze pontos» e o executor achou doze. **Achado que decidiu a forma do remédio:** `careful_guides` já mandava explicar «o quê, onde, como, e o que esperar» ao pedir algo ao usuário, e estava ligado nos 18 nichos enquanto o autor recebia «o teste manual deu X» — princípio escrito no infinitivo não tem hora, então o remédio foi o **gatilho**, não repetir a virtude num segundo lugar. **Três superfícies:** cada passo de verificação do modelo de WO ganha os três campos **Quem roda / Chega no ramo? / Prova de vida**; seção **`## Inventario`** nova, posicionada ANTES da primeira edição, exigindo que a lista saia do artefato (não da memória), não trunque e declare a contagem — com contra-passo de recontagem no checklist («número diferente: PARE e reporte»); o item «Próximo (b)» do bloco de fecho ganha o gatilho de que a frase pedida de volta só pode conter resultado que o usuário saiba produzir. Check **C42** novo, com sete provas negativas. `KIT_VERSION 1.107.0`. **Custo de teto ZERO** — C28 imprime os mesmos números da v1.106.0 (`padrao 6611/6900 · +Code 514/550 · +ASU 372/400 · compart 372/450 · combo 7497/7600`). Harness **18/18, 85/85 → 86/86, 0 erros**. **Teste manual feito em campo:** kit do Claude Code baixado pela aba de saída com Modo Code ligado — `_TEMPLATE.md` conferido (`## Inventario` antes de `## Edicao 1`, três campos presentes); `CEREBRO.md` conferido com a linha nova do «Próximo (b)», presente **nos dois estados** do Modo Code (incondicional, como a Edição 5 previa), enquanto o botão de download do kit do Claude Code some com o Modo Code desligado (prova de que o artefato lido veio do ramo Code). **Parqueado (D):** «ideia aprovada com gatilho vencido é dívida sem cobrança» segue no IDEAS com gatilho de repetição — um segundo projeto relatar o mesmo estado.

## 💬 Sessão anterior (2026-08-11 — v1.106.0)
- **O fecho de conversa em modo Code registra em vez de listar (D-119, wo0085):** o prompt E do kit pedia «liste o que ainda falta registrar» no ramo de modo Code — e listar produz bloco colável na caixa do executor, que tem limite, e que é o problema que as WOs existem para resolver; pior, o critério estava invertido (um fecho bom termina com essa lista **vazia**). Custo de campo, no Mapsmith: uma conversa de cinco dias encerrada sem um único log, um turno inteiro perdido re-derivando contexto na conversa seguinte, a conversa antiga reaberta para fazer o fecho que devia ter feito, e uma terceira abrindo contra um STATUS que apontava para uma análise **que nunca existiu**. Quatro edições, uma por causa: **(a)** regenerar ≠ criar — arquivo que não existe não tem dois escritores, e escrevê-lo é obrigatório; **(b)** o critério de aceite inverte — REGISTRE, e mostre a lista vazia; **(c)** o log do dia ganha **gatilho de evento** (cortar versão, registrar decisão ou bug grave, virar o dia) em vez de pender de um «fim de conversa» que numa conversa longa nunca chega; **(d)** fato relatado pelo dono não existe até estar em arquivo, com a origem marcada — `[relatado pelo dono]` ≠ `[medido por instrumento]`. **Dois achados lidos no fonte, ausentes do feedback:** o ramo Code do prompt E era o **único** que não nomeava o log do dia (o ramo sem executor sempre nomeou), e a regra geral de fecho era cega ao modo, mandando «nunca blocos soltos para colar à mão» onde o bloco com âncora *é* o artefato certo. **A medição definiu o desenho:** o orçamento do modo Code tinha **28 caracteres de folga** (522/550) — a variante que SOMA um ramo Code ao texto universal marca 558 e deixa o C28 vermelho; escrita como **substituição**, marca **514**. Custo de teto **negativo**: C28 vai de `+Code 522/550 · combo 7505/7600` para **`+Code 514/550 · combo 7497/7600`**; padrão 6611 e folga do `narrative` 289 inalterados. Check **C41** novo, com cinco provas negativas. `KIT_VERSION 1.106.0`. Harness **18/18, 84/84 → 85/85, 0 erros**. **Aberto:** a análise irmã (`260811-ANALISE-a-conferencia-sai-do-artefato.md` — Mapsmith item 9 + sand-land FK-J/FK-K) segue sem decisão, com dois pontos de decisão para o autor; ela não toca nenhum ponto desta WO.

## 💬 Sessão anterior (2026-08-11 — v1.105.0)
- **Vocabulário turno x conversa, prompt de update que alcança projeto desatualizado, WO no próprio git add (D-118, wo0084):** decisão de vocabulário do autor — **turno** = uma troca de mensagens · **conversa** = o fio inteiro. Não foi cosmético: `## Ritual de início de sessão` contradizia a **D-111** (abertura roda a CADA turno); `Fim de sessão` vinha colado, indistinguível, em `Fim de QUALQUER turno de trabalho`. Treze ocorrências trocadas uma a uma, sem `sed` — log do dia, `SESSAO.md` do `rpg` e a tabela de conversa nova ficam como estavam. **O furo do `buildUpdatePrompt`, achado no primeiro uso em campo** (pacotes de update para *My Little Lady* e *I will die*, 2026-08-08): a wo0082/D-116 pôs «Linhas revogadas» e o carimbo de modos no `_UPDATE-MANIFEST.md`, com a regra de uso morando no CEREBRO — mas o CEREBRO de um projeto desatualizado é justamente o arquivo velho que o update vem consertar. O prompt colado na conversa-alvo é a única superfície garantida a chegar lá, e não mencionava nem uma seção nem a outra; os dois prompts reais tiveram de ser escritos à mão. Corrigido: as duas seções entram no prompt, ANTES de `Arquivos no pacote:`. **A WO nunca versionava a si mesma** — o `git add` de cada WO listava só a anterior, e a última ficava sempre não rastreada (três vezes seguidas: wo0081, wo0082, wo0083). `buildWoTemplate` passa a incluir o próprio caminho, com a cláusula de que `add` vazio não é erro. Check **C40** novo. `KIT_VERSION 1.105.0`. **Custo de teto NEGATIVO** — «turno» é mais curto que «sessão»: C28 caiu de `padrao 6618/6900 · combo 7512/7600` para **`padrao 6611/6900 · combo 7505/7600`**, folga do `narrative` de 282 para **289**. Harness **18/18, 83/83 → 84/84, 0 erros**. **Os dois pacotes de update para *My Little Lady* e *I will die* foram entregues e aplicados pelo autor** (2026-08-08/11) — retorno dos merges pendente por escolha dele, não é bloqueio. **Próxima frente: estudo e extração do feedback do Mapsmith e do Sand Land.**

## 💬 Sessão anterior (2026-08-07 — v1.104.0)
- **Skill `ficha-de-choque` no nicho narrativo, para extração de acervo pré-existente (D-117, wo0083):** o nicho `narrative` modelava fundar e escrever uma obra, não herdá-la — zero menção a extração de acervo antes desta WO. Funde os dois métodos que os projetos narrativos criaram por conta própria (genealogia honesta: **não** é convergência independente, o autor transplantou os quatro campos de um projeto para o outro): do *I will die*, os quatro campos com o texto pronto no campo 4, status com 🔧 sob critério duplo, ciclo de leva, formato de lacuna, DEC-11 e DEC-12; do *My Little Lady*, marcas de confiança, regra da frase-fonte, nota antiga suspeita, conferir o cânone antes de abrir pendência. **Teste de entrada** na primeira linha (obra sem acervo não usa a skill); capítulo escrito deixa de ser fonte protegida contra choque; ticket de leva `AAMMDD-HHMM-leva-<assunto>.md` fora do versionamento, que se deriva do cânone, nunca de si mesmo; fronteira explícita (ordem/tamanho/sequência são do autor, e não é muralha — trocar de trilho vira análise). Gatilho novo em `triggersExtra` do nicho. Check **C39** novo, verde reportando **5 skills** no pacote. `KIT_VERSION 1.104.0`. **Custo de teto: zero** — C28 imprime os mesmos números da v1.103.0 (`padrao 6618/6900 · +Code 522/550 · +ASU 372/400 · compart 372/450 · combo 7512/7600`), porque a skill vive só no CEREBRO gerado e no `skills.zip`. Harness **18/18, 82/82 → 83/83, 0 erros**. **Alcance só `narrative` por ora** — `game` e `rpg` esperam, pedido do autor (um nicho por vez, a skill vai mudar depois da primeira leva real). **A frente de feedback dos dois narrativos (levas 1, 3 e 4) fica ENCERRADA** com esta WO. Duas pendências seguem em aberto, fora do escopo desta WO: **(i)** montar os pacotes de update para *My Little Lady* e *I will die* quando o kit estabilizar — o *My Little Lady* roda hoje com a linha de ritual revogada na v1.90.0; **(ii)** o vocabulário «sessão» × «turno», ainda sem decisão, levantado pelo autor em `260807-1701.txt`.

## 💬 Sessão anterior (2026-08-07 — v1.103.0)
- **Higiene universal e o protocolo de update que sabe subtrair (D-116, wo0082):** o merge do template-update só sabe somar — compara o que é *novo* no template, então uma correção que **apaga** uma linha nunca atravessa; a D-102 protege o arquivo vivo refinado sem poder dizer «esta linha saiu de propósito, remova a sua também». Medido no `meta/CEREBRO.md` do *My Little Lady*: a linha revogada na v1.90.0 (wo0068/D-102) sobreviveu até a v1.96.0 e virou FIX-016, diagnosticada lá como falha de adesão do assistente — **não era: era o CEREBRO deles mandando fazer aquilo.** Segunda superfície do mesmo defeito: modo desligado (ASU→Code) deixa seção órfã num CEREBRO já gerado, que o kit não tem como saber se é sobra ou modo esquecido. **Correção:** registro `REVOCATIONS` publicado na seção «Linhas revogadas» do `_UPDATE-MANIFEST.md` (texto citável + motivo); carimbo de modos do manifesto passa a declarar os **quatro** modos (faltavam ASU e compartilhado) com a regra de **reportar como choque e nunca remover sozinho**; quatro regras de higiene universais nos 18 CEREBROs (varrer pelo fato não pela frase — skills incluídas —, documento derivado nunca é fonte, citar a frase-gatilho antes de perguntar, mudança de método não se adota no meio do trabalho), todas com custo real medido nos dois projetos narrativos; gatilho do Bloco de fecho entra na tabela de eventos (existia só nas Instruções desde a wo0058). Check **C38** novo. `KIT_VERSION 1.103.0`. **Custo de teto: zero** — C28 imprime os mesmos números da v1.102.0 (`padrao 6618/6900 · +Code 522/550 · +ASU 372/400 · compart 372/450 · combo 7512/7600`). Harness **18/18, 81/81 → 82/82, 0 erros**. **Levas 3 e 4a da análise fundida (`260807-ANALISE-feedback-dois-narrativos.md` + `260807-ANALISE-extracao-de-acervo-nicho-narrativo.md`) fechadas** — **leva 4b (skill `ficha-de-choque` no nicho `narrative`) segue aberta**, com o desenho já fixado na análise. Os pacotes de update para os dois projetos narrativos (*My Little Lady*, *I will die*) ficam para quando o kit estabilizar — pedido do autor.

## 💬 Sessão anterior (2026-08-07 — v1.102.0)
- **settings.json válido, Write no allow, push antes do relatório e CONTINUIDADE que cresce (D-115, wo0081):** quatro defeitos medidos no mesmo mount da `260807-ANALISE-feedback-dois-narrativos.md` (leva 1), reportados pelo projeto *My Little Lady* em 2026-07-23. **(1)** o `.claude/settings.json` gerado terminava com um comentário `//` **depois** do `}` final — JSON inválido, e o Claude Code descarta o arquivo inteiro em silêncio, derrubando junto o `allow` e o `additionalDirectories` de que dependem o relatório em arquivo (D-108) e a medição delegada (D-113); nenhum dos 80 checks pegava, porque o harness nunca abria um `.json` pelo parser (`JSON.parse` = 0 ocorrências em `validate.js`; o único check testava por substring, que casa num arquivo quebrado — a mesma doença da D-110 numa superfície acima). **(2)** `Write` não estava no `allow`, e a skill `/wrap` do próprio kit manda criar `logs/AAAA-MM-DD.md` — a skill pedia o que a permissão negava. **(3)** `CONTINUIDADE.md` era rotulada `Snapshot` na tabela do CEREBRO gerado, contra o próprio nicho narrativo, que declara o arquivo como CRESCE (append-only). **(4)** o kit do Code não dizia nada sobre push — nem quando, nem em que ordem —, e relatórios chegaram a afirmar "push pendente" enquanto o push saía logo depois, sem registro. **Correção:** comentário sai do array JSON do settings gerado; `Write` entra no `allow`; `fileBehaviorLabel` separa `CONTINUIDADE` (Cresce) de `BRIEF` (Snapshot); o `CLAUDE.md` do kit-Code, as skills `apply-wo`/`wrap` e o modelo de WO ganham a seção «Push e relatório — nesta ordem, sempre» (decidida pelo autor em 2026-08-07): verde empurra sozinho sem perguntar; vermelho não commita nem empurra e fecha com **menu numerado** (a recomendada em 1), nunca pergunta em prosa; o relatório é sempre o último passo, escrito depois da decisão de push; o pacote de update avisa os projetos **já instalados** sobre o settings.json quebrado, porque consertar o gerador não conserta quem já baixou. Check **C37** novo — princípio: todo artefato emitido com extensão de formato conhecido abre pelo **parser daquele formato**, nunca por substring. `KIT_VERSION 1.102.0`. **Custo de teto: zero** — C28 imprime os mesmos números da v1.101.0 (`padrao 6618/6900 · +Code 522/550 · +ASU 372/400 · compart 372/450 · combo 7512/7600`). Harness **18/18, 80/80 → 81/81, 0 erros**. **Leva 1 da análise `260807-ANALISE-feedback-dois-narrativos.md` fechada** — levas 3 e 4 seguem abertas (leva 4 começa por análise própria; a D-113/medição-como-bloco-colável foi tirada desta leva pelo autor e vai para a frente de feedback do Mapsmith/sand-land).

## 💬 Sessão anterior (2026-08-03 — v1.101.0)
- **Modelo de análise no pacote, contagem repetida no STATUS e IDEAS por ID (D-114, wo0080):** duas últimas levas do «Feedback para o Kit» do Mapsmith (leva C e leva D, parqueadas pela wo0079/D-113). **(C)** a skill `/wrap` (kit gerado e deste próprio repo) passa a mandar procurar o valor ANTIGO no `STATUS.md` INTEIRO antes de atualizar uma contagem ou um estado, não só no cabeçalho — a frente irmã relatou a mesma contagem esquecida no meio do texto duas vezes seguidas. **(D-i)** o pacote de update ganha uma terceira natureza, `modelo-em-espera` (ao lado de `template` e `fusao`): guarde o arquivo, só coloque no destino se a pasta já existir ali — o `_TEMPLATE.md` de `meta/analises/` passa a viajar no pacote (via `buildUpdatePack`, fora do `if(codeOn)`) sem furar a regra de que a pasta nasce no primeiro uso. **(D-ii)** a válvula de desvio registrado nos 18 CEREBROs ganha o exemplo nomeado: projeto que organiza o IDEAS por status + ID estável (porque roadmap/decisões referenciam por ID) está desviando legitimamente. Check **C36** novo. `KIT_VERSION 1.101.0`. **Custo de teto: zero** — C28 imprime os mesmos números da v1.100.0 (`padrao 6618/6900 · +Code 522/550 · +ASU 372/400 · compart 372/450 · combo 7512/7600`). Harness **18/18, 79/79 → 80/80, 0 erros**. **Push do `126f754` (wo0079):** já confirmado sincronizado (`main`/`origin/main`, `git rev-list` → `0 0`) antes desta sessão começar — não ficou pendente. **IDEAS atualizado:** as três observações restantes do feedback do Mapsmith (leva C e leva D) marcadas como implementadas — **as quatro levas do feedback do Mapsmith estão fechadas**, resta só a mensagem de volta à frente (raia do chat); ideia nova **i-N58** registrada sobre a natureza `modelo-em-espera` ser a primeira natureza nova do pacote, com gatilho para quando um segundo arquivo pedir o mesmo tratamento.

## 💬 Sessão anterior (2026-08-03 — v1.100.0)
- **Medição delegada: quem tem o disco mede, quem tem o contexto decide (D-113, wo0079):** evidência de campo da frente irmã (2026-08-01/02): três instruções erradas em dois dias, todas por afirmar estado de arquivo em vez de mandar medir — a raia de planejamento tem teto de contexto e enxerga só o mount, a raia de execução lê o disco inteiro e não tem nenhum dos dois limites. Parentesco com a D-111: mesma doença (estado deduzido apresentado como observado), superfícies diferentes (arquivo não lido vs. arquivo que não cabe no canal). **Nova seção «Medição delegada» no CEREBRO dos 18:** a regra (quem tem acesso ao disco mede, quem tem contexto decide), o formato de retorno (número cru + comando, sem interpretação), a permissão para ler fora da raiz (`permissions.additionalDirectories`) e onde o número pousa (relatório, decisões, armadilhas da WO — número não registrado volta a ser deduzido). Gatilho novo nas Instruções, condicionado ao Modo Code. Modelo de WO ganha a seção opcional `## Medicao previa` (sem âncora, sem commit, sem arquivo). Kit do Code aprende a responder um pedido de medição. **Três desvios deliberados da proposta da frente irmã** (registrados na D-113): pedido de medição é bloco colável, não seção-de-arquivo-de-WO; gatilho condicional ao Modo Code; quinta parte nova (onde o número pousa). Check **C35** novo. `KIT_VERSION 1.100.0`. **Custo de teto: negativo** — duas linhas enxugadas (Commit, Entregáveis) pagam o gatilho novo com sobra: `+Code` 529→**522**/550, `compart` 435→**372**/450, combo 7519→**7512**/7600. Harness **18/18, 78/78 → 79/79, 0 erros**. **IDEAS atualizado:** item (5) do feedback Mapsmith marcado como implementado, com os três desvios anotados; levas C (`/wrap`) e D (`_TEMPLATE.md`/IDEAS por ID) seguem abertas; ideia nova registrada sobre o padrão «cada linha nova paga a própria entrada enxugando outra» (funcionou 2x seguidas — wo0077 e wo0079).

## 💬 Sessão anterior (2026-08-03 — v1.99.0)
- **Degrau de saída do funil de análise, saída do CRLF e mensagem entre frentes (D-112, wo0078):** evidência de campo do Mapsmith em 2026-08-02 — o dono declarou uma pasta como fato canônico, o assistente encontrou uma armadilha técnica legítima na leitura, e devolveu uma análise com duas opções, uma delas «não fazer, só documentar», negando o que o dono acabara de decidir. O achado era bom; faltava endereço. **Três correções:** (a) o teste (1) do funil («Antes de escrever, dois testes baratos») deixa de perguntar O QUÊ está decidido e passa a perguntar **QUEM decide** — preserva a frase asserida pelo C30 e ganha a definição de uma linha «Análise é para quando a pergunta ainda é dele»; (b) o achado técnico de uma análise abandonada ganha endereço — vai para as armadilhas da ordem de trabalho como risco a tratar, não volta como pergunta ao dono («achado sem endereço vira pergunta»); (c) o modelo de WO passa a dar a saída do CRLF que só avisava: âncora de uma linha não tem quebra dentro, para várias linhas ancorar em uma só. Registrada também a decisão do usuário de 2026-08-03: mensagem trocada com frente irmã é nota transitória, não ganha pasta versionada. Check **C34** novo. `KIT_VERSION 1.99.0`. **Custo de teto: zero** — C28 imprime os mesmos números da v1.98.0 (`padrao 6618/6900 · combo 7519/7600`). Harness **18/18, 77/77 → 78/78, 0 erros**. **Convergência independente:** a frente irmã chegou ao teste «quem decide» sem ter a wo0074 (v1.94.0). **IDEAS atualizado:** item (4) do feedback Mapsmith implementado; item (5) parqueado como leva B (medição delegada); leva C (`/wrap`) e leva D (`_TEMPLATE.md`/IDEAS por ID) abertas; ideia nova sobre check que consolida regra restringir o próprio refino depois (C15, C30).

## 💬 Sessão anterior (2026-08-03 — v1.98.0)
- **Leitura antes do trabalho: abertura de turno, carimbo Base, falsa confirmação e canal rápido (D-111, wo0077):** falha real de 2026-08-02 apurada no chat — o assistente afirmou «a wo0076 você ainda não aplicou» **quatro horas depois** de ela ter sido aplicada (commit `d423747`, relatório `260802-2239-code-kcm.txt`, 22:39), sem ter listado o mount uma única vez naquele turno. As Instruções já mandavam reler o mount em dois lugares — mais exortação não resolvia. **Quatro correções mecânicas:** (a) a releitura vira passo de **abertura de TURNO**, antes de qualquer outra ferramenta (nova regra em `HYGIENE_RULES`, com degradação explícita para projeto sem cópia achatada); (b) o item «Estado» do bloco de fecho ganha o **carimbo `Base:`** (arquivo lido NESTE turno + data que ELE declara + commit/versão que ELE traz) — carimbo inventado é mentira detectável, campo vago não é; (c) a regra «a cópia não é a fonte da verdade» ganha o aviso da **falsa confirmação do sandbox**: reconstruir o projeto a partir da cópia que se tem e ver a âncora casar prova que a cópia é velha, não que o trabalho está pendente; (d) fica declarado que o **relatório em arquivo lidera a cópia achatada** quando os dois discordam. O texto das Instruções (Edição 1) caiu de 314 para 235 caracteres preservando a frase do C25; o carimbo (Edição 2) somou 59 — líquido **−20**. Check **C33** novo. `KIT_VERSION 1.98.0`. **Custo de teto: negativo** — C28 caiu de `padrao 6638/6900 · combo 7539/7600` para `padrao 6618/6900 · combo 7519/7600`. Harness **18/18, 76/76 → 77/77, 0 erros**. **Push do `d423747` (wo0076):** já confirmado sincronizado (`main`/`origin/main`) antes desta sessão começar — não ficou pendente. **Correções no IDEAS:** `i-N54` movida de ABERTA para Descartadas (a auditoria de 2026-08-02 já a tinha refutado); ideia nova registrada (medir quantos dos cinco campos do bloco de fecho carregam informação real, gatilho em ~10 turnos de uso do carimbo `Base:`); errata registrada para a mensagem enviada ao FlatDrop em 2026-08-02 (afirmava a wo0076 pendente e a auditoria da i-N54 como já descartada — o inverso do estado real na hora).

## 💬 Sessão anterior (2026-08-02 — v1.97.0)
- **Anatomia do bloco gerado, metade estrutural do P11 e Arquivos Críticos no CONTEXT (D-110, wo0076):** o kit tinha o princípio (HYGIENE_RULES desde a v1.89.0) e não tinha a anatomia — princípio não é verificável, e uma frente irmã provou isso citando os marcadores do bloco dentro de um comentário explicativo, o que o gerador (que procura por substring) lê como um segundo bloco. **O mesmo defeito estava vivo no `.flatdropignore` deste próprio repositório** — corrigido junto com o gerado (`structuredFlatdropignore`). **Anatomia entra nas HYGIENE_RULES:** cinco regras (comentário FORA do bloco; regra DENTRO; UM bloco só; bloco é o ÚLTIMO conteúdo; **os marcadores não se citam em comentário**) + duas obrigações do gerador (recusar diante de ambiguidade, não adivinhar; normalizar só o próprio bloco). **P11 ganha a metade estrutural do rename** (caminho de pasta, nome de comando, identificador — não só concordância de gênero/número na prosa), com o exemplo real `meta/CLAUDE.md` → `meta/CEREBRO.md` (o exemplo «spec → WO» sugerido pela frente irmã é impossível aqui: o C15 bane as duas strings do `index.html` inteiro, por ter nascido daquele rename). **`📁 Arquivos Críticos` muda de STATUS para CONTEXT no nicho `dev`** — conhecimento estável, não estado do agora; os prompts Debug/Plano/Auditoria já procuravam a seção em CONTEXT, onde ela não existia. Check **C32** novo. `KIT_VERSION 1.97.0`. **Custo de teto: zero** — nenhuma edição toca as Instruções (C28 imprime os mesmos números de v1.96.0). Harness **18/18, 75/75 → 76/76, 0 erros**. **Achados registrados no DECISIONS/IDEAS:** o defeito vivo no `.flatdropignore` da raiz; o C15 tornando impossível o exemplo original do P11; duas devoluções pendentes para a frente do FlatDrop (manifesto «limpo» sem `ahead/behind`; exemplo do P11 trocado). **Push da wo0075 (`b719864`) já estava feito** antes desta sessão começar — `main`/`origin/main` sincronizados, confirmado por `git rev-list`.

## 💬 Sessão anterior (2026-08-02 — v1.96.0)
- **Paridade dos templates de nicho, formato legado e estado do repo pelo manifesto (D-109, wo0075):** frente irmã devolveu quatro observações confirmadas na leitura do template. Três eram o **mesmo defeito estrutural**: `dev` e `brainstorm` redefinem os templates universais de `IDEAS.md`, e esses overrides não herdavam o refinamento das seções «Feedback para o Kit»/«Feedback para o ASU» que o CEREBRO gerado manda usar em três lugares — todo projeto de nicho dev/brainstorm nascia com uma regra apontando para um endereço que o próprio kit não criava. **Corrigido:** as duas seções entram no fim do `IDEAS.md` próprio dos dois nichos. **Os outros dois pontos, menores e independentes:** o kit se contradizia sobre `.claude/commands/` (protocolo de update chamava de «descontinuado»; instalação e README do zip diziam «também funcionaria») — corrigido nos dois pontos para «formato descontinuado», sem alternativa a escolher; e a tabela de documentos do CEREBRO não dizia o que fazer com duas sessões no mesmo dia — corrigido: **o mesmo arquivo `logs/AAAA-MM-DD.md`**, com `## Sessão N` para cada uma. **Acréscimo à parte:** quando o manifesto da cópia achatada já trouxer o estado do repo (commit/branch/limpo-sujo, contrato novo do FlatDrop), o item «Estado» do bloco de fecho usa esse dado em vez de pedir — registrando que é foto da hora da geração, não do turno. Check **C31** novo, `ok (2 nichos com IDEAS proprio)`. `KIT_VERSION 1.96.0`. **Custo de teto: zero** — nenhuma edição toca as Instruções (C28 imprime os mesmos números de v1.95.0). Harness **18/18, 74/74 → 75/75, 0 erros**. **Fora de escopo, adiado para a leva 2:** `📁 Arquivos Críticos` do `STATUS.md` do `dev` e o exemplo do Princípio 11 (decisão pendente do usuário); a Parte 1 da mensagem do FlatDrop (anatomia do bloco gerado).

## 💬 Sessão anterior (2026-08-01 — v1.95.0)
- **Contrapeso do gatilho de análise + relatório do Code em arquivo (D-108, wo0074):** achado de campo do ASU (2026-07-30, item 5 do «Feedback para o Kit»): um pedido pequeno disparou o gatilho de análise (wo0063), a leitura da fonte derrubou a premissa (o formato já não mudava), e o assistente **continuou escrevendo** em vez de parar — devolveu como «ponto de decisão» uma escolha técnica do próprio autor, custando um turno. **Causa:** assimetria de concretude — o lado que alarga o gatilho vinha com cinco exemplos nomeados e reforço; o lado que estreita era um adjetivo solto e vinha depois. **Remédio = posição, não texto:** os dois testes baratos (o QUÊ já está decidido? cabe em meia página?) sobem para ANTES do gatilho que alarga; o gatilho ganha o fecho «pergunta a refazer DEPOIS de ler a fonte» + limite do formato já extensível; nasce a **cláusula de abandono** (ler a fonte tem valor, escrever o documento não; premissa caiu → pare, diga o que viu, vá trabalhar). **Em paralelo:** o kit do Code passa a **gravar o relatório de trabalho em arquivo** (`../AAMMDD-HHMM-code-<slug>.txt`, pasta-pai do repo) a cada `/apply-wo`/`/wrap`, sem pedir — `CLAUDE.md` + `settings.json` (`additionalDirectories`) + as duas skills do kit; interruptor local, avisa e segue se a escrita for negada. **Dogfood:** o próprio KCM adota a regra (slug `kcm`) — testado nesta sessão, gravação funcionou de primeira, sem negação de permissão. **`.flatdropignore`:** análises Decidida/Implementada/Abandonada/Substituída saem do mount (só a ABERTA sobe). Check **C30** novo. `KIT_VERSION 1.95.0`. **Custo de teto:** +10 chars na linha universal (Edição 1) — `narrative` 6628→**6638** (folga 262), `game` 6536→**6546** (folga 354), `dev` 6084→**6094** (folga 806); C28 `padrao 6638/6900 · +Code 529/550 · +ASU 372/400 · compart 435/450 · combo 7539/7600`. Harness **18/18, 73/73 → 74/74, 0 erros**.

## 💬 Sessão anterior (2026-07-29 — v1.94.0)
- **O KCM migra para `.claude/skills/` (D-107, wo0073, repo):** dogfood pendente desde a wo0068 (o kit marca `.claude/commands/` como legado no protocolo de update desde então, e a casa continuava no formato antigo). Os três comandos (`apply-wo`, `check-wo`, `wrap`) viram `.claude/skills/<nome>/SKILL.md`, com `name`/`description` no front-matter e `disable-model-invocation: true` — a invocação **não muda** (`/apply-wo`, `/check-wo`, `/wrap` continuam sendo a forma de chamar; testado nesta sessão que o modelo não dispara a skill por conta própria). `.claude/commands/` removido do repo. **Não** entrega o `check-wo` aos projetos-filhos (segue aberto no IDEAS). **Sem bump:** nada em `src/` foi tocado, produto inalterado, `KIT_VERSION` segue **1.94.0**. Harness **18/18, 73/73, 0 erros** (conferência).
- **Fecho da leva sand-land (D-106, wo0072):** sobravam três itens — os dois pequenos das sugestões de template (§«Feedback para o Kit» do IDEAS, wo0070/D-104) e a lição da nota `260729-1252.txt`, adiada por sequenciamento (a corrente wo0069→0071 tinha de fechar na ordem), não esquecimento. **Item 1 — gaveta «Adiadas» com gatilho de volta:** faltava o estado mais comum — decidi não fazer agora — que hoje ou poluía «Ativas» ou mentia como «Descartada»; entra no template universal (`narrative`) e no IDEAS do `dev`; o `brainstorm` já tinha «Em banho-maria» e recebe só a exigência do gatilho. **Regra cobrada, nome da seção não.** **Recusado:** a taxonomia estendida do IDEAS (IDs, referências de origem, gavetas extras) — aumenta o vocabulário obrigatório de todo projeto. **Item 3:** `HISTORY.md` do `dev`/`research` ganha dois tipos de seção — pesquisa de convenções e autópsia. **A nota `260729-1252.txt`:** o pacote de template-update fica no mount até o merge fechar; se já saiu, o assistente declara a cobertura de leitura (verbatim vs. não). **Correção de fato:** a wo0071 errou ao dizer que `validate.js` é CRLF — são LF (`validate.js`, `build.js`, `src/niches/*.js`); só `index.template.html` é CRLF (CONTEXT §7, item 17). Check **C29** novo, `ok (18 IDEAS · 2 HISTORY)`. `KIT_VERSION 1.94.0`. **Custo de teto: zero** — C28 com os mesmos números (`padrao 6628/6900 · +Code 529/550 · +ASU 372/400 · compart 435/450 · combo 7529/7600`). Harness **18/18, 72/72 → 73/73, 0 erros**. **A leva sand-land fecha aqui** — não sobra item aberto dela.

## 💬 Sessão anterior (2026-07-29 — v1.93.0)
- **Teto por configuração: trava o incremento, não o total (D-105, wo0071):** a wo0069 curou as linhas de modo e deixou recomendação registrada — orçamento **Code ≤ 550 · ASU ≤ 400**. Esta WO instala o check (opção C da análise `260727-ANALISE-teto-por-configuracao.md`). **O princípio:** trava-se o **incremento**, não o total — o total varia com a riqueza do nicho (do projeto, não do kit); o `G16` não muda, segue medindo só o pior caso de conteúdo. **Achado da medição:** o modelo de dois orçamentos (Code/ASU) reprovou 17/18 nichos — as linhas de commit e entregáveis de repo são liberadas por `asuModeOn() || codeModeOn() || CHANGELOG`, então ficavam **sem dono** quando só o ASU estava ligado. Consequência: **terceiro balde**, `compartilhado`, orçamento **450**. Números medidos, todos dentro do orçamento: compartilhado **435**/450 · Code **529**/550 · ASU **372**/400 · combo **7529**/7600 · padrão **6628**/6900 — nenhum número aprovado pelo autor mudou. `INSTR_TETO_MODOS`/`MODO_ORCAMENTO` viram fonte única, lida pelo harness e pelo CEREBRO gerado (que agora publica os três orçamentos e o total). `C26` passa a derivar o limite do orçamento em vez do 950 fixo. Check **C28** novo, imprimindo os cinco números a cada rodada. `KIT_VERSION 1.93.0`. **Custo de teto: zero** — `narrative` 6628, `game` 6536, `dev` 6084, todos inalterados. Harness **18/18, 71/71 → 72/72, 0 erros**.

## 💬 Sessão anterior (2026-07-29 — v1.92.0)
- **Leva sand-land: molde de WO, Técnicas específicas, Estado ilegível pelo canal, data que não envelhece (D-104, wo0070):** o sand-land foi o segundo projeto a rodar um ciclo completo sob as regras pós-wo0066, e trouxe FK-A/B/C (`260729-1138.txt`) + a sugestão do `IDEAS-sand-land.md` (DEC-018 deles, confirmada 2×). **FK-C (o mais forte):** o kit não tinha molde para o artefato que a raia chat→Code mais produz — a WO —, então cada uma derivava por imitação; `buildWoTemplate()` novo entrega `meta/workorders/_TEMPLATE.md` no zip estruturado, no kit do Code e no pacote de atualização, com reinclusão no `.flatdropignore` (o `.gitkeep` da pasta some). **FK-A:** o campo Estado agora distingue «não verificado» (desleixo) de «não legível pelo canal» (fato — mount sem `.git`, sem `git log` para ler). **FK-B:** a regra «a cópia não é a fonte da verdade» (D-100) ganha a contrapartida — o que envelhece é o estado do repo e as âncoras, não o carimbo de emissão de um artefato. **Estrutural:** seção «Técnicas específicas deste projeto» vira oficial no CEREBRO gerado — começa vazia, protegida do template-update, é o único conteúdo que sobrevive a um rebase do kit. **Achado à parte:** pré-requisito do ASU corrigido — só `INSTRUCTION_GUIDE.md`; o `PROMPT_IA.md` semeia diretriz divergente em projetos sem o kit. **Recusado:** tipo de artefato «proposta de fase» (análise+ROADMAP já cobrem); taxonomia estendida de IDEAS (só a gaveta «Adiadas com gatilho de volta» vai na leva seguinte). Check **C27** novo. `KIT_VERSION 1.92.0`. **Custo de teto: zero** — `narrative` 6628, `game` 6536, `dev` 6084 (folga 816), todos inalterados. Harness **18/18, 70/70 → 71/71, 0 erros**.

## 💬 Sessão anterior (2026-07-29 — v1.91.0)
- **Curadoria das linhas de modo + o ASU pela interface (D-103, wo0069):** a análise do teto mediu 13/18 nichos acima de 6.900 no combo Modo Code+ASU (pior caso `narrative` 8.097). Opção **(D)** aprovada pelo autor — curar antes de travar, porque travar sem curar carimbaria o inchaço medido como orçamento aceito. **A linha do ASU nas Instruções, o feedback do ASU, e `.gitignore`+README** perdem a duplicação com o que o CEREBRO gerado já detalhava por completo (`Saída de código via ASU (patch)`, `Artefatos de repo`) — versão curta na Instrução, definição completa no CEREBRO, check nas duas pontas (mesma arquitetura da wo0056-A). A linha do commit perde «Não pule o commit» (redundante com «ENTREGUE»). **Informação nova do autor:** ele aplica pela **interface** do ASU e guarda o `.yaml` fora da raiz — o CEREBRO passa a dizer que o trabalho do assistente termina no `.yaml` **válido**; como o usuário aplica e onde guarda não é assunto da conversa. **Números:** nichos acima do teto no combo caem de **13 para 2**; incremento dos modos cai de **1469 para 901** (Code 779→529, ASU 690→372). Configuração padrão: `narrative`/`game` inalterados; `dev` cai para **6084** (folga 816). Check **C26** novo. `KIT_VERSION 1.91.0`. Harness **18/18, 69/69 → 70/70, 0 erros**. **Recomendação registrada para a próxima WO (opção C, teto por configuração):** Code ≤ 550 · ASU ≤ 400 · total ≤ 7600 nos combos; `G16` não deve incluir os modos.

## 💬 Sessão anterior (2026-07-29 — v1.90.0)
- **Protocolo de update + gatilho da releitura (D-102, wo0068):** a nota `260728-2029.txt` do FlatDrop apontou a quarta causa do erro de releitura do mount, sem antídoto até aqui — **trabalho pedido expulsa ritual não-pedido**: mensagem cheia de pedidos empurra a releitura para fora, e é justamente aí que ela mais importa. O gatilho de releitura nas Instruções troca a lista de exemplos de sinal («já subi», «veja o txt»...) — que ensinava a esperar o sinal — por «inclusive, e principalmente, quando eu não sinalizo upload» + «mensagem cheia de pedidos é onde essa releitura mais falha»; os 66 chars economizados pagam a maior parte dos 82 do gatilho novo (saldo universal **+16**). **Os quatro modos de falha da releitura** (este + os três já resolvidos pela wo0067) viram item novo nas `HYGIENE_RULES` do CEREBRO gerado. **Protocolo de update, três itens do FlatDrop:** o `_UPDATE-PROMPT` passa a pedir versão/commit do repo antes de comparar qualquer arquivo; **template genérico deixa de ser candidato a substituir arquivo vivo refinado** (`CLAUDE.md`, `.claude/*`, skills e os `meta/` especializados caem por padrão em (c); única exceção é formato descontinuado — `.claude/commands/` → `.claude/skills/<nome>/SKILL.md`); e o manifesto do `SPEC.md` (nicho `dev`) passa a dizer que é spec-de-feature (SDD), sob demanda, e **não** o modelo das WOs. **Campo Estado e bloco de fecho MANTIDOS** por decisão registrada — campo autodeclaratório («não verificado nesta rodada») é mais barato que confabular. Novo check **C25**. `KIT_VERSION 1.90.0`. Teto: `narrative` 6612 → **6628** (folga 272), `game` 6520 → **6536** (folga 364), `dev` 6117 → **6334** (folga 566). Harness **18/18, 68/68 → 69/69, 0 erros**. **Aberto:** folga da narrativa em 272 chars — a próxima linha universal vai precisar da curadoria das linhas de modo (`260727-ANALISE-teto-por-configuracao.md`) antes de caber; migrar o próprio KCM de `.claude/commands/` para `.claude/skills/` (dogfood pendente, o produto já marca `commands/` como legado).

## 💬 Sessão anterior (2026-07-29 — v1.89.0)
- **Convivência gerado × manual: bloco marcado, verificação no ponto de uso, HUB aposentado (D-101, wo0067):** o FlatDrop (0.14.0) devolveu doze itens no primeiro ciclo completo pós-v1.87; esta WO aplica os cinco de maior valor. **`.flatdropignore` gerado** ganha os marcadores `# >>> flatdrop-editor` / `# <<<` (regra dentro, comentário fora, nada depois do fim) — corrige `logs/`→`logs/*` e o par `meta/analises/*`+`!meta/analises/_TEMPLATE.md` incompleto (bug apontado pelo FlatDrop); explica o motivo real do `pasta/*` (poda de diretório + a regra normativa do git puro, as duas coisas). **Campo Estado** do bloco de fecho passa a exigir leitura feita neste turno e admite «não verificado nesta rodada» como resposta válida. **Gatilho concreto de análise:** mudar o formato de um artefato que outro vai ler ou editar pede análise, mesmo com diff pequeno. **Princípio novo nas `HYGIENE_RULES`:** artefato gerado que convive com edição humana precisa enxergar o que existe fora, ter precedência por posição e nunca desfazer o que não é seu. **HUB de infraestrutura (`HUB.md` da raiz, toolchain KCM·ASU·FlatDrop) aposentado** — parado desde 2026-07-03; contratos preservados numa subseção nova ao final da §8 do `meta/CONTEXT.md`; `buildHub()` do produto inalterado. **Achado:** o arquivo vivia na raiz (`HUB.md`), não em `meta/HUB.md` como o texto da WO nomeava. Novo check **C24**. `KIT_VERSION 1.89.0`. Teto inalterado: `narrative` 6612, `game` 6520, `dev` 6117. Harness **18/18, 67/67 → 68/68, 0 erros**.

## 💬 Sessão anterior (2026-07-27 — v1.88.0)
- **A cópia não é a fonte da verdade; análises voltam ao mount; a instrução viva é a do painel (D-100, wo0066):** o turno anterior reentregou a wo0065 já aplicada (linha `/apply-wo` + handoff de arquivos já no repo) porque não releu o mount — o relatório do Code (`260727-2216.txt`) estava lá desde antes. Causa estrutural: um **sandbox reconstruído do mount** para testar a WO passou a *parecer* o estado do projeto (congela quando nasce; o repo anda) — e o `src/index.template.html` do repo era byte-idêntico ao do sandbox, prova de que cópia correta não protege contra estado errado. **Regra nova nas `HYGIENE_RULES`** (18 CEREBROs gerados, custo de teto zero — check **C23** prova que não vazou às Instruções). **Dogfood:** armadilhas **15/16** no `meta/CONTEXT.md` §7 (tratar a própria cópia/sandbox como o estado do projeto; comparar contra a cópia versionada das Instruções em vez do painel), duas regras novas no `meta/CEREBRO.md`, `meta/analises/*` volta a subir ao `.flatdropignore` (análise «Em discussão» que o chat não vê no turno seguinte é reescrita do zero, não discutida). `KIT_VERSION 1.88.0`. Teto inalterado: `narrative` 6612, `game` 6520, `dev` 6117. Harness **18/18, 66/66 → 67/67, 0 erros**.

## 💬 Sessão anterior (2026-07-27 — v1.87.0)
- **Raia do bloco de fecho + disciplina de entrega + fim dos resíduos spec (D-099, wo0065):** o Code passou a emitir o bloco de fecho de turno por conta própria (nota `260727-1833.txt`) porque a seção do CEREBRO dizia «todo turno» sem dizer de quem é a raia — corrigido: **planejamento fecha com o bloco; execução (Claude Code) fecha com relatório de trabalho** (o que fez, achados/desvios do texto da WO, arquivos tocados, build/validação, commit). `meta/CEREBRO.md`, `CLAUDE.md`/`INSTRUCOES-DO-PROJETO.md` (raiz) e `.claude/commands/wrap.md` do próprio KCM adotam a separação; `CLAUDE.md` gerado e skill `apply-wo` do kit-Code passam a mandar relatar. **Duas disciplinas de entrega reforçadas nas Instruções** (não só no CEREBRO): WO nunca vai sozinha (linha `/apply-wo <arquivo>` junto) e bloco `git` parcial (só `add`) não serve. **9 resíduos do rename `spec`→WO (wo0053)** corrigidos na prosa do template e do kit do Code (o C15 vigiava caminho/comando, não a prosa). **Dogfood:** guia renomeado `_GUIA-doc-por-spec.md` → `_GUIA-doc-por-wo.md`, referências corrigidas em `.flatdropignore`/CEREBRO. Novo check **C22**. `KIT_VERSION 1.87.0`. Teto: padrão inalterado (`narrative` 6612, `game` 6520); `dev` 6038 → **6117** (folga 783). Harness **18/18, 65/65 → 66/66, 0 erros**.
- **Ideia aberta registrada (IDEAS, prioridade A ANALISAR):** o teto (`N[]`/`G16`) não cobre os **modos** — com Code+ASU ligados, `narrative` vai a 8081 e `game` a 7989, mais de 1.000 chars acima do teto de 6.900, sem check vermelho. É análise, não WO.

## 💬 Sessão anterior (2026-07-27 — v1.86.0)
- **Bloco de fecho v2 + higiene dos docs-âncora (D-098, wo0064):** o item **Próximo** ganha uma segunda parte — **(a) Ação** (a próxima coisa concreta) + **(b) Peça no próximo turno** (a frase já redigida que o usuário devolve para retomar sem reconstruir contexto) — e **Arquivar / Manter** vira **lista** (linha **Arquivar:** / linha **Manter:**), como Config recomendada e Handoff. Check **C19** ganha 4 asserções novas; nenhum check novo (segue **65/65**), **custo de teto zero** (`narrative` segue 6612/folga 288, `game` segue 6520/folga 380). **Achado corrigido:** o `meta/CEREBRO.md` do próprio KCM tinha ficado na versão **pré-wo0060** (ordem Estado→Próximo→Notas, rótulo antigo «Notas») — dogfood realinhado ao produto. **Higiene:** `CLAUDE.md` (raiz, lido pelo Claude Code em toda sessão) corrigido de **17/17 para 18/18 nichos**; `BUILD.md` corrigido de 17→18 (4 pontos) e `/check-spec` (inexistente desde a wo0053) trocado por `/check-wo`; `meta/CONTEXT.md` corrigido de 17→18 e do vocabulário `spec`→WO (9 pontos), preservando as linhas históricas de versões passadas. `INSTRUCOES-DO-PROJETO.md`: ordem do bloco de fecho corrigida. `KIT_VERSION 1.86.0`. Harness **18/18, 65/65, 0 erros**.

## 💬 Sessão anterior (2026-07-27 — v1.85.0)
- **«Análise antes do compromisso» vira convenção do produto + higiene do FlatDrop (D-097, wo0063):** o produto passa a ensinar a convenção `meta/analises/` aos projetos **gerados** (antes só dogfood no KCM) — os 18 nichos recebem a seção nova **«Análise antes do compromisso»** no CEREBRO gerado (onde, o que tem dentro, medir antes de propor, a análise não decide sozinha, funil via `analiseFunil()`, cláusula de adoção para projeto que já usa outro nome como `design/`/`estudos/`/`rfc/`) + gatilho de **145 chars** nas Instruções + linha na tabela de documentos. **Pasta preguiçosa:** `analises/` e `specs/` só nascem no primeiro uso — zip não cria pasta vazia. `.flatdropignore` gerado **corrigido**: `meta/workorders/*` (a receita antiga excluía a pasta inteira e o `!` não reincluía nada) + bloco novo para `analises/` com a regra «modelo e guia sempre sobem». **Dogfood:** `.flatdropignore` da raiz libera `meta/analises/_TEMPLATE.md`; `meta/CEREBRO.md` registra as duas regras; comandos `check-wo`/`apply-wo`/`wrap` atualizados (vocabulário WO, caminho `src/`, 18/18, status das análises). Fecha a pendência «Ensinar o produto sobre `analises/`» (D-096/wo0062). Novo check **C21**. `KIT_VERSION 1.85.0`. Teto: `narrative` 6467→**6612** (folga 288), `game` 6375→**6520** (folga 380), `dev` 5893→**6038** (folga 862), `career` 5817→**5962** (folga 938) — nenhum nicho estoura. Harness **18/18, 64/64 → 65/65, 0 erros**.

## 💬 Sessão anterior (2026-07-27 — v1.84.0)
- **Nome do handoff nos prompts de transferência e retomada (D-095, wo0061):** o nome `AAMMDD-HANDOFF-BRIEF.md` — que as wo0058/wo0060 tinham posto só no CEREBRO (bloco de fecho) — passa a constar também nos **prompts E (transferir agora) e F (retomar após transferência)**: é o prompt que **manda gerar** o arquivo, então é ali que a convenção evita cada projeto inventar o próprio nome. Prompt E: «Gere um **`AAMMDD-HANDOFF-BRIEF.md`** (arquivo NOVO...)»; prompt F: «Leia o `AAMMDD-HANDOFF-BRIEF.md` da conversa anterior». Fecha a nota `260720-2129` e as ideias **i-N44/i-N45**. `UPDATE_PROTOCOL` revisado nesta passada e mantido como está. Novo check **C20**. `KIT_VERSION 1.84.0`. **Custo zero de teto** (prompts ficam fora do `N[]` das Instruções); `narrative` segue 6467, `game` 6375. Harness **18/18, 63/63 → 64/64, 0 erros**.

## 💬 Sessão anterior (2026-07-27 — v1.83.0)
- **WO-C: correção do bloco de fecho + poda + migração para os `meta/` (D-094, wo0060) — enxugamento A→B→C concluído:** escopo **reduzido** por decisão do usuário — papéis dos arquivos ficam como estão (folgas de 862–1647 tornavam a compressão desnecessária). Entregue: **(a)** correção do bloco de fecho da wo0058, que saiu com ordem/rótulos errados — agora **Próximo** (item 1, antes do divisor) → **Estado** → **Arquivar / Manter** → **Config recomendada** (lista por raia) → **Handoff**, e o bloco declarado **personalizável** pelo projeto («não uma jaula»); **(b)** poda do cabeçalho auto-referencial das Instruções (−110 chars nos 18); **(c)** regra nova de **migração para os `meta/`** — personalização do formulário de montagem preenche os arquivos de contexto e sai das Instruções, que guardam identidade + ritual + gatilhos + disciplina. Check **C19** atualizado (4 asserções novas). `KIT_VERSION 1.83.0`. Teto: `narrative` 6612→**6467** (folga 433), `game` 6520→**6375** (folga 525), `dev` 6038→5893 (folga 1007), `career` 6049→5817 (folga 1083) — nenhum nicho estoura. Harness **18/18, 63/63, 0 erros**.

## 💬 Sessão anterior (2026-07-26 — v1.82.0)
- **Enxugamento etapa B, 2ª e última leva: curadoria dos 13 nichos restantes (D-093, wo0059) — WO-B concluída:** 68 frases curadas no campo `short` de dev/design/client/marketing/research/product/business/pixel/brainstorm/music/cuisine/animation/comics; `custom` não tem comportamentos próprios. Com a wo0057, totaliza **96 frases curadas nos 18 nichos**. Definições integrais preservadas no CEREBRO. Economia de **130–531 chars/nicho**. **Regra de exceção aplicada:** 19 comportamentos ficaram sem `short` por já serem mínimos (≤80 chars). Os dois mais apertados seguem `narrative` (6612, folga 288) e `game` (6520, folga 380) — inalterados nesta WO, porque receberam o gatilho da wo0058 depois da curadoria da wo0057. **Registrada em IDEAS a correção pendente** do bloco de fecho de turno da wo0058 (ordem e rótulos), a corrigir depois da WO-C. `KIT_VERSION 1.82.0`. Nenhum check novo; **C18 passa a reportar `ok (96 curados)`**. Harness **18/18, 63/63, 0 erros**.

## 💬 Sessão anterior (2026-07-26 — v1.81.0)
- **Bloco de fecho de turno padronizado e condicional (D-092, wo0058):** protocolo de encerramento de turno vira **formato fixo** — Estado · Próximo passo · Notas (arquivar/manter) · Config recomendada por raia · Handoff —, emitindo só as linhas com conteúdo real (linha vazia não aparece; nunca escrever «nada a arquivar» nem inventar handoff). Origem: pedido do usuário (260726-0842) — *«esse tipo de protocolo padronizado e identificável é excelente disciplina»* — depois do padrão emergir na prática. Resolve duas dores: o usuário tinha de perguntar o que arquivar; e a config recomendada vinha sem dizer para qual raia. **Absorve i-N44/i-N45 e o nome `AAMMDD-HANDOFF-BRIEF.md`** (nota 260720). Gatilho de 159 chars nas Instruções («Feche o turno com o bloco padrão…»); estrutura completa no CEREBRO (grátis). **Dogfood:** `INSTRUCOES-DO-PROJETO.md` e `meta/CEREBRO.md` do próprio KCM adotam o bloco, com as raias **Chat** (planejamento) e **Code** (execução). Novo check **C19**. `KIT_VERSION 1.81.0`. Teto medido: `narrative` 6453→**6612** (folga 288), `game` 6361→**6520** (folga 380), `career` **5962**, `rpg` **5813** — nenhum nicho estoura. Harness **18/18, 62/62 → 63/63, 0 erros**.

## 💬 Sessão anterior (2026-07-26 — v1.80.0)
- **Enxugamento etapa B, 1ª leva: curadoria de `game`/`narrative`/`career`/`rpg` (D-091, wo0057):** 28 frases curadas no campo `short` (game 9, narrative 6, career 6, rpg 7); definições integrais intactas no CEREBRO. **Correção de expectativa:** a análise projetava ~2000–2775 chars/nicho; o real foi **330–518**, porque a projeção comparava com a definição integral e não com o `shortDef` (1ª frase) que as Instruções já usavam desde antes da wo0056. Medido: `game` 6879→**6361** (folga 21→**539**, 25× mais espaço — a parede que bloqueava novas frentes caiu), `narrative` 6783→**6453** (folga 447), `career` 6260→**5803** (folga 1097), `rpg` 6113→**5654** (folga 1246). **Regra de exceção em ação:** `narrative/no_overdoc` (39 chars) e `rpg/pc_center` (77 chars) seguem sem `short` — já mínimos, não preencher é a exceção declarada. A curadoria também corrigiu linhas que eram afirmação, não regra (ex.: `scope_killer`). **Integridade verificada:** o CEREBRO da narrativa segue com o protocolo-sanduíche integral e os dois modos de colaboração — nada de estrutura se perdeu. `KIT_VERSION 1.80.0`. Nenhum check novo; **C18 passa a reportar `ok (28 curados)`**. Harness **18/18, 62/62, 0 erros**. Consequência para a WO-C: o ganho restante virá dos papéis dos arquivos e da poda de markdown, não dos comportamentos. 2ª leva (14 nichos restantes) e WO-C parqueadas em IDEAS.

## 💬 Sessão anterior (2026-07-26 — v1.79.0)
- **Enxugamento etapa A: motor do campo `short` curado (D-090, wo0056):** instalado o mecanismo do enxugamento das Instruções (`meta/analises/260726-ANALISE-ENXUGAMENTO-INSTRUCOES.md`, etapa A do plano A→B→C aprovado) **sem mudar uma palavra de conteúdo**. Comportamento de nicho ganha campo opcional `short` (4º elemento) com **precedência nas Instruções** sobre o corte automático (`shortDef`, 1ª frase, teto 180); `buildClaudeMd` (CEREBRO) inalterado — segue com a definição completa. **Achado que corrigiu a análise:** as Instruções já cortavam pela `shortDef()` — o ganho da etapa B não vem de *cortar*, vem de **curar** a frase que sobra. **Regra de exceção embutida:** comportamento sem `short` mantém o corte automático — não preencher é dizer «este fica como está». Novo check **C18** (motor + contrato: nada some das Instruções, definição completa no CEREBRO, `short` comprime de fato) — hoje reporta **0 curados** (correto; ninguém foi curado ainda, isso é a etapa B). `KIT_VERSION 1.79.0`. Bump **minor** (mecânica nova + check novo). **Prova de neutralidade:** `buildInstr` dos 18 nichos byte-idêntica; `buildClaudeMd` difere só no carimbo de versão. **Teto: custo zero — `game` inalterado, 6879/6900 (folga 21); nenhum nicho muda de tamanho**. Harness **61/61 → 62/62, 18/18, 0 erros**.

## 💬 Sessão anterior (2026-07-26 — v1.78.0)
- **Auto-refino das Instruções vira dever proativo (D-088/D-089, wo0055):** o bloco «Refino das Instruções do Projeto» (G19) deixa de ser conhecimento passivo e ganha **dever proativo com gatilho** — o projeto deve propor o refino por conta própria (fim da primeira sessão real; depois, a cada sinal de atrito), sem esperar o usuário pedir. Acompanha a **liberdade de promover regra crítica a texto integral** (o projeto administra o próprio orçamento) e o **feedback ao kit** (IDEAS.md) como desfecho legítimo do refino. Origem: decisão do usuário (260726) — *"não deveria ser eu pedindo"*. **Pré-condição do enxugamento das Instruções:** sem este dever, entregar Instruções mais curtas seria só perda; com ele, é troca de peso genérico por peso relevante. **Dogfood:** nova subseção «Refino deste CEREBRO.md» no próprio `meta/CEREBRO.md`. **D-089:** método `meta/analises/` confirmado como RFC/design doc, com campo **Status** e elos cruzados (análise → WO/spec → D-0XX); modelo novo em `meta/analises/_TEMPLATE.md`; regra de proporção (só para mudança não-trivial). Novo check **C17**. `KIT_VERSION 1.78.0`. Bump **minor** (dever proativo novo no G19 + check novo). **Teto: custo zero — `game` inalterado, 6879/6900 (folga 21); nenhum nicho muda (mudança 100% CEREBRO)**. Harness **60/60 → 61/61, 18/18, 0 erros**.

## 💬 Sessão anterior (2026-07-26 — v1.77.0)
- **SDD leve nos nichos dev e game (D-087, wo0054, análise i-N7 `meta/analises/260718-ANALISE-i-N7-SDD-NICHOS-CODIGO.md`, itens P1/P2/L3):** importada a **espinha** do Spec-Driven Development (spec como artefato durável + critério de aceite verificável), **não** a cerimônia do Spec Kit (sem `constitution.md` separada — CONTEXT/JOGO + DECISIONS + princípios já são a constituição; sem CLI; sem 6 arquivos por feature). `dev` ganha o modelo `SPEC.md` (opcional): problema, critérios de aceite verificáveis, decisões de design, fora de escopo, passos — copiar para `specs/AAMMDD-nome.md`. Prompt **J** (dev) e prompt **H** (game) passam a exigir critérios de aceite verificáveis/observáveis antes de codar/implementar. `game` **não** ganha arquivo novo — teto medido não comporta (6976 > 6900, folga de só 21 chars); reforço vai só no prompt (custo zero no teto), porque o nicho já tem `MECANICAS.md` como spec de mecânica — evidência concreta de que o enxugamento das Instruções precisa vir antes de qualquer nova frente no `game`. Princípio `analyze` ganha cláusula de ambiguidade: pedido ambíguo/de escala de feature expõe lacunas e o que foi assumido ANTES de construir (tarefa pequena continua sendo fazer, não levantar bandeira; cresce só o CEREBRO). Novo check **C16**. `KIT_VERSION 1.77.0`. Bump **minor** (comportamento novo em 2 nichos + princípio universal + check novo). Teto: `dev` 6011/6900 (era 5835, folga 889); `game` 6879/6900 inalterado (folga 21). Harness **59/59 → 60/60, 18/18, 0 erros**.

## 💬 Sessão anterior (2026-07-26 — v1.76.0)
- **Rename «spec» → Work Order (WO) (D-086, spec0053):** o artefato de aplicação Chat→Code deixa de se chamar «spec» (que não é spec — é instrução de aplicação) e passa a ser **Work Order (WO)**: `AAMMDD-woNNNN-desc.md` em `meta/workorders/`; comandos `/check-wo` e `/apply-wo`; skill `apply-wo`; identificador JS interno `applyWo`. «Spec» fica reservado para a **spec-de-feature do SDD** (spec0054). **Gerador** (`src/index.template.html`): caminhos, comando, identificador, padrão de nome e a prosa do fluxo trocados — **sem** find-replace cego (palavras pt-BR com «spec» = 20, `aspect-ratio` = 3 e comentários `(specNNNN)` = 14 preservados). Novo check **C15** trava o rename **e** as três armadilhas; **G7** acompanhou o rename (`apply-wo`/`applyWo`). **Dogfood:** o próprio KCM adota o WO — pasta `meta/specs/` → `meta/workorders/` (git mv, histórico preservado), comandos `.claude/commands/{check,apply}-wo.md`, `CEREBRO.md` e `INSTRUCOES-DO-PROJETO.md` com glossário «WO vs Spec». História preservada (WOs antigas mantêm o nome; próxima é `wo0054`). `KIT_VERSION 1.76.0`. Bump **minor** (mudança de vocabulário que os projetos-filhos vão adotar + check novo). Teto: nenhum nicho estoura; **`game` segue em 6879/6900 (folga 21) — a vigiar**. Harness **58/58 → 59/59, 18/18, 0 erros**.

## 💬 Sessão anterior (2026-07-25 — v1.75.0)
- **Adesão ao ritual: mount-check por turno + memória×mount + fix vazamento ASU (D-085, spec0052, origem: relatório 260722):** três correções + dogfood, prioridade URGENTE (projetos ao vivo ignorando o ritual — respondendo de memória a mounts já atualizados). **C-a (CEREBRO, sem teto):** o princípio universal `check_before_ask` (P8) ganha a lógica **mount-por-turno incondicional** — revisar o mount a CADA turno (novos `.txt`, `_MANIFEST`, arquivos mudados) antes de responder, **sem** depender de sinal do usuário (um «continuar», uma correção ou uma reclamação também pode vir com o mount atualizado) — mais a **comparação memória × mount** (nem tratar o mount como verdade absoluta nem confiar só na memória; se divergem, estuda; se o mount bate com a memória mas o usuário afirma ter aplicado algo ausente, faz o que dá e AVISA, em vez de inferir cegamente ou regenerar). **C-b (+81 chars):** gatilho curto no ritual das Instruções (superfície lida todo turno). **C-c:** fim do **vazamento da nomeação ASU no modo Code** — a linha «Nomes padronizados» só cita `AAMMDD-asuNNNN.yaml` com o modo ASU ligado. **Dogfood:** mesmas correções nos arquivos próprios do KCM (`INSTRUCOES-DO-PROJETO.md` item 4 + `CEREBRO.md` item 8). Novo check **C14**. `KIT_VERSION 1.75.0`. Bump **minor** (comportamento novo no CEREBRO/Instruções + check novo). **NÃO inclui** o «padrão de nome de spec nas Instruções» (o nome «spec» muda na spec0053). Teto: nenhum nicho estoura; **`game` em 6879/6900 (folga 21) — o mais apertado, a vigiar**. **B7 fechado** (verificado). Harness **57/57 → 58/58, 18/18, 0 erros**.

## 💬 Sessão anterior (2026-07-18 — v1.74.0)
- **E-ASU (ASU não cobre binários) + B6 (Retcon no CONTINUIDADE) (D-084, spec0051, análise 260716 §E + §B6 INCORPORADAS; §D DESCARTADO):** duas mudanças pequenas e independentes num só ciclo (decisão do usuário 260717-1313.txt). **E-ASU:** a seção «Saída de código via ASU» do CEREBRO passa a ensinar que o ASU opera **só em texto** (patch por âncora) e **não cobre `.docx`/`.xlsx`/binários** — Word/planilha sai inteiro para baixar, nunca como instrução ASU (o KCM é quem ensina a usar o ASU, então o aviso vem de dentro do produto); vale para qualquer nicho que use ASU. **B6:** o template do `CONTINUIDADE.md` da narrativa ganha a seção **opcional «Retcon»** (antes de «Inconsistências encontradas e resolvidas»): quando uma reescrita muda um fato canônico, a memória factual guarda só a **verdade atual**; o histórico da mudança vai para ENREDO.md (trama) ou DECISIONS.md (craft). Enxuta — sem a tabela de «fontes brutas». **§D (modo de migração/extração) DESCARTADO** — não precisa de modo (era organização + script Python mal-implementado + leitura incompleta de caminhos, não uma lacuna do kit). Novo check **C13**. `KIT_VERSION 1.74.0`. Bump **minor** (comportamento novo no CEREBRO/template + check novo). Teto: narrativa **inalterada** (6702 — ambas as mudanças são CEREBRO/template, fora das Instruções). Harness **56/56 → 57/57, 18/18, 0 erros**. Da análise 260716 segue aberto só o item **B7**.

## 💬 Sessão anterior (2026-07-17 — v1.73.0)
- **Nomes com opções fundamentadas na ficção (D-083, spec0050, análise 260716 §C C2 INCORPORADA na ficção):** C2 — parqueado na leva C — resolvido como **comportamento de ficção**, não universal (decisão do usuário 260717-1313.txt: dev/produto já resolvem com nome temporário; naming universal conflitaria com P2). **Narrativa:** o naming sai do bloco Direção Criativa e passa a valer em **ambos os modos**, enriquecido (uma recomendação por opção; não precisa ser menu formal; nome temporário que valha manter é sinalizado, com alternativas mesmo assim). **game/rpg/comics:** novo behavior `naming` (mesmo caso: elenco e mundo com muitos elementos nomeáveis). A extensão a **music/design/animation foi descartada** (naming lá é raro/próximo do dev); **§C fechada por completo**. Novo check **C12**. `KIT_VERSION 1.73.0`. Bump **minor** (comportamento novo em 4 nichos + check novo). Teto: narrativa **inalterada** (6702); game **6798** (folga 102, o mais apertado — vigiar), rpg 6032, comics 5692 (todos < 6900). Harness **55/55 → 56/56, 18/18, 0 erros**. **B6/B7/D/E** da análise seguem abertos.

## 💬 Sessão anterior (2026-07-17 — v1.72.0)
- **Universais da base: leva C (D-082, spec0049, análise 260716 §C C1/C4/C5 INCORPORADA):** terceira e última leva do refino narrativo. Três universais entram na base, **todos só no CEREBRO** (teto dos 18 inalterado; nenhum princípio novo, G2 segue 13). **C1 — `cadence` ganha «pedido composto»:** quando vários pedidos vêm numa mensagem, enumerar as partes, executar o que não bloqueia e parar só na decisão que de fato trava (não deixar um pedido soterrar os outros nem virar tudo pergunta). **C4 — `consistency` ganha a armadilha de gênero em rename:** ao renomear um termo por busca-e-troca, conferir a concordância (gênero/número) no entorno (FIX-003 do projeto Rascunho). **C5 — bloco «Refino das Instruções» (G19) ganha a sincronia Instr↔CEREBRO:** ao mexer no CEREBRO, checar se as Instruções curtas ainda batem e propor alinhar/regenerar se divergirem. **C3 já era coberto por P1 (analyze)** — não entra. **C2 (2–4 opções para nome) NÃO promovido a universal** (não se aplica a nichos não-criativos; conflita com P2) — fica para eventual passada nos criativos. Novo check **C11**. `KIT_VERSION 1.72.0`. Bump **minor** (universais da base + check novo). Harness **54/54 → 55/55, 18/18, 0 erros**. **B6/B7/D/E** da análise seguem abertos.

## 💬 Sessão anterior (2026-07-17 — v1.71.0)
- **Refino do nicho Narrativa a partir de feedback de 4 projetos reais (D-081, spec0048, análise 260716 §B B1–B5 INCORPORADAS):** segunda e última spec do refino narrativo (a spec0047 fez a base). Tudo em `src/niches/narrative.js` (skills/CEREBRO) — **teto inalterado**, `N[narrative]` segue em 6702/6900. Cada item vem de um FIX real em produção. **B1 — `checagem-continuidade`, 5 erros nomeados novos:** narração que hedgeia o próprio POV (My Little Lady FIX-006/007/008), vazamento de familiaridade cedo demais, transição ausente disfarçada de economia, eco não verificado (My mother DEC-007), craft afirmado sem verificação (P13). **B2 — `escrita-serial`, passo PRÉ «a cena já existe?»** (My mother FIX-001): não reescrever do zero cena já aprovada. **B3 — `escrita-serial`, bloco «processando notas de revisão do autor»** (I will die, princípios 21-23): interpretar antes de transcrever. **B4 — gatilho «concluída OU revisada»** (My Little Lady FIX-005): revisão de capítulo existente dispara o mesmo gatilho triplo (linha do `triggersExtra`, que vive no CEREBRO, + heading na `checagem-continuidade`). **B5 — `textura-mundo`, «eco físico vs. eco comportamental»** (My Little Lady): reconhecimento tardio de parentesco/identidade. Novo check **C10** (trava o conteúdo novo; `triggersExtra` conferido no CEREBRO). `KIT_VERSION 1.71.0`. Bump **minor** (refino de nicho + check novo). Harness **53/53 → 54/54, 18/18, 0 erros**. **B6/B7/C/D/E** da análise seguem abertos para as próximas levas.

## 💬 Sessão anterior (2026-07-16 — v1.70.1)
- **Doc-âncora por nicho (`anchorDoc`) + fim do choque `CONTEXT` + G25 (D-080, spec0047, i-N53 nascida e FECHADA):** primeira das duas specs do refino narrativo, esta **de base** (afeta os 18 nichos). O resolvedor do «doc de contexto» adivinhava o arquivo-âncora por **regex de nome** — e falhava em silêncio em **4 nichos** cujo âncora a regex não casa: narrative/`BIBLIA.md`, marketing/`MARCA.md`, pixel/`ESTILO.md`, rpg/`MUNDO.md`. Neles o ritual gerado (Instruções E CEREBRO) caía para `CEREBRO → STATUS` e **omitia** o arquivo em torno do qual o nicho é construído. Agora cada nicho **declara** `anchorDoc`; `buildInstr` e `buildClaudeMd` preferem a declaração e só caem na regex como retaguarda. `custom` declara `anchorDoc:null` («sem âncora» explícito). Corrigido também o **choque `CONTEXT`**: duas linhas do modo ASU e o `CLAUDE.md` do kit-Code cravavam o literal `CONTEXT`/`CONTEXT.md` (arquivo que a narrativa e vários nichos não têm) — viraram genéricas. Novo check **G25** (ritual cita o âncora de todo nicho; Instruções nunca citam um `.md` inexistente — testado com ASU on/off). `KIT_VERSION 1.70.1`. Bump **patch** (fix de geração + invariante nova). Harness **52/52 → 53/53, 18/18, 0 erros**. Base: `meta/analises/260716-ANALISE-REFINO-NARRATIVE.md` §A. Falta a spec0048 (do nicho narrativa).

## 💬 Sessão anterior (2026-07-15 — v1.70.0)
- **Prompts C/D mode+entrega-aware + `_MANIFEST` na retomada + `KIT_VERSION` exposto (D-078/D-079, spec0046, i-N42/i-N10 FECHADAS):** três frentes numa spec. **i-N42 (D-078):** os prompts universais de setup deixam de ser cegos a modo e a entrega. **Títulos** explícitos (a confusão que a ideia mirava): **C** «Começar um projeto do zero com o KCM», **D** «Adotar o KCM num projeto já em andamento» — o eixo (novo × já existe) fica no rótulo. Corpos viram montagem por `L.push` com ramos `codeModeOn()`/`asuModeOn()`; o **D** detecta `_MANIFEST.md` (lê pelo mapa, não regenera) e o **F** cita o `_MANIFEST.md` na retomada. Sem lógica nova — reusa a consciência que já vivia no CEREBRO. Texto-guia da view Prompts reescrito. Novo check **G23**. **i-N10 (D-079):** o produto ganha `KIT_VERSION = "1.70.0"` (fonte única, logo após `INSTR_TETO`) — rodapé mostra `Dev · v1.70.0` (não mais `v1` cravado), helper `kitStamp()` carimba os READMEs e os demais downloads citam `v${KIT_VERSION}`; `_UPDATE-MANIFEST` ganha `- Kit: v1.70.0`. Espelha o `INSTR_TETO` da spec0045. Novo check **G24**. Bump **minor** (prompts + versão exposta). A partir daqui, **todo bump de versão mexe em `KIT_VERSION`** (i-N50). Harness **50/50 → 52/52, 18/18, 0 erros**.

## 💬 Sessão anterior (2026-07-15 — v1.69.0)
- **Contador de instrução na UI + `INSTR_TETO` como fonte única + CHANGELOG reconstruído (D-076/D-077, spec0045, i-N46/i-N47 FECHADAS):** duas frentes independentes, baratas. **i-N46 (D-076):** o produto ganha `INSTR_TETO` (6900) — os **três literais `6900`** cravados no `validate.js` (assert do check `N`, teto do career, rótulo do check `N`) migraram para a constante, então mudar o teto num só lugar move UI + os checks juntos (`grep 6900 validate.js` agora **vazio**). A saída ganha um contador ao lado de «Copiar» (`#instr-count`): «5754 / 6900 (83%)», verde até 90%, âmbar de 90–100%, vermelho cheio acima; na aba CEREBRO vira «CEREBRO · sem teto». `updateInstrCount` roda dentro de `updatePreview`, então reage a cada chip marcado. Novo check **G22**. **i-N47 (D-077):** CHANGELOG reconstruído — 12 entradas v1.54–v1.66 preenchem o salto v1.53→v1.67; registrado que **não há v1.64** (salto real de numeração v1.63→v1.65). Bump **minor** (nova capacidade de UI + constante de produto). Harness **49/49 → 50/50, 18/18, 0 erros**.

## 💬 Sessão anterior (2026-07-14 — v1.68.1)
- **Paleta unificada dos 18 nichos + blindagem do hook (D-074/D-075, spec0044, i-N49 FECHADA):** o KCM tinha **duas fontes de cor por nicho** sem invariante — `cardColor` (card, em `src/niches/<id>.js`) e `html[data-niche="<id>"]{ --amber }` (página, em `src/index.template.html`) — e o `career` **não tinha bloco `[data-niche]` nenhum**, herdando o âmbar do dev (causa raiz do bug relatado na spec0042). **D-074:** uma cor principal por nicho, card == página; template ganha o 18º bloco `[data-niche="career"]` (novo, depois de `business`); 11 `cardColor` realinhados e matizes redistribuídos (pixel lima `#a3e635`, música ciano `#22d3ee`, cozinha laranja `#f97316`, animação índigo `#818cf8`, HQ fúcsia `#e879f9`, game esmeralda `#34d399`, rpg vermelho `#ef4444`, negócios bege `#d4b896`, narrativa azul `#7aa2f7`, marketing coral `#f87171`, brainstorm amarelo `#fbbf24`); dois checks travam a invariante — **G20** (card == página em todos, nicho sem bloco `[data-niche]` quebra o harness) e **G21** (nenhuma cor principal repetida). **D-075:** `.gitattributes` fixa LF no `.githooks/pre-commit` (evita *bad interpreter* em clone novo com `core.autocrlf=true`). Bump **patch**. Harness **47/47 → 49/49, 18/18, 0 erros**.

## 💬 Sessão anterior (2026-07-14 — v1.68.0)
- **Refino de instrução pela própria conversa + dois portões de processo (D-072/D-073, spec0043):** três frentes. **CEREBRO (D-072, i-N48 nascida e fechada):** nova seção «Refino das Instruções do Projeto» — as Instruções são lidas em toda mensagem, então a versão do kit é ponto de partida genérico que o projeto deve encurtar e especializar **sem perder processo**; seis regras (cortar o que não se aplica, especializar, «mover é barato, apagar é caro» = regra que já evitou erro não sai, some ao CEREBRO; não inchar; uma regra por linha; **teto de ~6.900 chars**, dizer tamanho antes/depois) + registro em DECISIONS/«Feedback para o Kit». Novo check **G19**. **Toolchain (D-073):** **i-N38** `.githooks/pre-commit` bloqueia commit que toque o produto (`src/`, `index.html`, `build.js`, `validate.js`, `build-manifest.json`) sem build+harness verdes e exige o `index.html` do build atual (bypass `--no-verify`, ligado por `git config core.hooksPath .githooks`, documentado no BUILD.md); **i-N39** `/check-spec` — comando read-only que confere âncoras (0=morta/2+=ambígua), pré-requisitos e estado verde antes de aplicar uma spec. IDEAS: i-N38/i-N39/i-N48 FECHADAS; **i-N49** (paleta unificada dos nichos = spec0044) registrada. Bump **minor** (CEREBRO novo + toolchain). Harness **46/46 → 47/47, 18/18, 0 erros**.

## 💬 Sessão anterior (2026-07-14 — v1.67.1)
- **Correções do career + SO no modal + engine/fase na aba Nicho + teto real no harness (D-070/D-071, spec0042):** patch de correções e migrações, sem nicho novo. **career:** cor do card `#84cc16`→`#4ade80` (o lima colidia com o âmbar do dev na grade); **Área-alvo** vira `chips` (múltipla escolha) e ampliada de 7 para 16 opções (transição de área é justamente marcar duas); **Fronteiras** ampliada para 13 opções (inclui `scopecreep`, que o caso de origem pedia); hero do card genérico (deixou de contar a vida do autor). **i-N36 fase C fechada:** o seletor de SO (`#g-os`) sai do painel esquerdo e vai para a seção «Ambiente» dentro do modal ⚙ (JS inalterado; DOM antes do `<script>`, D-059). **game:** `engineSel`/`phase` ganham `panel:"modal"` → aba Nicho do modal (muda só onde renderizam; `phase` segue na linha Estágio). **Harness:** achado grave — o check de chips (FIX-004) estava **vácuo** (lia `norm.groups`, sempre `undefined` no formato `.items`); corrigido + assert de não-vazio. **D-070:** dois orçamentos — base ≤ 6900 (inalterado) e **pior caso ≤ 7600** (novo G16, com todos os chips/multi marcados), porque o teto nunca fora medido sob escolha do usuário (narrative 7174, career 7009, game 6978 já furavam 6900 no produto publicado). +3 checks (G16/G17/G18). Bump patch. Harness **43/43 → 46/46, 18/18, 0 erros**. IDEAS: i-N46 (contador de chars na UI), i-N47 (reconstruir CHANGELOG v1.54–v1.66).

## 💬 Sessão anterior (2026-07-13 — v1.67.0)
- **Nasce o 18º nicho `career` (Carreira & Trabalho) (D-069, spec0041):** primeiro nicho de conteúdo novo desde a fase de refino de modos. Tese em cadeia: **fato datado + prova** (`EVIDENCIAS.md`, append-only) → **competência com lastro** (`DOSSIE.md`, derivado) → **artefato** (currículo/pitch que nunca afirma o que não prova); em paralelo `SITUACAO.md` guarda o **contratado × real** (linha do tempo do escopo) e `DECISIONS.md` registra o porquê **com o melhor contra-argumento na mesa**. 6 behaviors (`evidence_first`, `scope_ledger`, `benchmark_sourced`, `counterargue_before_irreversible`, `mine_projects`, `vent_is_not_fact`), campo **Fronteiras** no builder, regra de privacidade (projeto do usuário local/privado; template público), topbar `momentSel`+`frentes` (multi), 10 templates, 6 prompts extras (G–L). Template: marcador, hero `.hero-career` (dossiê/evidência), `NICHE_CODE` (CARR), responsivo; `build-manifest` com 18 módulos. Novo check **G15** (behaviors-chave + arquivos do dossiê + campos de topbar chegando ao `buildInstr` sob o teto 6900; instr do career 6179/6900); G1 subiu para 18 nichos; linha final do harness deixou de ser hardcoded. 7 análises antigas renomeadas para `AAMMDD-ANALISE-*.md` em `meta/analises/`; IDEAS: i-N42 reescrita (C/D são prompts do projeto receptor, não dos downloads do KCM) + i-N43 (auto-refino registrado), i-N44 (handoff enxuto + log do Code), i-N45 (prompt de retomada fixo). Bump minor. Harness **+1 nicho, +1 check (G15), 17/17 · 41/41 → 18/18 · 43/43, 0 erros**. Base: `meta/analises/260713-ANALISE-NICHO-CARREIRA.md`.

## 💬 Sessão anterior (2026-07-13 — v1.66.0)
- **FIX: prompts de transferência eram mode-blind e mandavam regenerar os meta no chat (D-068, spec0040):** os prompts A–F eram o último subsistema mode-blind; **E** mandava "gere **todos** os arquivos de contexto… ATUAL e COMPLETO" — regenerar os meta grandes no pior momento (anti-padrão que comeu 33 bullets do `IDEAS.md` num projeto consumidor; dois escritores no modo Code). Agora **E/F são mode-aware**: E gera o **HANDOFF-BRIEF** (arquivo novo) e trata o contexto por modo — Code: **não regenerar**, listar o append e exigir commit/push; ASU: edições por `.yaml`; vanilla: **só os arquivos que mudaram** + higiene P12; grupo: empilha o HUB. F vira **ritual** com precedência (**os arquivos vencem o brief**). `PROMPTS_BASE` exposto no SHIM; **G14** trava a regressão. Prompts C/D ficam para a próxima (i-N42). Bump minor. Harness **+1 check (G14), 40/40 → 41/41**. 17/17, 41/41, 0 erros.

## 💬 Sessão anterior (2026-07-10 — v1.65.0)
- **FIX: ignores faltavam no pacote de atualização (D-067, spec0039):** `buildUpdatePack` vinha sem `.gitignore`/`.flatdropignore` — somados sempre, antes do manifesto, reusando `structuredGitignore`/`structuredFlatdropignore` (natureza `template`, nomes planos `gitignore__template-update` / `flatdropignore__template-update`); downloads granulares seguem só com conteúdo. G13 trava a regressão. Pacote de atualização auditado e completo. Bump minor. Harness **+1 check (G13), 39/39 → 40/40**. 17/17, 40/40, 0 erros.
- **Modo Atualização Fase C: gatilho `UPDATE_PROTOCOL` no CEREBRO (D-066, spec0038):** `buildClaudeMd` ganha seção incondicional «Ao receber um template-update do KCM» antes de «Regras de higiene» — versão condensada e permanente do prompt da Fase B, em todo projeto gerado: reconhece sufixo `__template-update` + `_UPDATE-MANIFEST.md`, rotina comparar→reportar→nunca-sobrescrever, ciente de `template`/`fusao`. G12 trava a presença. **i-N40 fechada (Fases A+B+C completas).** Bump minor. Harness **+1 check (G12), 38/38 → 39/39**. 17/17, 39/39, 0 erros.

## 💬 Sessão anterior (2026-07-07 — v1.63.0)
- **FIX downloads incompletos + fonte única `generatedContextFiles` (D-065, spec0037):** CEREBRO/INSTRUCOES são gerados e não vivem em `contextFiles`, então `downloadZIP`/`downloadAllTemplates`/`downloadStructuredZIP` saíam sem eles (estruturado sem `meta/CEREBRO.md` — bug do usuário). Helper `generatedContextFiles` vira fonte única (CEREBRO→`meta/`, INSTRUCOES→raiz) consumida pelos três; G11 trava a regressão. Bump minor. Harness **+1 check (G11), 37/37 → 38/38**. 17/17, 38/38, 0 erros.

## 💬 Sessão anterior (2026-07-03 — v1.49.0)
- **Nicho narrativa, fase B: switch skills-pack de escrita (D-051, i-N35, spec0022):** toggle niche-scoped `skillsMode` ("Gerar skills de escrita?") no topbar, presente só em nichos que declaram `skillsPack` (hoje só narrative). Ligado, o CEREBRO.md ganha o «Apêndice — skills de escrita» com 4 Agent Skills (`escrita-serial`, `checagem-continuidade`, `voz-calibragem`, `textura-mundo`), formato oficial (frontmatter `name`/`description` + seção «Aplicação neste projeto» em branco). Mesmo padrão do Modo Code: pacote vive no CEREBRO sob demanda, sem impacto no teto das Instruções (`N[narrative]` segue em 6688/6900). Harness ganha o check G6. 17/17, 33/33, 0 erros.
- i-N35 marcada ✅ IMPLEMENTADA; i-N36 (universais da fase C) segue Ativa.

## 💬 Sessão anterior (2026-07-03 — v1.48.0)
- **Nicho narrativa, fase A do refino por feedback de campo (D-050, spec0021):** `never_writes` removido (contradizia `writes_prose`); modelo de colaboração vira escolha de fundação — grupo «Colaboração» no builder com «Rascunho dirigível» (padrão) e «Direção criativa». Novo behavior `write_discipline`: protocolo-sanduíche pré→durante→pós com 6 erros nomeados e «Lista de invariantes» que só cresce. Gatilho triplo de capítulo concluído + gatilho de erro apontado. Templates atualizados: CONTINUIDADE (Estado atual + Lista de invariantes), PERSONAGENS (interior/tell físico/3 camadas), BIBLIA (teto de poder), CRONOLOGIA (tempo relativo), GLOSSARY (equação de mecânica), VOZ (refresh por arco + espelho de erros). Base: meta/ANALISE-REFINO-NICHO-NARRATIVO.md (feedback dos 3 projetos de novel + ConStory-Bench). Harness 17/17, 32/32, 0 erros; `N[narrative]` instr 6688/6900.
- Registradas i-N35 (switch skills-pack de escrita, fase B) e i-N36 (universais da fase C) como Ativas.
- Item "Teto 6500 no nicho game" removido de PRÓXIMOS — absorvido pelo teto 6900 (confirmado no harness: game em 6578).

## 💬 Sessão anterior (2026-07-01 — v1.42.0)
- **Config mode-aware + nome de spec no Modo Code + obediencia (D-038/039/040, disciplina v2 Fases B-D):** diretriz de config distingue chat (modelo+esforco+pensamento) de Claude Code (modelo+`/effort`/`ultrathink`, sem toggle de pensamento); Modo Code passa a prescrever `AAAA-MM-DD-specNNNN.md`; instrucao curta reforca registro de Feedback ASU e nome simples no download. Harness 17/17. ✅ **Disciplina v2 (Fases A-D) CONCLUIDA.** Layout desktop resolvido em v1.46.0 (D-048); mobile/layout empilhado alternativo ficam como i-N33 (futuro).
- meta/ reconciliados ao estado real v1.42.0 (spec0009); mineração das notas para IDEAS; débito do teto-game e risco de nome-de-spec registrados.
- Ciclo de verificação (round-trip) gravado no CEREBRO + D-042; reforço de entrega-inteira e nome-real na entrega.
- Capturados 5 feedbacks do ASU que a sessão anterior não extraiu (spec0011).
- Auditoria de nomes dos 17 nichos concluída (D-043): repo são; política DEC/FIX por nicho formalizada; sem migração.
- **Diretriz de geração: .gitignore e README no perfil dev/Code (D-044, v1.43.0):** instrução curta ganha `.gitignore` na primeira leva que cria estrutura + `README.md` quando a estrutura estabiliza (com aviso se adiar); nota equivalente no CEREBRO gerado; commit separado confirmado já explícito (Tarefa B sem mudança). Harness 17/17.
- **Releitura do mount por turno ao sinal de upload (D-045, v1.44.0):** instrução curta funde o ritual `.txt` do início com a releitura por turno (mesmo sem nomear arquivo); v1 estourou o teto de 6500 em `game`/`narrative` só com a linha somada — v2 fundiu a linha e elevou o teto para 6900 (era conservador). Harness 17/17. Resolve o item 5 de PRÓXIMOS ("Teto 6500 no nicho game").
- **Refino da diretriz ASU por tipo de arquivo + verificação obrigatória (D-046, v1.45.0):** revisa D-037 com o estudo ASU-vs-spec-vs-inteiro — trecho localizado de capítulo vai por ASU (edição cirúrgica preserva o resto), escrita nova/reescrita profunda vai inteira (é geração, sem âncora); docs rolantes de qualquer nicho (incl. equivalentes de domínio como REVISOES) seguem sempre inteiros; verificação no disco de cada arquivo tocado por ASU agora é obrigatória e autônoma; nome do arquivo ASU corrigido para `AAMMDD-asuNNNN.yaml`. Harness 17/17.
- **Fix: D-041 aplicado ao gerador (D-047, v1.45.1):** o CEREBRO gerado ainda emitia `AAAA-MM-DD-asuNNNN.yaml` e `AAAA-MM-DD-specNNNN.md` em duas linhas (D-041 só tinha sido registrado no DECISIONS, nunca aplicado ao gerador; spec0015 corrigiu só a instrução curta). Ambas corrigidas para `AAMMDD`; logs seguem ISO (split proposital). Harness 17/17.
- Registradas i-N33 (layout responsivo, a escopar via wireframe) e i-N34 (afixo prefixo+sufixo simultaneos, refino da i-N3).
- Fecho de sessao: CONTEXT atualizado para v1.46.0, pendencia-fantasma de layout limpa, i-N34 precisada.
- **Afixo prefixo e sufixo independentes e simultaneos (D-049, v1.47.0):** afixo no download deixa de ser escolha exclusiva (none/prefix/suffix) e passa a ter dois toggles + duas caixas (prefixo e sufixo) independentes, combinando numa unica passada; cobre os 4 casos (nenhum/so prefixo/so sufixo/ambos); preview do nome final atualiza ao vivo. Fecha i-N34. Harness 17/17.
- Fecho de sessao: spec0020 aplicada, testada visualmente (4 casos batendo no navegador), commitada e enviada (8e75750).

## 💬 Sessão anterior (2026-06-30 — v1.41.0)
- **ASU por download + escopo código-vs-docs (D-037, disciplina v2 Fase A):** instrução ASU passa a ser entregue como arquivo `.yaml` para baixar (bytes UTF-8 exatos), não colada no chat; novo item de escopo código-vs-docs (código + docs de heading único como DECISIONS/CONTEXT via ASU; docs rolantes STATUS/CHANGELOG/IDEAS inteiros); dica de âncora não-ASCII. Harness 17/17.

## 💬 Sessão anterior (2026-06-29 — v1.40.0)
- **CEREBRO niche-aware (D-036, Fase 3):** gatilhos/higiene/nota "criar se faltar" respeitam os `contextFiles` do nicho — CHANGELOG/ROADMAP só onde existem; criação automática só da camada universal (STATUS/IDEAS/DECISIONS). Commit foi para a instrução curta (dev/ASU/Modo Code); ritual de checar `.txt` avulso adicionado; limpeza `DECISOES`→`DECISIONS` no template. Harness 17/17.
- ✅ **Migração para inglês + Decisão 3 CONCLUÍDA** (Fases 0-3 feitas): Fase 0 (repo KCM), Fase 1 (nomes dos nichos), Fase 2 (camada universal DECISIONS), Fase 3 (CEREBRO por nicho). D-035 + D-036 fechados.

## 💬 Sessão anterior (2026-06-29 — v1.39.0)
- **Camada universal DECISIONS (D-035, Fase 2):** DECISIONS.md adicionado aos 12 nichos que nao tinham (animation, client, comics, cuisine, custom, game, marketing, music, narrative, pixel, research, rpg); game: residuo IDEIAS->IDEAS em prosa corrigido. Harness 17/17. Fase 3 pendente — exige Opus no Code.

## 💬 Sessão anterior (2026-06-29 — v1.38.0)
- **Nomes de gestao dos nichos em ingles (D-035, Fase 1):** DECISOES→DECISIONS, IDEIAS→IDEAS, GLOSSARIO→GLOSSARY, CONTEXTO→CONTEXT, HISTORICO→HISTORY nos 7 nichos afetados; conteudo de nicho permanece PT. Harness 17/17. Fases 2-3 pendentes.

## 💬 Sessão anterior (2026-06-29 — v1.37.1)
- **Nomes de gestao em ingles (D-035, Fase 0):** `git mv` de DECISOES→DECISIONS, IDEIAS→IDEAS, GLOSSARIO→GLOSSARY, HISTORICO→HISTORY + todas as referencias nos `.md` do repo. Template (nichos, Fases 1-3) pendente.

## 💬 Sessão anterior (2026-06-29 — v1.37.0)
- **Diretriz ASU reescrita (D-033):** editar→ASU (`AAAA-MM-DD-asuNNNN.yaml`); arquivo novo→baixar; lembrete UI e instrução curta citam ASU + `PROMPT_IA.md`.
- **Recomendação de config (D-034):** home nomeada no CEREBRO + gatilho na instrução curta. Nome de log reforçado na instrução curta.
- Harness: **17/17, 0 erros, 32 checagens**.

## 💬 Sessão anterior (2026-06-21 — v1.36.0)
- **Refator modular (D-028) embutido:** `index.html` agora é gerado de `src/index.template.html` + 17 módulos `src/niches/*.js` via `build.js`. Produto continua 1 arquivo único, sem build no lado do usuário (preserva D-001).
- **Cérebro renomeado (D-029):** arquivo gerado do cérebro mudou de `CLAUDE.md` → `CEREBRO.md` em todos os projetos gerados (sempre, não condicional). Libera o nome `CLAUDE.md` para o arquivo-raiz do Claude Code. Todas as referências no template, triggers, behaviors e harness atualizadas.
- **"Feedback para o ASU" no IDEAS gerado:** nova seção logo abaixo de "Feedback para o Kit" no template universal `UNIVERSAL_IDEAS_TPL`.
- Harness: **17/17, 0 erros, 32 checagens**.
- **ROADMAP atualizado para v1.34.0** (modular concluído, i18n em avaliação, fase «Modo Code» aberta) — primeira atualização feita pelo método "doc por spec" (D-030).
- Arquivos tocados nesta sessão (Code): meta/ROADMAP.md, meta/DECISIONS.md, meta/STATUS.md, meta/CEREBRO.md.
- **v1.35.0 — switch «Modo Code» implementado** (D-031): toggle `codeMode` injeta no CEREBRO.md gerado as raias chat↔Code + o método doc-por-spec; (Tarefa B) apêndice com starter `.claude/`. Projetos gerados herdam a capacidade de trabalhar no Claude Code.
- **v1.36.0 — ASU quick wins** (D-032): lembrete na UI ao ligar o ASU + diretriz ancorada no `format_version` do guia. (Parte "HUB" do item c: sem referência de versão no HUB hoje — nada a fazer.)

## 🎯 PRÓXIMOS (decidir/fazer)
1. ✅ **ASU quick wins** (v1.36.0, D-032): lembrete na UI ao ligar o switch ASU + diretriz ancorada no `format_version` do guia — **concluído**.
2. ✅ **"Modo Code"** (v1.35.0, D-031; refinado em v1.53.0, D-055): switch que gera o kit de arranque — agora como pacote `claude-code-kit.zip` separado (CLAUDE.md raiz + `.claude/settings.json` + comandos no formato Skills atual), protocolo de raias no CEREBRO. **Concluído.**
3. **Consolidar o CINZEIRO no HUB** — EM ANDAMENTO pelo usuário.
4. **README/PLANNING:** reescrever quando der (pitch novo — "kit desenvolve" + HUB/Cânone).
5. **Padronização de nome de spec não obedecida em campo:** ao atualizar o CEREBRO do ASU com o Modo Code, o projeto consumidor **não** corrigiu os nomes das specs para o padrão. Sinal de que D-039/D-041 precisam de reforço na camada lida-todo-turno (instrução curta), não só no CEREBRO. Conecta às frentes de auditoria/obediência.

## Fase atual
🏁 **Maduro e em produção.** Publicado (`silujones.github.io/kit-contexto/`), dogfooded, com pilotos reais (jogo CINZEIRO/Fando em grupo). São **17 nichos** (16 de conteúdo + 1 construtor `custom`). Três capacidades no ar: manter contexto; o kit DESENVOLVE (narrative escreve, game cria); coordenar grupos (HUB com Cânone Central). O harness 17/17 + anti-testes é a rede de segurança a cada release.

## 🎯 PRÓXIMO TRABALHO (decidir/fazer)
1. **Consolidar o CINZEIRO no HUB** — EM ANDAMENTO **pelo usuário** (ele faz; depois pode trazer o resultado para ajuste fino). O `buildHub` já gera a forma do `CANON.md` do piloto; falta comparar/consolidar (conteúdo do piloto + forma do kit) e ver se Cânone Central + códigos cobrem o uso real.
2. **DECISÃO ESTRATÉGICA — refator modular + i18n (i-N13 expandido / i-N26).** O usuário **aceita a direção** (dados de nicho em JSON + núcleo → abre i18n e idioma misto); preocupação = não quebrar a ferramenta no processo (mitigação: harness 17/17 como rede, migrar nicho a nicho). Condiciona o README/PLANNING. **Sem código até o "vai" explícito.**
3. **README/PLANNING:** reescrever quando der (pitch novo — "kit desenvolve" + HUB/Cânone). Idealmente depois de decidir o item 2.
4. **Esperar os pilotos** (decisão do usuário): lote i-N23 (pixel), i-N25 (música), e estender "desenvolve" a HQ/RPG/animação — ele vai usar mais e dar feedback antes de qualquer mexida.

✅ **Concluído nesta sessão (v1.33.0):** códigos de área curados + variador (D-027); rótulo de grupo. Direção i18n/modular capturada. Handoff completo gerado.
✅ **Concluído antes:** HUB inspirado no CANON.md (v1.32.0, D-026); página construtora do HUB (v1.31.0, D-025); switch + Instruções enxutas −27% (v1.30.x, D-024); FIX-005 Pages (.nojekyll); "kit desenvolve" fase 1 (v1.29.0, D-023); lote D-022 + D-018 (v1.28.0); FIX-004 chips (v1.27.1); P12/P13 (v1.27.0, D-020/D-021).

## 🧭 Decisões maiores em avaliação (ver ROADMAP / IDEAS)
- **Refator modular + i18n (i-N13 expandido + i-N26):** migrar dados de nicho para JSON + núcleo central; abre troca de idioma da UI e dos templates de forma auditável, inclusive **idioma misto** (artefatos/código/meta em inglês; UI e conversa em pt-BR). **Direção aceita; sem código até decisão.** Risco: a migração quebrar a geração → mitigação: harness 17/17, migrar nicho a nicho validando a cada passo. (Era a Fase 4 "em avaliação"; agora com motivação forte.)
- **Nicho/ferramenta de guias/tutoriais/wikis (i-N14):** aprender ferramentas (Aseprite/Unity/Godot/Unreal/Excel/linguagens), platinar jogos. Pode ser nicho OU ferramenta separada. Conecta a "Educação" (NICHOS-CANDIDATOS nº1).
- **Auto-aplicação de patches (i-N15) + entrega por diff (i-N16):** a IA gera "arquivos de atualização" estruturados (estilo apply_patch) e uma ferramenta local aplica — menos trabalho manual e, com diffs em vez de arquivos inteiros, menos output tokens. Avaliar viabilidade/segurança.

## 🎯 Outras pendências (sem urgência — detalhe no ROADMAP/IDEAS)
- **README/PLANNING** desatualizados (pitch pós "kit desenvolve" + HUB) — reescrever quando der (também item 3 acima).
- **Revisar polimento/qualidade das Instruções geradas** — em parte resolvido pela compressão (−27%) + teto de 6500; confirmar em uso real.
- **Nichos novos (FUTURO, adiados de propósito):** ver `NICHOS-CANDIDATOS.md` — Educação & Cursos (nº1), depois Desenvolvimento Pessoal/Journaling (sensível), Jurídico/Podcast/Tradução. (i15 / i-N14 em IDEAS.)
- **spec-kit para dev/game (i-N7, FUTURO):** análise do Spec-Driven Development quando houver mais feedback de uso.
- **Evoluções de polish** (do CHANGELOG "possíveis v1.1/v1.2"): export/import de preset JSON (i11), tema claro (i9), tradução EN (i10 → agora dentro de i-N26/i18n), PDF dos templates (i13), drag-and-drop no Custom (i20), carimbo de versão nos downloads (i-N10). Nada prometido.
- ✅ **Resolvidos:** MAPA.md "17 prontos" → 16+1 (v1.29.0); reagrupar/renomear narrative (rótulo do grupo, v1.33.0).

## 🔎 Mount (D-018) + localStorage — lembrete operacional
O conector do GitHub alimenta **só o RAG/Conhecimento do Projeto** (busca, com subpastas); **não** popula o mount `/mnt/project/`. **Só o upload direto** popula o mount, **achatado** (sem subpastas; nomes iguais colidem). Para eu ler/editar pelo mount: subir os arquivos DIRETO no Projeto + ligar a ferramenta de código. **`localStorage` é por origem:** presets/HUB do site publicado NÃO aparecem no arquivo local (`file://`) e vice-versa (não é bug).
**FlatDrop / `_MANIFEST.md`:** NÃO é padrão — detectar pela presença do `_MANIFEST.md`. Se existe: consultar (caminho original → nome plano; sufixo `__pasta` = colisão), entregar pelo nome real, usar para entender a estrutura. Se não existe: fluxo normal, sem travar. O FlatDrop filtra o upload (tipos não aceitos; `node_modules`/`venv`/`.git`; `.gitignore` opcional) — ausência pode ser deliberada. Regra no CLAUDE.md; decisão em D-022.

## 🧪 Validação (regra dura: NUNCA publicar sem 17/17 e 0 erros)
Hoje o harness está **consolidado em `/home/claude/kit/validate.js`** (boot limpo por nicho via shim; ~24 checagens de conteúdo + chips + round-trip do HUB + smoke do `buildHub` + teto de 6500 nas Instruções). O container reseta entre sessões → **recriar o harness a cada sessão** (e `npm install jsdom` se faltar). Em sessões antigas existiram arquivos separados (`validate-switch/compose/conflict/reuse`, `t-prompt/shortcut/granular`) — as checagens foram absorvidas no `validate.js` atual; recriar conforme a necessidade. Padrão a cada mexida: `node --check` no `<script>`, balanceamento `<div>`/`</div>` (283/283), **anti-testes** (desfazer a correção numa cópia → o teste reprova), sincronizar para outputs e conferir md5.

## 🗺 Onde está no código (v1.33.0; números aproximados, mudam ao editar)
- **HUB (D-025/026/027), ~8151+:** `NICHE_CODE` (mapa de códigos curados), `baseCode(f)`, `computeCodes(frentes)` (variador DEV0/DEV1), `buildHub()` (identificadores → D1–D6 → Cânone Central → frentes com `[ORIGEM-NNN]` → status), `renderHubChips()` (chips add-style), `renderHubRows()` (linhas com campo `código` + CSS `.hubrow select,.hubrow input` no estilo do kit), `renderHub`/`wireHub`/`updateHubPreview`/`persistHub`/`loadHub`. View `#v-hub` (nav `data-view="hub"`); `setView` chama `renderHub`; boot chama `loadHub()`+`wireHub()`. `STATE.hub` em `LS_HUB="kit-hub-v1"`. `effectiveFiles` NÃO injeta HUB.md no nicho.
- **Diretriz de personalização das Instruções (v1.32.0):** `L.push` logo após "versão curta deste arquivo" no `buildClaudeMd`.
- **`## Código / build` (v1.32.0):** dentro do LOG-TEMPLATE do nicho **game** (após "Decisões de design"). LOG-TEMPLATE é **por-nicho** (16 definições distintas).
- **Instruções enxutas (v1.30.0):** em `buildInstr`, os universais (ids de `BEHAVIORS_BASE`) viram 1 linha de nomes; behaviors do nicho seguem em bullets. Teto de 6500 no harness.
- **IDEAS universal (v1.29.0):** `UNIVERSAL_IDEAS_TPL` (constante de fundação, antes de `HYGIENE_RULES`) injetado em `normNiche` (`_files.some(/^IDE(A|IA)S\.md$/i)`); regra "cria na primeira necessidade" no `buildClaudeMd` após a tabela de gatilhos. Narrative: convention[0] reescrita + kishōtenketsu + behavior `writes_prose` + prompt `id:"J"`. Game: `builds_game` + template `ROTEIRO.md` + output `roteiro` + convention "constrói".
- **Lote D-022 (v1.28.0):** i-N19 na def de `check_before_ask` (P8) + bullet na seção «Verifica antes de pedir um arquivo» do buildClaudeMd; i-N22 em `HYGIENE_RULES`; i-N21 em `TRIGGERS_BASE`; i-N20 em `commitIntro`; i-N18 em `handoffComo`. D-018: itens do `handoffComo` + callouts da tela "Tokens & Fluxos".
- **FIX-004:** `normBuilderSection` (~6200) `opts: g.items.map(it => Array.isArray(it) ? it : [it,it])` — chips de `client`/`narrative` (formato par). `BEHAVIORS_BASE` = 13 (P12 `shrink_hygiene` + P13 `research_refute` no fim). `getCurrentNiche` usa `raw.isBuilder`.
- **Custom:** `composeFromNiches(niches,sel)` (~7507) + chips `data-sc`; `STATE._sc`; presets `toPreset`(body STRING, FIX-003)/`fromPreset`/`mergeCustom`; atalho "Nichos salvos"; `CONTROLS_SKELETON`+captura/restauração (FIX-001). LS: `LS_PRESETS="kit-custom-presets"`, `LS_PRESET_CURR="kit-custom-current"`.

## 🗂 Convenções
- pt-BR em tudo, inclusive comentários de código. Nomes de template profissionais.
- Entrega: arquivos completos em `outputs/` (o usuário organiza no repo: `index.html` na raiz, `.md` em `meta\`).
- Commit ao final: comando completo p/ CMD Windows (UMA linha por comando, `-m` repetido), **sem acentos** (CMD corrompe acentos em `-m`). 3 linhas: `git add` listando, `git commit`, `git push`.
- Usuário no CMD do Windows. Repo: `github.com/SiluJones/kit-contexto`; site `silujones.github.io/kit-contexto`; `.nojekyll` na raiz.

## 💬 Última sessão (2026-06-14 — v1.33.0 + handoff)
Fechamento da conversa p14–p18 (v1.27.1 → v1.33.0). Nesta virada:
- **Códigos de área do HUB** (D-027): defaults curados por nicho (mapa `NICHE_CODE`: DEV, GAME, PIXEL, NARR, SOM, HQ, COZ, RPG, ANIM, BRAIN/IDEIA…) e **variador** de duplicata (`computeCodes`: DEV0/DEV1/DEV2; único fica sem sufixo). Rótulo de grupo: "Histórias, Jogos & Mídia — mundos, arte e som".
- **Direção estratégica capturada** (sem código): refator modular (i-N13) + **i18n com idioma misto** (i-N26) — UI/conversa no idioma do usuário, artefatos/código/meta em inglês. O usuário aprova a direção; preocupação = não quebrar a ferramenta (harness 17/17 é a rede).
- **Handoff gerado** com máximo detalhe: CONTEXT (reescrito), STATUS, DECISIONS, CHANGELOG, IDEAS, ROADMAP, GLOSSARY, LOG-TEMPLATE + HISTORY.md + logs/2026-06-14.md.
**Próximo de fato:** decidir o refator modular + i18n (item 2); o CINZEIRO no HUB segue com o usuário.

## 💬 Última sessão (2026-07-02 — v1.46.0, spec0018)
Aplicada a spec0018 (layout desktop) via `/apply-spec`: `.builder` (2 colunas) só colapsa a partir de 700px (antes 900px, novo `@media (max-width: 700px)`); `.rail` vira barra superior **sticky** (não mais `static`) em `<=900px`, com fundo semi-opaco + blur para legibilidade sobre o conteúdo que rola por baixo; `.out` ganha `min-height:340px` (antes `height:fit-content`) e `@keyframes fade` perde o `translateY` — elimina o layout shift ao trocar opções/abas. Harness 17/17, 0 erros. Checagem visual feita em ~800px (servidor estático local): painéis lado a lado confirmados, rail sticky confirmado por scroll, `.out` sticky mantido. Mobile e layout empilhado alternativo seguem como i-N33 (futuro), fora do escopo desta spec (D-048).

## 💬 Última sessão (2026-07-04 — v1.50.0, spec0023)
Aplicada a spec0023 (skills de escrita viram `skills.zip` separado + ponteiro no CEREBRO, D-052) via `/apply-spec`: apêndice inline no CEREBRO (que emitia as 4 SKILL.md inteiras, +29% de inchaço, com instrução autodestrutiva «pode apagar este apêndice») vira ponteiro curto (intro + tabela de gatilhos + instalação, +4,4%); novo `buildSkillMd`/`downloadSkillsZIP` gera o zip `skills/<nome>/SKILL.md` × 4 + `skills/README.md`, reusando o JSZip do `downloadZIP`; novo botão «↓ Baixar skills (.zip)» na aba Templates (visível só com `skillsPack` + modo skills ligado). Harness G6 reescrito (prova que o corpo NÃO vaza pro CEREBRO). 17/17, 33/33, 0 erros; `N[narrative]` segue em 6688/6900. Verificação manual em navegador (servidor estático local): botão aparece ao ligar o toggle, zip gera com a estrutura correta, CEREBRO gerado confirmado só com o ponteiro. Princípio «CEREBRO só com regra fixa, sem artefato autodestrutivo» firmado como diretriz do KCM; abre i-N37 (espelhar no Modo Code, spec0026).

## 💬 Última sessão (2026-07-04 — v1.51.0, spec0024)
Aplicada a spec0024 (controle de skills sai do topbar e vai para o builder «A obra», default ligado, D-053) via `/apply-spec`: toggle `skillsMode` sai da linha de toggles universais do topbar e passa a viver dentro do painel «A obra» do narrative (seção «Skills de escrita», ao lado de Gênero/Formato/Colaboração), já **LIGADO por padrão**; estado migra de `STATE.topbar.skillsMode` para `STATE.builder.skillsMode`; `skillsPackOn()` vira default-ON (só `"no"` explícito desliga). Harness G6 reescrito para provar o default LIGADO e a ausência de `skillsMode` no `topbar` normalizado. 17/17, 33/33, 0 erros; `N[narrative]` segue em 6688/6900. Base: `meta/ANALISE-CEREBRO-MODOS-E-SKILLS.md` (Q2, opção (a)). Q2 marcada aplicada em IDEAS (i-N36); reforma dos 3 universais segue como pesquisa futura.

## 💬 Última sessão (2026-07-04 — v1.52.0, spec0025)
Aplicada a spec0025 (refino das 4 skills de escrita com 10 técnicas de campo, Q3, D-054) via `/apply-spec`: `escrita-serial` ganha «Técnicas de cena» (flag de uma frase, discurso direto vs. resumo do narrador, teste de imersão, ratio de diálogo ~40%, regra de ouro do POV paralelo); `checagem-continuidade` ganha a «Pergunta-oráculo»; `voz-calibragem` ganha a cura do drift cognitivo (verbo mental → gesto físico) + teste «ela-não-come» + anti-correção; `textura-mundo` ganha «Fazer o mundo respirar» (retecer fora de cena, revelar por atrito, checklist 1-de-3). Sem custo de contexto: técnicas vão pro SKILL.md do zip (spec0023), ponteiro no CEREBRO não muda. 17/17, 33/33, 0 erros; `N[narrative]` segue em 6688/6900. Base: `meta/ANALISE-CEREBRO-MODOS-E-SKILLS.md` (Q3). Encerra o ciclo do refino narrativo (fases A/B + Q1/Q2/Q3); restam spec0026 (Modo Code espelha D-052) e a fase futura (reforma dos 3 universais + feedback ambiental, i-N36).

## 💬 Última sessão (2026-07-04 — v1.53.0, spec0026)
Aplicada a spec0026 (Modo Code: kit de arranque vira download separado + formato Skills atual, i-N37, D-055) via `/apply-spec`: apêndice inline (starter `.claude/commands/*.md` + instrução autodestrutiva «pode apagar este apêndice») vira ponteiro curto (tabela + instalação) no CEREBRO; novo `buildCodeKitFiles`/`downloadCodeKitZIP` gera `claude-code-kit.zip` (`CLAUDE.md` + `.claude/settings.json` + `.claude/skills/apply-spec/SKILL.md` + `.claude/skills/wrap/SKILL.md` + `README.md`), reusando o JSZip do `downloadZIP`; comandos migram do formato legado para `.claude/skills/<nome>/SKILL.md` (frontmatter `name`/`description`/`disable-model-invocation: true`); CLAUDE.md starter atualizado (regra «< 200 linhas», config Sonnet/Opus atual). Novo botão «↓ Baixar kit do Claude Code (.zip)» na aba Templates (visível só com Modo Code ligado). Harness G7 novo. 17/17, 34/34, 0 erros. Verificação manual em navegador (servidor estático local): botão aparece/some com o toggle, CEREBRO gerado confirmado só com o ponteiro (sem inline, sem «pode apagar»), zip gerado com a estrutura correta via JSZip real. Fecha i-N37 e o ciclo de refino de modos (skills+code); i-N7 fechada (analisada); abertas i-N38 (hook de pre-commit) e i-N39 (`/check-spec`).

## 💬 Última sessão (2026-07-04 — fecho de sessão, spec0027, doc-only)
Fecho da jornada 0021→0026: cabeçalho do STATUS atualizado para 2026-07-04/v1.53.0 (corpo já estava em dia); item 2 de PRÓXIMOS marcado ✅ concluído (Modo Code); criado `logs/2026-07-04.md` com o resumo da jornada; FIX-006 registrado no DECISIONS (chat entregou meta de handoff incompletos sem avisar — sem dano ao repo, appends do Code mantiveram tudo íntegro). Ciclo de refino de modos (skills+Code) FECHADO.

## 💬 Última sessão (2026-07-06 — v1.54.0, spec0028)
Aplicada a spec0028 (painel «Modo de trabalho» global recolhível; os 3 modos saem do topbar, i-N36 fase C parte 1, D-056) via `/apply-spec`: `groupMode`/`asuMode`/`codeMode` saem da injeção de toggles soltos no `topbar` de todo nicho e passam a viver num painel `<details class="workmode">` (checkbox agrupado, progressive disclosure nativa), posicionado no `.main` logo abaixo do topbar sticky e **fora de qualquer `.view`** — visível/recolhível de qualquer aba, com contador "N ativos" no summary. Estado migra de `STATE.topbar.*` para `STATE.workmode.*`, continua **por-nicho** (persistência e saída gerada inalteradas). Harness G4/G5/G7 migrados para `STATE.workmode` + guarda nova "modo NAO deveria mais estar no topbar" (trava de regressão). 17/17, 34/34, 0 erros. Verificação manual em navegador (servidor estático local): painel aparece aberto com os 3 checkboxes + descrições, toggles sumiram do topbar, contador atualiza ao clicar, estado sobrevive a reload. Registrada i-N40 (Modo Atualização, analisada/a escopar) em IDEAS — spec0029 (selos de estado) é o próximo passo, depende dos helpers deste spec.

## 💬 Última sessão (2026-07-06 — v1.55.0, spec0029)
Aplicada a spec0029 (selos de estado multicanal empilháveis perto da saída, i-N36 fase C parte 2, D-057) via `/apply-spec`: cada modo ligado (grupo/Code/ASU) ganha um `.selo` acima do preview da saída — três canais (cor + glifo + rótulo), nunca cor sozinha (WCAG 1.4.1); contraste no contorno/texto, fundo transparente (WCAG 1.4.11); ordem estável grupo→Code→ASU; ASU = chevron duplo `»` + `--teal:#5cc2c9`. `workBadges()` pura (fonte de verdade testável) + `renderWorkBadges()` espelha no DOM, chamada no fim de `updatePreview`. Harness G8 novo + `workBadges` exposto no SHIM. **Desvio da spec aplicado e registrado (D-057):** classes CSS viraram `.selo-group`/`.selo-code`/`.selo-asu` (em vez de `.group`/`.code`/`.asu`) porque `.group` colidia com a classe utilitária genérica de painéis já existente no template, quebrando o contraste do selo «Grupo» (fundo sólido em vez de transparente) — achado na verificação visual e confirmado com o usuário antes de corrigir; ids internos e harness não mudaram. 17/17, 35/35, 0 erros. Verificação manual em navegador (servidor estático local): 0 modos → 0 selos; 1 modo → 1 selo; 3 modos → 3 selos na ordem certa, contorno colorido + fundo transparente confirmado via computed styles; desligar 1 mantém a ordem dos que restam. **i-N36 fase C (reforma dos 3 modos + feedback ambiental) está FECHADA** (0028+0029); resta só a fase "topbar inteiro" (adiada) + o encaixe do atualizador i-N40.

## 💬 Última sessão (2026-07-06 — fecho de sessão, spec0029, doc-only)
Fecho da aplicação da spec0029: commit `6fa5521` já feito e enviado (`git push`) nesta mesma sessão. FIX-007 registrado no DECISIONS — a spec nomeou a classe CSS do selo de grupo como `.group`, que colidia com a classe utilitária genérica de painel já existente no template (achado só na verificação visual manual no navegador, não pelo harness JS); corrigido para `.selo-group`/`.selo-code`/`.selo-asu` com o usuário confirmando a correção antes de aplicar. Nenhuma mudança de código pendente nesta sessão — árvore de trabalho limpa.

## 💬 Última sessão (2026-07-06 — v1.56.0, spec0030)
Aplicada a spec0030 (botões de modo no topbar, i-N36 fase "topbar inteiro" parte 1, D-058) via `/apply-spec`: os 3 modos (grupo/Code/ASU) saem do painel `<details class="workmode">` (spec0028) e dos selos perto da saída (spec0029) e passam a viver como cluster `.modes` de botões-toggle (`.modebtn`, `aria-pressed`) dentro do próprio `#topbar`, herdando o `sticky` dele — fixos ao rolar de graça. Ativo, o botão enche com a cor do modo (Grupo verde, **Code laranja de verdade** via nova variável `--code:#e8823a`, ASU teal) + rótulo curto; descrições viram tooltip própria (`.tip`, não `title`) em `:hover`/`:focus-visible`, `pointer-events:none`, `aria-describedby`→`role="tooltip"` (WCAG 1.4.13). `STATE.workmode` e `workBadges()` mantidos (G8), `workBadges()` agora lê a fonte única `WORK_MODES` (antes `WORK_SELOS`), mesma forma/ordem. Harness sem check novo nem alterado — 17/17, 35/35, 0 erros.

## 💬 Última sessão (2026-07-06 — v1.57.0, spec0031)
Aplicada a spec0031 (cluster de ação no canto + modal de configuração, i-N36 fase "topbar inteiro" parte 2, D-059) via `/apply-spec`: o `.sync-note` redundante do canto do topbar (● + rótulo do nicho) sai e vira cluster `.actions`, que nasce com a engrenagem (`#act-cfg`) — clicar abre um `<dialog id="cfg-dialog">` nativo (`showModal()`: centraliza, prende foco, fecha no Esc, tem backdrop) com a seção **Projeto**, que passa a hospedar a config do afixo (migrada da aba Templates por move de HTML, IDs e `applyAffix` intactos). Download/atualizar entram no cluster nas specs 0033/0034; SO adiado (spec própria). **Desvio técnico encontrado na verificação visual e corrigido com confirmação do usuário (ver D-059):** a spec mandava inserir o `<dialog>` depois do `</script>` final, mas isso quebra a fiação — `boot()` roda síncrono antes do parser HTML alcançar aquele trecho, então `$("#cfg-close")`/`$("#affix-prefix-on")` etc. retornavam `null` (X e backdrop não fechavam o modal; toggle de prefixo/sufixo não habilitava os campos nem atualizava o preview; só o Esc nativo funcionava, mascarando o problema). Corrigido movendo o bloco `<dialog>` para antes do `<script>` (mesmo HTML/IDs/CSS/JS, só a posição no documento). Reconfirmado no navegador: X, backdrop e Esc fecham; toggle liga o campo e o preview atualiza ao digitar; aba Templates não mostra mais o afixo; abrir/fechar não troca nicho nem página. Harness sem check novo nem alterado — 17/17, 35/35, 0 erros.

## 💬 Última sessão (2026-07-06 — v1.58.0, spec0032)
Aplicada a spec0032 (campos multi-seleção + aba Nicho no modal, i-N36 fase "topbar inteiro" parte 3, D-060) via `/apply-spec`: o sistema de campos ganha `type:"multi"` (chips aditivos, valor-lista) e `type:"segmented"` (chip de escolha única), roteados ao modal por `panel:"modal"` — nova seção **Nicho** (`#cfg-sec-nicho`/`#cfg-nicho-body`, `renderNicheConfig`), `hidden` quando o nicho não tem campo `panel:"modal"`; `renderTopbar` passa a filtrar esses campos para fora da topbar. Reusa `.chip`/`.chips`/`.subhead` do builder, sem componente novo. Gênero de jogo (`genreSel`, 14 opções) sai da topbar, vira `multi`+`panel:"modal"`, label "Gênero (pode ser mais de um)"; degrada limpo de valor antigo string (cai no default `[]`). **Achado registrado:** campos de topbar do nicho não entram na saída gerada hoje (só metadados de UI) — fiação até o CEREBRO/Instruções fica para spec futura (i-N41). Harness sem check novo nem alterado — 17/17, 35/35, 0 erros.

## 💬 Última sessão (2026-07-07 — v1.59.0, spec0033)
Aplicada a spec0033 (campos de nicho na saída + conserto do Estágio, i-N41, D-061) via `/apply-spec`: `buildInstr` ganha o bloco **"Contexto do projeto"** logo após o "Estágio", nas Instruções — emite os campos de `niche.topbar` ainda não consumidos no cabeçalho (ex.: gênero, engine) só quando têm valor (array vira lista por vírgula, campos unidos por " · "); IDs já consumidos (`project`/`versao`/`fase`/`phase`/`langSel` etc.) ficam fora, sem duplicar. **Conserto de brinde:** o check do Estágio só testava `tb.fase`, mas o campo "Fase" (game/narrativa) usa `id:"phase"` — a Fase nunca aparecia na saída; ganhou `|| tb.phase`. `src/niches/game.js`: rótulo do `genreSel` simplificado de "Gênero (pode ser mais de um)" para "Gênero" (os chips já deixam claro que é multi). `buildClaudeMd` intacto — contexto é do CONTEXT, não do CEREBRO. Harness sem check novo nem alterado — 17/17, 35/35, 0 erros; teto 6900 intacto (bloco guardado por presença de valor, não emite com topbar vazia). **i-N41 fechada.**

## 💬 Última sessão (2026-07-07 — v1.60.0, spec0034)
Aplicada a spec0034 (download estruturado — projeto novo — + botão no cluster de ação, i-N36 / análise §1, D-062) via `/apply-spec`: novo botão `#act-dl` ("↓") no cluster de ação do canto, ao lado da engrenagem, chamando `downloadStructuredZIP` — gera um zip com a **árvore de pastas pronta pra começar**, ciente da config: `<raiz>/meta/` (arquivos de contexto, nomes canônicos **sem afixo** — o afixo é do fluxo de atualização, spec0035) + `INSTRUCOES-DO-PROJETO.md` na raiz + `logs/.gitkeep` sempre; com modo Code ligado, soma `CLAUDE.md`, `.claude/settings.json`, `.claude/skills/apply-spec|wrap/SKILL.md` e `meta/specs/.gitkeep`; skills do nicho (se ligadas) em `.claude/skills/<nome>/SKILL.md` (Code) ou `skills/<nome>/SKILL.md` (sem); `.gitignore`/`.flatdropignore` gerados (o segundo já enxuga `meta/specs/` no modo Code) + `README.md` de arranque; raiz do zip = slug do nome do projeto ou "projeto". Downloads granulares (Baixar todos/ZIP/skills/kit-Code, aba Templates) mantidos — intenção "peças avulsas" separada. Verificado no navegador (JSZip in-page): árvore base, árvore com Code, árvore com skills+Code e conteúdo do `.flatdropignore` conferem com a spec. Harness sem check novo nem alterado — 17/17, 35/35, 0 erros (função de download é ação de blob, como os demais, sem check dedicado).

## 💬 Última sessão (2026-07-07 — v1.61.0, spec0035)
Aplicada a spec0035 (Modo Atualização Fase A: motor `buildUpdatePack`, i-N40, D-063) via `/apply-spec`: novas funções no template — `updateFlat` (insere o afixo fixo `__template-update` antes da última extensão), `buildUpdateManifest` (gera `_UPDATE-MANIFEST.md`: nicho/modos/data/afixo + tabela nome-plano → destino real · natureza · papel) e `buildUpdatePack(niche)` (coleta meta + CEREBRO + INSTRUCOES + skills/kit-Code conforme modo, achatados e afixados; CEREBRO/INSTRUCOES classificados `fusao`, os demais `template`). SHIM do harness ganhou `buildUpdatePack`; novo check **G9** valida pack não vazio, manifesto com assinatura, nomes planos únicos (com/sem Code), afixo presente em todos, gating do kit-Code e natureza `fusao` do CEREBRO/INSTRUCOES. Só o motor — sem UI, sem zip, sem prompt (Fase B/C ficam para spec0036/0037). Aditivo; downloads antigos intactos. Harness: **+1 check — 17/17 nichos, 36/36 checagens, 0 erros.**

## 💬 Última sessão (2026-07-07 — v1.62.0, spec0036)
Aplicada a spec0036 (Modo Atualização Fase B: prompt + UI do atualizador, i-N40, D-064) via `/apply-spec`: `buildUpdatePrompt(niche)` monta o disparo para a IA-alvo (comparar cada arquivo do pacote com o vivo, reportar novidade/choque/lacuna, nunca sobrescrever, merge para itens `fusao`) — **regra dura: sem blocos de diff**; `downloadUpdatePack()` zipa (JSZip) o pacote achatado + `_UPDATE-MANIFEST.md` + `_UPDATE-PROMPT.md`; botão **↻** (`#act-upd`) no cluster de ação abre `<dialog id="upd-dialog">` com nota do pacote, linha de status dos modos ligados agora (`renderUpdateDialog`) e as duas saídas (baixar zip / copiar prompt, com fallback se `navigator.clipboard` falhar); `<dialog>` antes do `<script>` (lição D-059). SHIM do harness ganhou `buildUpdatePrompt`; novo check **G10** trava a regra dura (prompt não vazio, com rotina de comparação e regra de não-sobrescrever, sem ```). Verificado no navegador: cluster com três botões, dialog abre com status correto, prompt sem blocos de diff, fechamento mantém nicho/aba. Harness: **+1 check — 17/17 nichos, 37/37 checagens, 0 erros.** Falta só a Fase C (`UPDATE_PROTOCOL` no CEREBRO, spec0037) para fechar o i-N40.
