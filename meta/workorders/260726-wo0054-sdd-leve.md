# wo0054 — SDD leve: spec de feature + critérios de aceite + cláusula de ambiguidade

> **Raia:** Code. **Config:** modelo **Sonnet 5**, esforço **Alto**, terminal **PowerShell**.
> **Pré-requisito:** `v1.76.0` (pós-wo0053, commit `eca02f7`, pushado), harness **18/18 · 59/59 · 0 erros**.
> **Resultado esperado:** **18/18 nichos · 60/60 checagens · 0 erros.**
> **⚠️ Já validado:** aplicado numa reconstrução pós-wo0053 em sandbox → verde **18/18 · 60/60**.
> **Rode `/check-wo` antes de aplicar** (primeira WO com o comando novo).
>
> **Origem:** análise i-N7 (`meta/analises/260718-ANALISE-i-N7-SDD-NICHOS-CODIGO.md`), itens **P1/P2/L3**.
> Primeira WO a usar «spec» no sentido novo: **spec = spec de feature** (o *que* construir e o que é
> «pronto»); a instrução de aplicação é a WO.

## ⚠️ Restrição de teto que moldou o desenho (medida, não suposta)

Testei adicionar um arquivo opcional ao `game`: **6976 > 6900 — estoura**. O `game` tem folga de **21 chars**.
Por isso o desenho **não** é simétrico:

- **`dev`** (folga 889) → ganha o arquivo-modelo `SPEC.md`.
- **`game`** → **não ganha arquivo novo**. Ele **já tem `MECANICAS.md`**, que é a spec de mecânica; o reforço
  vai no **prompt** (custo zero no teto).
- **L3** vai na descrição de um princípio → cresce só o CEREBRO (custo zero).

Isto é a evidência concreta de que o **enxugamento das Instruções** (sua ideia) precisa vir antes de qualquer
nova frente que queira arquivo no `game`.

---

## Tarefa A — `src/index.template.html`

### A1 (L3) — princípio `analyze` ganha a cláusula de ambiguidade
**Âncora:** `e apresenta a posição — a favor, aprimorando ou contra — sempre explicada e sinalizada.`
**Substituir por:**
```
e apresenta a posição — a favor, aprimorando ou contra — sempre explicada e sinalizada. Quando o pedido for ambíguo ou de escala de feature, exponha as lacunas e o que assumiu ANTES de construir; em tarefa pequena a regra continua sendo fazer, não levantar bandeira.
```
> A segunda metade é deliberada: sem ela, a cláusula viraria o «levantar bandeira em tudo» que queremos evitar.

### A2 — bump `KIT_VERSION`
**Âncora:** `const KIT_VERSION = "1.76.0";` → `const KIT_VERSION = "1.77.0";`

---

## Tarefa B — `src/niches/dev.js`

### B1 (P1) — arquivo-modelo `SPEC.md` (opcional)
**Âncora:** `  contextFiles:[`
**Substituir por:**
```javascript
  contextFiles:[
    {name:"SPEC.md", cat:"opcional", role:"Modelo de spec de feature: o problema, os critérios de aceite verificáveis, as decisões e o fora-de-escopo. Copie para `specs/AAMMDD-nome.md`, uma por feature.", content:`# SPEC — [nome da feature]

> Modelo. Copie para \`specs/AAMMDD-nome-da-feature.md\` e preencha ANTES de codar.
> A spec diz **o que** construir e **quando está pronto**; o passo a passo da edição é outra coisa.

## Problema
[Que dor real isto resolve? Para quem? O que acontece hoje sem isto?]

## Critérios de aceite (verificáveis)
> Cada linha precisa ser conferível — se não dá para dizer «passou/não passou», reescreva.
- [ ] [ex.: usuário sem sessão que abre /painel cai no login]
- [ ] [ex.: a lista carrega em < 1s com 10k registros]

## Decisões de design
[O que foi escolhido e por quê; o que foi descartado. Decisão estrutural → registre também em DECISIONS.md.]

## Fora de escopo
[O que esta feature NÃO vai fazer — o limite que impede o escopo de crescer sozinho.]

## Passos
[Quebra em passos pequenos, ordenados, cada um entregável e testável.]
`},
```
> Atenção às crases escapadas (`\``) dentro do template literal — copie o bloco como está.

### B2 (P2) — prompt **J** passa a exigir critérios de aceite
**Âncora:** `Objetivo em uma frase e critério de «pronto»`
**Substituir por:**
```
Objetivo em uma frase e **critérios de aceite verificáveis** (lista curta, cada linha conferível como passou/não passou)
```

---

## Tarefa C — `src/niches/game.js`

### C1 (P2) — prompt **H** («Projetar / revisar uma mecânica») ganha critérios de aceite
**Âncora:** `Que SENSAÇÃO esta mecânica cria? Serve à experiência alvo e ao core loop, ou é peso?`
**Substituir por** (a âncora + a linha nova, tudo dentro da mesma string do `body`):
```
Que SENSAÇÃO esta mecânica cria? Serve à experiência alvo e ao core loop, ou é peso?\n- **Critérios de aceite verificáveis:** o que dá para observar no jogo quando a mecânica estiver pronta (cada linha conferível como passou/não passou) — acorde isso ANTES de implementar e registre em MECANICAS.md
```
> O `\n-` é literal dentro da string do prompt (é `\\n` no arquivo-fonte, como as demais linhas do body).

---

## Tarefa D — `validate.js`: check **C16**

**Âncora:** `check("C15 rename spec->WO`
**Substituir por** (insira C16 ANTES do C15, que permanece logo depois):
```javascript
check("C16 SDD leve (wo0054): SPEC.md no dev, criterios de aceite nos prompts, clausula de ambiguidade no analyze", () => {
  const dev=T.normNiche(T.NICHES.dev);
  const spec=(dev.contextFiles||[]).find(f=>f.name==="SPEC.md");
  assert(spec,"dev sem o modelo SPEC.md");
  assert(/Critérios de aceite \(verificáveis\)/.test(spec.content),"SPEC.md sem secao de criterios de aceite");
  assert(/Fora de escopo/.test(spec.content),"SPEC.md sem fora-de-escopo");
  const pj=(dev.promptsExtra||[]).find(p=>p.id==="J");
  assert(pj && /critérios de aceite verificáveis/.test(pj.body({},dev)),"prompt J sem criterios de aceite");
  const gm=T.normNiche(T.NICHES.game);
  const ph=(gm.promptsExtra||[]).find(p=>p.id==="H");
  assert(ph && /Critérios de aceite verificáveis/.test(ph.body({},gm)),"prompt H (game) sem criterios de aceite");
  assert(/ambíguo ou de escala de feature/.test(T.buildClaudeMd(dev)),"analyze sem clausula de ambiguidade");
  return "ok";
});

check("C15 rename spec->WO
```

---

## Tarefa E — docs (append)

- **`meta/DECISIONS.md` → D-087:** SDD leve entra nos nichos de código (análise i-N7). Importada a **espinha**
  (a spec como artefato durável + critério de aceite verificável), **não** a cerimônia do Spec Kit (sem
  `constitution.md` separada — CONTEXT/JOGO + DECISIONS + princípios já são a constituição; sem CLI; sem
  6 arquivos por feature). `dev` ganha o modelo `SPEC.md` (opcional, copiar para `specs/AAMMDD-nome.md`);
  `game` reforça o prompt de mecânica em vez de ganhar arquivo — **porque o teto do `game` não comporta**
  (medido: 6976 > 6900). L3 vira cláusula no princípio `analyze`, limitada a pedido ambíguo/de feature para
  não virar over-flagging. Check C16.
- **`meta/IDEAS.md`:** **i-N7 → INCORPORADA** (P1/P2/L3; P3 dispensado — o enquadramento «constituição» ficou
  no cabeçalho do próprio SPEC.md). Parqueados: **handoff + bloco de config padronizado** (próxima WO) e a
  **análise de enxugamento das Instruções** — agora com evidência dura: o `game` não aceita mais nada.
- **`meta/STATUS.md`:** v1.76.0 → **v1.77.0**; testes **18/18 · 60/60 · 0 erros**; `KIT_VERSION 1.77.0`;
  somar C16; **manter e reforçar a nota do `game` (6879/6900, folga 21 — nada novo cabe até enxugar)**.
- **`meta/CHANGELOG.md`:** entrada v1.77.0 no topo.

---

## Verificação

1. `/check-wo` → APLICÁVEL.
2. `node build.js` · `node validate.js index.html` → **18/18 · 60/60 · 0 erros**. C16 verde.
3. **Teto:** `dev` = **6011** (era 5835; folga 889) · `game` = **6879 inalterado** (folga 21). Nenhum outro nicho muda.
4. **Visual:** nicho **dev** → `SPEC.md` aparece como arquivo **opcional** e baixa com as seções Problema /
   Critérios de aceite / Decisões / Fora de escopo / Passos; o prompt «Planejar uma feature ou fase» pede
   critérios verificáveis. Nicho **game** → o prompt «Projetar / revisar uma mecânica» pede critérios de
   aceite observáveis. Qualquer nicho → CEREBRO, princípio «Analisa antes de aceitar», traz a cláusula de
   ambiguidade **com** a ressalva da tarefa pequena.
5. `git diff --stat` — template + dev.js + game.js + validate.js + index.html + 4 meta-docs + esta WO.

---

## Commit (bloco separado, sem acento)

```bash
git add src/index.template.html src/niches/dev.js src/niches/game.js index.html validate.js \
        meta/CHANGELOG.md meta/DECISIONS.md meta/IDEAS.md meta/STATUS.md \
        meta/workorders/260726-wo0054-sdd-leve.md
git commit -m "feat(codigo): SDD leve nos nichos dev e game - spec de feature, criterios de aceite, clausula de ambiguidade (wo0054, D-087)

- dev ganha o modelo SPEC.md (opcional): problema, criterios de aceite verificaveis, decisoes, fora de escopo
- prompt J (dev) e prompt H (game) passam a exigir criterios de aceite conferiveis antes de implementar
- game NAO ganha arquivo novo: teto nao comporta (medido 6976 > 6900); reforco vai no prompt
- principio analyze ganha clausula de ambiguidade limitada a pedido ambiguo ou de feature
- importada a espinha do SDD, nao a cerimonia (sem constitution.md, sem CLI, sem 6 arquivos por feature)
- check C16; KIT_VERSION 1.77.0; 18/18, 60/60, 0 erros"
git push
```
