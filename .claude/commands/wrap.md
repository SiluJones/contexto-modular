Encerre a tarefa atual:
- Atualize `meta/STATUS.md` (append na «Última sessão»; ajuste a versão se mudou — NÃO reescreva o arquivo).
- Acrescente `D-0NN` / `FIX-0NN` em `meta/DECISIONS.md` se houve decisão ou correção registrável.
- Se alguma análise de `meta/analises/` foi decidida, implementada ou abandonada nesta sessão, atualize o **Status** dela (e os elos «Vira» / «Decisão») — análise não se apaga, muda de estado.
- Me mostre o `git diff` e o comando de commit pronto: uma linha por comando (`git add` / `git commit` / `git push`), mensagem SEM acento, e o `git commit` em bloco separado para eu copiar isolado.
